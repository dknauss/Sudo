# Upstream source registry

Every claim this repo makes about **third-party code** lives here, once. Code comments,
changelog entries, specs, and design docs carry a one-line summary and point at an ID in
this table — they do not restate the citation. This is the same arrangement
`docs/current-metrics.md` uses for counts, and for the same reason: a fact duplicated
across seven files is a fact that will be wrong in six of them within a month.

`bin/verify-sources.sh` checks every row against the live upstream file. It fails when a
snippet has **disappeared** — the claim it supported is now **unsupported and needs
re-verification** (the behaviour may be unchanged; upstream may simply have reformatted
the cited expression across lines, so the one-line snippet no longer matches). It warns
when a snippet has merely **moved** (the line number drifted; upstream trunk moves
constantly and that alone is not a lie).

Every ID carries a registered prefix — currently `GB-` or `FT-`, enforced by `ID_REGEX`
in `bin/verify-sources.sh`. The prefix set, not any one prefix, is what matters: the
checker scans the whole repo for references matching it and fails on any that no longer
have a row here, so a renamed or deleted row cannot leave a dangling citation. An ID
outside the registered set is rejected.

The prefixes are historical rather than semantic and should not be read as a source
taxonomy — `GB-` began as Gutenberg but now covers `wordpress-develop`, `wp-cli`, and
`abilities-api` too; `FT-` covers the Fortress comparison sources. To register a family
that warrants a new prefix, widen `ID_REGEX` in `bin/verify-sources.sh` — one edit. Both
the ID validation and the dangling-reference scan read that one pattern, so there is no
second definition to keep in step, and adding one would reintroduce the drift the single
source removed.

## How to add a row

1. Read the file. Not `grep` — read enough of it to name the **enclosing symbol**, the
   component or function the line sits inside. If you cannot name it, you have not
   verified anything: `starFilled` appearing at some line proves a token's address, not
   that it belongs to the control you are describing. That exact mistake was made in
   #317's own first commit (`503e565`): `starFilled` was cited at a real line that
   belongs to a different control. It spanned five files and was corrected on-branch 24
   minutes later in `724ebcb`, before merge — caught by re-reading the file in context,
   which is the habit this column exists to force.
2. Add the row: a stable ID, the raw URL, the line, an exact snippet, the enclosing
   symbol, and the claim it supports.
3. Run `bin/verify-sources.sh`.
4. In prose, write `see GB-ICON-SWAP in docs/upstream-sources.md` rather than another
   copy of the URL and line.

Escape any `|` inside a snippet as `\|`.

## Registry

| ID | Raw URL | Line | Must contain | Enclosing symbol | Claim it supports |
|---|---|---|---|---|---|
| GB-PRESSED-FILL | https://raw.githubusercontent.com/WordPress/gutenberg/trunk/packages/components/src/button/style.scss | 347 | `background: $components-color-foreground;` | `&.is-pressed` block in `.components-button` | The pressed state of a core button fills with a **neutral foreground token**, not a status/semantic colour. (This one declaration is all this row proves; core sets other backgrounds — `:disabled`/`[aria-disabled]` gets `$gray-600`, `.is-tertiary:hover` an accent mix — none status-coloured, but the check verifies only this line.) The design reading built on it: because core does not colour a pinned button by meaning, WP Sudo carries state on the glyph, not a chip. |
| GB-ICON-SWAP | https://raw.githubusercontent.com/WordPress/gutenberg/trunk/packages/interface/src/components/complementary-area/index.js | 280 | `icon={ showIconLabels ? check : icon }` | `ComplementaryAreaToggle` rendered inside `<PinnedItems>` | A conditional icon on this exact button is core's own construction, so a state-driven glyph is precedent, not invention. Also the mechanism by which core takes the icon slot when "Show button text labels" is on. |
| GB-NO-TOOLTIP | https://raw.githubusercontent.com/WordPress/gutenberg/trunk/packages/interface/src/components/complementary-area/index.js | 281 | `showTooltip={ ! showIconLabels }` | `ComplementaryAreaToggle` rendered inside `<PinnedItems>` | With icon labels on, core suppresses the tooltip too — so that cohort loses both the glyph and the hover affordance, which is why the active chip returns in that mode alone. |
| GB-SELECTED-ICON | https://raw.githubusercontent.com/WordPress/gutenberg/trunk/packages/interface/src/components/complementary-area-toggle/index.js | 58 | `icon={ selectedIcon && isSelected ? selectedIcon : icon }` | `ComplementaryAreaToggle` | The component itself takes a state-selected icon by design. |
| GB-PIN-STAR | https://raw.githubusercontent.com/WordPress/gutenberg/trunk/packages/interface/src/components/complementary-area/index.js | 326 | `icon={ isPinned ? starFilled : starEmpty }` | `interface-complementary-area__pin-unpin-item` — the className on the pin/unpin Button element, L325, immediately above the cited line | **Counter-example, recorded so it is not miscited again.** This is the pin/unpin star inside the complementary-area panel header — a different control from the pinned toolbar button. #317's first commit cited it as precedent for the toolbar toggle; that was wrong, and was corrected on the same branch before merge. |
| GB-SCOPED-SUBSCRIBE | https://raw.githubusercontent.com/WordPress/gutenberg/trunk/packages/data/src/registry.ts | 64 | `storeNameOrDescriptor?: StoreNameOrDescriptor` | `subscribe` closure in `createRegistry` | `subscribe`'s second parameter is an optional store scope, so the indicator's `core/preferences` listener does not run on every editor keystroke. |
| GB-CLI-ASSOC | https://raw.githubusercontent.com/wp-cli/wp-cli/main/php/WP_CLI/Configurator.php | 298 | `preg_match( '\|^--([^=]+)=(.*)\|s', $arg, $matches )` | `Configurator::extract_assoc()` | A WP-CLI associative-argument value is never a PHP int. `extract_assoc()` assigns `false` for `--no-x` (L294), `true` for a bare `--x` (L296) or `-x` (L300), and otherwise the regex-captured remainder of `--x=v` — always a string. The decision this supports: `CLI_Command::resolve_user_id()` recognises a user ID with a digits-only `ctype_digit()` check (not `is_numeric()`, which also accepts floats and exponent notation — see #280's review history) rather than an `is_int()` check, because an `is_int()` check would reject every real `--user=123`. |
| GB-CORE-SESSION-RMW | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/class-wp-user-meta-session-tokens.php | 95 | `update_user_meta( $this->user_id, 'session_tokens', $sessions );` | `update_sessions()` in `WP_User_Meta_Session_Tokens` | Core stores **all** of a user's session tokens in one `session_tokens` user-meta row and rewrites the whole array — `get_sessions()` (L27) reads it, `update_sessions()` writes it back. So WP Sudo's per-login-session proof map carries the same read-modify-write lost-update window core already accepts for its own session store; the design decision this supports is that a human-paced reauth does not warrant a locking scheme core itself does not use. |
| GB-AREA-IDENTIFIER | https://raw.githubusercontent.com/WordPress/gutenberg/trunk/packages/interface/src/components/complementary-area-toggle/index.js | 45 | `${ context.name }/${ name }` | `ComplementaryAreaToggle` | The identifier falls back to `{context.name}/{name}` (later `.replace( '/', ':' )` → `{plugin-name}:{sidebar-name}`), which is what makes the `aria-controls` CSS selector stable and locale-independent. |
| GB-AREA-ARIA-CONTROLS | https://raw.githubusercontent.com/WordPress/gutenberg/trunk/packages/interface/src/components/complementary-area-toggle/index.js | 59 | `aria-controls={ identifier.replace( '/', ':' ) }` | `ComplementaryAreaToggle` | The pinned button's `aria-controls` is the identifier with the first `/` swapped for `:`, which is why `button[aria-controls="wp-sudo-session-indicator:wp-sudo-session-indicator"]` is a stable, locale-independent selector. |
| GB-PINNED-CLASS | https://raw.githubusercontent.com/WordPress/gutenberg/trunk/packages/interface/src/components/pinned-items/index.js | 23 | `'interface-pinned-items'` | `PinnedItemsSlot` (NOT `PinnedItems` — that is the three-line `Fill` at L11-13; the class lives in the `Slot`) | The container class the stylesheet scopes to, so the Options-menu copy of the same toggle is not painted. |
| GB-CORE-NOSIG | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/includes/class-core-upgrader.php | 128 | `$download = $this->download_package( $current->packages->$to_download, false );` | `Core_Upgrader::upgrade()` | Core update packages are **not** signature-checked: `$check_signatures` is passed `false`, so `verify_file_signature()` is never reached on this path. Supports the "WordPress has no working package signing" premise in `core-action-gate-proposal.md` §1/§10 and `core-sudo-gate-implementation-spec.md` §1 — the reason the auto-update channel is a provenance problem (#307) the gate does not solve. Recorded because the **inverse** of this claim was confabulated and nearly drove a spec decision (`llm-lies-log.md` #39). Note: the `GB-` prefix is currently the only one `bin/verify-sources.sh` admits, so it is used here for a `wordpress-develop` source; widening the prefix is tracked separately. |
| GB-NONCE-TOKEN | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/pluggable.php | 2544 | `return substr( wp_hash( $i . '\|' . $action . '\|' . $uid . '\|' . $token, 'nonce' ), -12, 10 );` | `wp_create_nonce()` | A WordPress nonce is derived from the **session token**, not from anything browser-specific. This is the fact that makes #322's browser binding necessary rather than redundant: an attacker holding a stolen `logged_in` cookie holds that same token, so they can mint a nonce that verifies in the victim's browser and lure the victim into issuing the gated request. A binding minted at stash time would therefore land in the *victim's* browser — which is why `Request_Stash::mint_binding_proof()` refuses unless `Sec-Fetch-Site: same-origin`, and why informed confirmation, not the cookie, is the primary control. |
| FT-SUDO-MODE | https://raw.githubusercontent.com/snicco/fortress/beta/docs/modules/session/sudo-mode.md | 26 | `The Fortress sudo mode mirrors the concept of the Linux` | `## Introduction` in *The Fortress sudo mode* | Fortress ships a sudo mode framed on the Linux `sudo` concept — the prior art the comparison matrix positions WP Sudo against. Cited from the public docs; Fortress runtime source is licence-gated and was not read (see FT-EULA). |
| FT-SESSION-TIMEOUTS | https://raw.githubusercontent.com/snicco/fortress/beta/docs/modules/session/session-managment-and-security.md | 274 | `## Relationship between the timeouts` | `## Relationship between the timeouts` section | Fortress's session model exposes distinct absolute / idle / rotation timeouts, which is the row the matrix compares against WP Sudo's single session duration. |
| FT-SESSION-DROPIN | https://raw.githubusercontent.com/snicco/fortress/beta/docs/modules/session/custom-session-storage.md | 42 | `session_token_manager` | *Custom session storage* — the WordPress-hooks paragraph | Fortress replaces session storage through the core `session_token_manager` drop-in hook, giving per-token rows. Supports the matrix's storage-model comparison with WP Sudo's per-verifier proof map. |
| FT-MU-LOADER | https://raw.githubusercontent.com/snicco/fortress/beta/docs/getting-started/02_installation.md | 83 | `directory of the WordPress site where you want` | step 5 of the installation procedure | Fortress deploys as a **must-use** plugin under `mu-plugins/snicco-fortress/`, i.e. a server-level integration rather than an ordinary plugin install — the deployment-model row in the matrix. |
| FT-PROTECTED-CAPS | https://raw.githubusercontent.com/snicco/fortress/beta/docs/configuration/02_configuration_reference.md | 61 | `[protected_capabilities](#protected_capabilities)` | session-module options in the configuration reference | Fortress gates runtime behaviour with `protected_capabilities` / `protected_pages` and `idle_timeout` config, the mechanism the matrix contrasts with WP Sudo's action registry. |
| FT-EULA | https://raw.githubusercontent.com/snicco/fortress/beta/LICENSE.txt | 73 | `otherwise reverse engineer the Software` | clause 3.1 of the Snicco Media EULA | Fortress is distributed under a proprietary EULA whose §3.1 bars reverse engineering and §3.2 bars competitive use. This is why every Fortress claim in this repo is sourced from public documentation and never from its runtime code. |
| GB-ADDUSER-VETO | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/ms-functions.php | 183 | `apply_filters( 'can_add_user_to_blog', true, $user_id, $role, $blog_id )` | `add_user_to_blog()` | `can_add_user_to_blog` is a real veto filter: the very next branch returns the filter's `WP_Error` to the caller. This is the ONE contested seam in spec §6 that needs no core patch — the spec previously claimed it required a signature change. |
| GB-GRANTSA-NOCHECK | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/capabilities.php | 1235 | `$super_admins[] = $user->user_login;` | `grant_super_admin()` | The append at this line is followed immediately (L1236) by `update_site_option( 'site_admins', $super_admins )`, whose return is **discarded** — the function then fires `granted_super_admin` and returns `true` regardless. So a `pre_update_site_option_site_admins` veto is reported to callers as a success. Why spec §6 row 7 marks `grant_super_admin` as needing a core patch rather than a filter. |
| GB-DELUSER-OBSERVE | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/includes/user.php | 381 | `do_action( 'delete_user', $id, $reassign, $user );` | `wp_delete_user()` | The only pre-delete hook is observational and the function returns `bool`, so `core/delete-user` cannot be gated by return value or by this hook — spec §6 row 5's core-patch ask. |
| GB-MSDELUSER-BOOL | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/includes/ms.php | 145 | `function wpmu_delete_user( $id ) {` | `wpmu_delete_user()` | Network-admin user deletion lives in **`wp-admin/includes/ms.php`**, not `wp-includes/ms-functions.php` — spec §6 row 5b named the latter, which does not contain the function. It returns `false` on three guard paths and `true` at `:213`, the same `bool` contract as `wp_delete_user()`, and reaching it does not pass through that function's seam. Within the body only `do_action( 'wpmu_delete_user', $id, $user )` (`:174`) and `do_action( 'deleted_user', $id, null, $user )` (`:211`) fire — both observational. **The "no `apply_filters` to veto through" claim is an absence** scoped to the function body as read in July 2026; per #388 a must-contain snippet cannot falsify an absence, so re-grep before relying on it. Why §6.1's ask covers this function as well as `wp_delete_user()`. |
| GB-SWITCHTHEME-WRITES | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/theme.php | 815 | `update_option( 'current_theme', $new_name );` | `switch_theme()` | `switch_theme()` performs several option writes beyond `template`/`stylesheet` (this one among them) and returns `void`, so vetoing `pre_update_option_template`/`_stylesheet` leaves a half-switched site. Spec §6 row 15. |
| GB-PROFILE-OBSERVE | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/user-edit.php | 149 | `do_action( 'personal_options_update', $user_id );` | `IS_PROFILE_PAGE` — the enclosing conditional at L139; user-edit.php is a script with no function scope, and this action fires only when a user edits their own profile | The self-email seam spec §4.1 named is a `do_action` whose return is discarded — it cannot veto the pending-email write. Basis for the `edit_user()` core-patch ask (#316). |
| GB-LOGGEDIN-COOKIE-PATH | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/pluggable.php | 1197 | `setcookie( LOGGED_IN_COOKIE, $logged_in_cookie, $expire, SITECOOKIEPATH, COOKIE_DOMAIN, $secure_logged_in_cookie, true );` | `wp_set_auth_cookie()` | The **logged-in** cookie is set on `SITECOOKIEPATH` (as well as `COOKIEPATH`) with its own `$secure_logged_in_cookie` — unlike the auth cookie, which uses `ADMIN_COOKIE_PATH`/`PLUGINS_COOKIE_PATH`. The proof cookie must mirror this one (#319). |
| GB-SESSION-TOKEN-SRC | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/user.php | 3691 | `$cookie = wp_parse_auth_cookie( '', 'logged_in' );` | `wp_get_session_token()` | The session token the recent-auth record is keyed to comes from the **logged-in** cookie, which is why the proof must follow that cookie's paths and secure flag rather than the auth cookie's (#319). |
| GB-THEME-SELFHEAL | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/theme.php | 927 | `switch_theme( WP_DEFAULT_THEME );` | `validate_current_theme()` | Core switches themes with **no actor** to recover a site whose theme files are missing, guarded only by `wp_installing()` and the `validate_current_theme` filter. The concrete case behind the no-actor/self-heal carve-out rule in spec §6.2. |
| GB-META-SHORTCIRCUIT | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/meta.php | 250 | `apply_filters( "update_{$meta_type}_metadata", null, $object_id, $meta_key, $meta_value, $prev_value );` | `function update_metadata(` (declared L196, above the cited line) | This row pins the **filter call**. The short-circuit itself — `if ( null !== $check ) { return (bool) $check; }` on the next two lines — is **not** pinned: neither line is unique in this file, so no single-line snippet can hold it. Re-read those two lines before relying on the veto. This is why spec §6 row 6 puts the escalation guard on the `{prefix}capabilities` meta write rather than on `WP_User::set_role()`, which returns `void` and cannot be vetoed. |
| GB-OPTION-PRE-VETO | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/option.php | 901 | `apply_filters( "pre_update_option_{$option}", $value, $old_value, $option );` | `function update_option(` (declared L846, above the cited line) | This row pins the **filter call**. The abort it depends on — the `$value === $old_value \|\| maybe_serialize(...)` guard a few lines below, which returns `false` — is **not** pinned (that line appears twice in the file). Re-read it before relying on the veto. Spec §6.3 relies on this for the registration-policy options — and records its limit: `false` is indistinguishable from "no change needed", and the technique only prevents the *effect* when the option write is the whole effect (contrast `switch_theme()`, §6.1). |
| GB-ABILITY-NOVETO | https://raw.githubusercontent.com/WordPress/abilities-api/trunk/includes/abilities-api/class-wp-ability.php | 631 | `do_action( 'wp_before_execute_ability', $this->name, $input );` | `WP_Ability::execute()` | The Abilities API's pre-execution hook is a `do_action` whose return is discarded — the very next statement is `$result = $this->do_execute( $input );`, so a listener cannot refuse the call. This is the load-bearing basis for spec §4.1.1 rejecting the Abilities API as an *enforcement* point (it remains fine for naming/annotation), and for §6 rejecting the same `do_action` pattern in the gate's own seams. Registered because the **inverse** of this claim was confabulated once already (`llm-lies-log.md` #33 — the hook was asserted to be a gate without reading `execute()`). Prefix note: `GB-` is used here for a WordPress-org repo that is neither Gutenberg nor wordpress-develop; the prefix set is `{GB-, FT-}` and widening it further is tracked separately. |
| GB-UPGRADER-PRE-INSTALL | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/includes/class-wp-upgrader.php | 556 | `$res = apply_filters( 'upgrader_pre_install', true, $args['hook_extra'] );` | `WP_Upgrader::install_package()`, immediately before the filesystem work | Core **already ships a vetoing seam** at the package-write chokepoint: the filter's return is checked with `is_wp_error()` on the next lines and returned, aborting the write — the same short-circuit shape as `pre_delete_post`. This is why the Group-A code-execution closure in `core-action-gate-proposal.md` §3 needs no core patch, and it is the seam the `poc/install-package-gate/` slice enforces at. Same `GB-` prefix caveat as GB-CORE-NOSIG: this is a `wordpress-develop` source, and re-prefixing is tracked in #361. |
| GB-AUTH-COOKIE-PARSE | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/pluggable.php | 1041 | `$cookie_elements = explode( '\|', $cookie );` | `wp_parse_auth_cookie()` | The auth-cookie "parser" only **splits** the cookie and checks it has four parts; it performs no HMAC and no expiry check, and returns the parts regardless. So `wp_get_session_token()`, which reads it, can be made non-empty by any caller attaching a junk cookie — testing it for emptiness is a check an attacker switches off. This is why `poc/install-package-gate/` verifies the token against `WP_Session_Tokens::verify()` instead. |
| GB-AUTOUPDATER-UPGRADE | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/includes/class-wp-automatic-updater.php | 478 | `$upgrade_result = $upgrader->upgrade(` | `WP_Automatic_Updater::update()` | The automatic updater does not implement its own package path: it instantiates the ordinary `Plugin_Upgrader`/`Theme_Upgrader`/`Core_Upgrader` and calls `upgrade()` with generic options. `$hook_extra` is therefore built by those classes exactly as for a manual run, so nothing in the payload reaching `upgrader_pre_install` identifies an automatic update. Supports the proposal's position that provenance must be decided upstream (#307) rather than read at the sink. |
| GB-CORE-UPDATE-SINK | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/includes/class-core-upgrader.php | 178 | `$result = update_core( $working_dir, $wp_dir );` | `public function upgrade(` (declared L66, above the cited line) | Core update writes through `update_core()` directly — that much this row verifies. The wider claim it supports, that `install_package(` occurs nowhere in this file, is an **absence** and is therefore NOT machine-checked: a must-contain snippet can confirm a positive fact but cannot falsify an absence (see #388), so if core added such a call on an adjacent line this row would still pass. Re-grep before relying on the absence. On that basis the shared package sink that covers plugin, theme and language-pack writes does **not** cover core — which is why spec §4.1 carries a separate `core/update-core` row and §5.4 says so explicitly (#302). |
| GB-RESET-ACTORLESS | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/user.php | 3526 | `wp_set_password( $new_pass, $user->ID );` | `function reset_password(` (declared L3511, above the cited line) | The lost-password completion path reaches `wp_set_password()` with **no authenticated actor** — the emailed key is the proof. Gating `wp_set_password()` without carving this out refuses every legitimate reset under default-on fail-closed, which is why spec §6.2 exempts the key-verified reset and gates the authenticated changes at `wp_update_user()` instead (#304). |
| GB-APPPASS-SINK | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/class-wp-application-passwords.php | 89 | `public static function create_new_application_password( $user_id, $args = array() ) {` | `class WP_Application_Passwords` | The single issuance sink both the REST controller and the `authorize-application.php` approve flow reach, and it already returns `WP_Error` (`:95`, `:116`) — so spec §4.1/§6 row 4d gates it here rather than enumerating two surfaces, with no core patch required (#326). |
| GB-ACTIVE-PLUGINS-OPT | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/load.php | 1015 | `$active_plugins = (array) get_option( 'active_plugins', array() );` | `function wp_get_active_and_valid_plugins(` (declared above the cited line) | Core loads plugins from the `active_plugins` **option**, so a direct option write activates code on the next request without `activate_plugin()` ever running. The concrete bypass behind spec §5.5 (#327). |
| GB-INSTALLING-MUTABLE | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/load.php | 1634 | `function wp_installing( $is_installing = null ) {` | `function wp_installing(` | `wp_installing()` is a mutable runtime switch — a `static` seeded from `WP_INSTALLING` but reassignable by any caller passing an argument — not a constant. Spec §8.1 therefore forbids resting the install carve-out on it alone (#329). |
| GB-SESSION-PLUGGABLE | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/class-wp-session-tokens.php | 59 | `return new $manager( $user_id );` | `final public static function get_instance( $user_id ) {` (declared L49, above the cited line) | The session-token store is swappable — the line above instantiates whatever class the `session_token_manager` filter returned (L58) — so a third-party manager may not persist the gate's proof record. Spec §4.2 point 4b requires capability detection and a non-fail-closed degraded mode rather than assuming the store keeps unknown fields (#328). |
| GB-ADD-META-SHORTCIRCUIT | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/meta.php | 90 | `$check = apply_filters( "add_{$meta_type}_metadata", null, $object_id, $meta_key, $meta_value, $unique );` | `function add_metadata(` (declared above the cited line) | The *add* path has its own short-circuit filter, parallel to `update_{$meta_type}_metadata`. A new user's first `{prefix}capabilities` write is an add, so an escalation guard hooked only on update misses user creation (spec §4.2 point 4c). |
| GB-MS-SIGNUP-ROLE | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/ms-functions.php | 2336 | `$role    = $meta['new_role'];` | `add_new_user_to_blog()` | Multisite activation assigns the role from the **signup meta**, never consulting the single-site `default_role` option — so a registration policy enforced only on that option does not constrain network signups (spec §5.6). The chain: `wpmu_activate_signup()` unserialises the signup row's meta and fires `wpmu_activate_user` with it (L1271); `add_new_user_to_blog()` is hooked to that action in `ms-default-filters.php:27`; the cited line reads `new_role` out of that meta and the next line passes it to `add_user_to_blog()`. **Do not cite `ms-functions.php:2296` for this claim.** That line is inside `add_existing_user_to_blog()` — the `/newbloguser/{key}/` invitation path, whose `$details` come from the `new_user_{key}` option, not from signup meta — and `wpmu_activate_signup()`'s own body contains no `add_user_to_blog` call and no `role` reference at all. This row carried that line until it was corrected (`llm-lies-log.md` #59). The "never consults `default_role`" half is an **absence** claim, which no snippet can falsify (#388); it rests on `default_role` appearing nowhere in `ms-functions.php`, which `grep -n default_role` against the cited file re-checks on demand. The fetch date this row was re-verified on is in `llm-lies-log.md` #59; dates do not belong in this file, which docs-lint enforces. |
| GB-UNPACK-BEFORE-INSTALL | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/includes/class-wp-upgrader.php | 358 | `public function unpack_package( $package, $delete_package = true ) {` | `class WP_Upgrader` | Extraction happens in `unpack_package()`, which `run()` calls **before** `install_package()` (L510) and which unzips into `wp_content_dir() . 'upgrade/'`. So a gate placed only at `install_package()` cannot prevent attacker-controlled PHP from reaching a web-reachable directory — spec §5.4's "necessary but not sufficient" correction, surfaced by the PoC slice (#380). |
| GB-UPGRADER-PRE-DOWNLOAD | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/includes/class-wp-upgrader.php | 322 | `$reply = apply_filters( 'upgrader_pre_download', false, $package, $this, $hook_extra );` | `public function download_package(` | The earliest veto in the upgrade path: the next branch returns the filter's value when it is not `false`, so a `WP_Error` stops the download — and therefore the unpack — entirely. `run()` calls `download_package()` (L849), `unpack_package()` (L887) and `install_package()` (L898) in that order, which is why this seam and not `install_package()` is the interactive enforcement point (spec §6 row 14-pre; #387). |
| GB-REFERER-SELFPOST | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-includes/functions.php | 1994 | `if ( $ref && wp_unslash( $_SERVER['REQUEST_URI'] ) !== $ref` | `wp_get_referer()` | `wp_get_referer()` returns **false** when the referer equals the current `REQUEST_URI`. Since `wp_referer_field()` sets `_wp_http_referer` to the URI the form rendered at, a form that posts to its own URI produces no referer at all — so `Request_Stash` stores `return_url = ''` for exactly the self-posting forms #429 is about. This is why the fail-closed landing in `Challenge::build_replay_response_data()` derives the screen from the intercepted request URL and never from `return_url`: an earlier cut of that fix classified the landing by comparing the two, and was dead on arrival for every core self-posting form. |
| GB-USER-NEW-SELFPOST | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/user-new.php | 519 | `<form method="post" name="createuser" id="createuser" class="validate" novalidate="novalidate"` | the `createuser` form in `wp-admin/user-new.php` | The core Add-New-User form carries **no `action` attribute**, so it posts to its own URI. Together with GB-REFERER-SELFPOST this is what makes `user-new.php` both the flagship case for the #429 landing fix and the case a `return_url` comparison could never satisfy. |
| GB-ADMIN-POST-BLANK | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/admin-post.php | 67 | `do_action( 'admin_post' );` | the `empty( $action )` branch of `wp-admin/admin-post.php` | With no `action`, `admin-post.php` fires the bare `admin_post` action and falls off the end of the file: no admin chrome, no `admin_notices`. Supports `Challenge::is_handler_endpoint()` treating it as a handler rather than a screen — landing a refused replay there would cost the user the explanatory notice as well as the form. |
| GB-OPTIONS-ALLSETTINGS | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/options.php | 393 | `<form name="form" action="options.php" method="post" id="all-options">` | the `All Settings` screen block in `wp-admin/options.php` | A bare GET of `options.php` renders the raw "All Settings" dump, not the settings form the user was filling in. Core guards the write path on `'update' === $action` rather than on request method, so a GET falls through to this block. Supports `Challenge::is_handler_endpoint()` classifying it as a handler: returning a refused Settings-API POST "to its screen" would land the user on the full options list, which is worse than the neutral dashboard. |
| GB-ADMIN-PHP-BLANK | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/admin.php | 429 | `do_action( "admin_action_{$action}" );` | the trailing `! empty( $_REQUEST['action'] )` block at the end of `wp-admin/admin.php` | `admin.php` with no `page` parameter never requires `admin-header.php`: it falls past the plugin-page and importer blocks to this final action dispatch and reaches end of file. The response has no admin chrome and fires no `admin_notices`. This is why `admin.php` is a handler for landing purposes — it is also the `pagenow` example in `docs/developer-reference.md` and where third-party settings screens live, so it is the case custom rules hit most. |
| GB-NETWORK-EDIT-REDIRECT | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/network/edit.php | 13 | `$action = $_GET['action'] ?? '';` | the top-of-file action guard in `wp-admin/network/edit.php` | The two lines immediately following this one are `if ( empty( $action ) ) { wp_redirect( network_admin_url() ); exit; }` — the snippet is cited here rather than on the redirect itself because `wp_redirect( network_admin_url() );` occurs twice in the file and would be an ambiguous citation. So network `edit.php` with no action redirects to the network dashboard and exits, discarding any query it was given, including WP Sudo's blocked-replay notice argument. Landing a refused network settings POST there costs the user the explanation as well as the form, which is why it is classified as a handler. Decided from the URL path, not `is_network_admin()`, which is false on the admin-ajax path where the landing is computed. |
| GB-USER-EDIT-DIES | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/user-edit.php | 27 | `} elseif ( ! $user_id && ! IS_PROFILE_PAGE ) {` | the user-resolution guard near the top of `wp-admin/user-edit.php` | A bare GET of `user-edit.php` has `$user_id = 0`, so `IS_PROFILE_PAGE` is false and this branch calls `wp_die( __( 'Invalid user ID.' ) )` — no admin chrome, no `admin_notices`, no form. (`profile.php` is unaffected: it defines `IS_PROFILE_PAGE` true, so the preceding branch assigns the current user.) This is why `user-edit.php` is a handler for landing purposes, and it matters more than the other entries: the built-in `user.promote_profile`, `user.change_password` and `user.change_email` rules all target it and all mark their stash non-replayable, so the fail-closed landing is their normal every-time path rather than an attack-only one. |
| GB-UPDATE-NEEDS-ACTION | https://raw.githubusercontent.com/WordPress/wordpress-develop/trunk/src/wp-admin/update.php | 22 | `if ( isset( $_GET['action'] ) ) {` | the sole top-level conditional wrapping the body of `wp-admin/update.php` | The entire body of `update.php` sits inside this conditional and the file ends with its closing brace, so a bare GET renders nothing at all — `admin-header.php` is required only inside the inner branches. Supports classifying it as a handler: the built-in `plugin.install`, `plugin.upload`, `plugin.update`, `theme.install`, `theme.upload` and `theme.update` rules all target `update.php`, so a refused upload would otherwise land on a blank page having also lost the notice. |
