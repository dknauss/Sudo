{
  "$schema": "https://playground.wordpress.net/blueprint-schema.json",
  "preferredVersions": {
    "php": "8.3",
    "wp": "6.7"
  },
  "landingPage": "/wp-admin/options-general.php?page=wp-sudo-settings",
  "steps": [
    {
      "step": "activatePlugin",
      "pluginName": "WP Sudo",
      "pluginPath": "wp-sudo/wp-sudo.php"
    },
    {
      "step": "login",
      "username": "admin",
      "password": "password"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $u = get_user_by( 'login', 'admin' ); if ( $u ) { foreach ( array( 'manage_wp_sudo', 'view_wp_sudo_activity', 'export_wp_sudo_activity', 'revoke_wp_sudo_sessions' ) as $c ) { $u->add_cap( $c ); } }"
    }
  ]
}
