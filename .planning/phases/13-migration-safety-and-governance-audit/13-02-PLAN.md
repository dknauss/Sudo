---
phase: 13-migration-safety-and-governance-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/security-model.md
  - docs/developer-reference.md
autonomous: true
requirements: [MIG-05, MIG-06]
must_haves:
  truths:
    - "A reader can find, in the security/developer docs, that WP_SUDO_RECOVERY_MODE is the sole break-glass path after compatibility-mode removal"
    - "A reader can follow a documented, reproducible recovery procedure for a misconfigured manage_wp_sudo grant (first-run lockout-safety)"
    - "The docs state that all admin/settings/widget surfaces gate on dedicated Sudo capabilities (manage_wp_sudo), not bare manage_options"
  artifacts:
    - path: "docs/security-model.md"
      provides: "Break-glass / recovery-mode section reflecting strict-only governance"
      contains: "WP_SUDO_RECOVERY_MODE"
    - path: "docs/developer-reference.md"
      provides: "Recovery procedure + capability-model note (dedicated Sudo caps, no bare manage_options)"
      contains: "manage_wp_sudo"
  key_links:
    - from: "docs/security-model.md"
      to: "docs/developer-reference.md"
      via: "consistent statement that WP_SUDO_RECOVERY_MODE is the sole break-glass"
      pattern: "sole|only remaining break-glass"
---

<objective>
Document the lockout-safe first-run story and the recovery path (MIG-06) and record the
capability-model audit outcome (MIG-05) in the existing security and developer docs.
After Phase 12 removed compatibility mode, `WP_SUDO_RECOVERY_MODE` is the sole break-glass
path — the docs must say so plainly and give a reproducible recovery procedure for a
misconfigured `manage_wp_sudo` grant.

Purpose: First-run governance must be clear and lockout-safe. The code already grants
manage_wp_sudo to an administrator on activation (verified); this plan ensures the human
escape hatch is documented and the capability-model facts are recorded.

Output: An updated break-glass/recovery section in docs/security-model.md and a recovery
procedure + capability-model note in docs/developer-reference.md.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-migration-safety-and-governance-audit/13-CONTEXT.md
@.planning/phases/13-migration-safety-and-governance-audit/13-RESEARCH.md

@docs/security-model.md
@docs/developer-reference.md
@includes/functions-governance.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document WP_SUDO_RECOVERY_MODE as the sole break-glass + recovery procedure</name>
  <files>docs/security-model.md, docs/developer-reference.md</files>
  <action>
    Verify-then-document. Confirm against live source before writing (anti-confabulation):
      - functions-governance.php: wp_sudo_is_recovery_mode() / the break-glass gate in
        wp_sudo_can() is the only remaining break-glass path (compatibility mode gone).
      - The activation/first-run grant of manage_wp_sudo to an administrator (the
        upgrade_3_3_0 backfill + activation hook) — state first-run is lockout-safe.

    In docs/security-model.md: update (do not duplicate) the break-glass / recovery section
    so it states plainly that, after the 4.0.0 compatibility-mode removal,
    WP_SUDO_RECOVERY_MODE is the SOLE break-glass path — no other recovery mechanism
    exists. Remove or correct any lingering text implying compatibility mode or another
    escape hatch is available.

    In docs/developer-reference.md: add a short, reproducible "Recovery from a misconfigured
    manage_wp_sudo grant" procedure — e.g. define( 'WP_SUDO_RECOVERY_MODE', true ) in
    wp-config.php grants access so an administrator can re-grant the capability, then remove
    the constant. Keep steps concrete and minimal. Cite the verified governance source in
    the commit message per CLAUDE.md.

    Docs-only commit (skips the Pre-Commit Reviewer per CLAUDE.md): `docs(13): document WP_SUDO_RECOVERY_MODE sole break-glass + recovery procedure (MIG-06)`.
  </action>
  <verify>
    <automated>grep -n "WP_SUDO_RECOVERY_MODE" docs/security-model.md docs/developer-reference.md</automated>
  </verify>
  <done>security-model.md states WP_SUDO_RECOVERY_MODE is the sole break-glass; developer-reference.md has a concrete, reproducible recovery procedure for a misconfigured manage_wp_sudo grant; no doc implies compatibility mode or another escape hatch remains.</done>
</task>

<task type="auto">
  <name>Task 2: Record the capability-model audit outcome (MIG-05 doc side)</name>
  <files>docs/developer-reference.md</files>
  <action>
    Add a brief capability-model note to docs/developer-reference.md stating the audited
    fact: every admin, settings, and dashboard-widget surface gates on dedicated Sudo
    capabilities (manage_wp_sudo for the settings/admin surfaces, view_wp_sudo_activity for
    the dashboard widget) and NOT on bare manage_options. Note the documented exceptions
    that are intentionally not access gates: the WP_SUDO_RECOVERY_MODE break-glass user_can
    check, the recovery-mode meta-cap mapping, and the Access-tab informational panel that
    merely lists manage_options holders (read-only display, not a gate).

    Keep it factual and concise — this records the audit result; the regression lock is the
    unit test in Plan 01 Task 3. Docs-only commit: `docs(13): record capability-model audit — dedicated Sudo caps, no bare manage_options gate (MIG-05)`.
  </action>
  <verify>
    <automated>grep -n "manage_wp_sudo" docs/developer-reference.md</automated>
  </verify>
  <done>developer-reference.md records that admin/settings/widget surfaces gate on dedicated Sudo caps with no bare manage_options access gate, and names the intentional non-gate exceptions.</done>
</task>

</tasks>

<verification>
- `grep -n "WP_SUDO_RECOVERY_MODE" docs/security-model.md docs/developer-reference.md` shows the sole-break-glass statement and the recovery procedure.
- `grep -n "manage_wp_sudo" docs/developer-reference.md` shows the capability-model note.
- Manual review confirms the recovery steps are concrete and reproducible and contain no jargon-only references.
</verification>

<success_criteria>
- WP_SUDO_RECOVERY_MODE is documented as the sole break-glass path (MIG-06).
- A reproducible recovery procedure for a misconfigured grant exists in the developer reference (MIG-06).
- The capability-model audit outcome is recorded (MIG-05 doc side).
</success_criteria>

<output>
After completion, create `.planning/phases/13-migration-safety-and-governance-audit/13-02-SUMMARY.md`
</output>
