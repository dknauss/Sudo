---
phase: 13-migration-safety-and-governance-audit
plan: 03
type: tdd
wave: 2
depends_on: ["13-01"]
files_modified:
  - tests/Integration/UpgraderTest.php
  - tests/Integration/UninstallTest.php
  - tests/Integration/MultisiteTest.php
  - tests/Integration/GovernanceTest.php
autonomous: true
requirements: [MIG-02, MIG-03, MIG-04, MIG-06, MIG-07]
must_haves:
  truths:
    - "Upgrading from a 3.3 install with wp_sudo_governance_mode='compatibility' to 4.0.0 lands on strict governance with the option deleted and no PHP errors"
    - "WP_SUDO_RECOVERY_MODE grants full access after compatibility-mode removal and is the only break-glass path"
    - "Uninstall leaves no wp_sudo_governance_mode option on single-site OR multisite (sitemeta)"
    - "Multisite upgrade deletes the sitemeta option and leaves super-admin capabilities intact with no cross-site bleed"
    - "First activation grants manage_wp_sudo to an administrator; the no-super-admin lockout scenario is recoverable via WP_SUDO_RECOVERY_MODE"
    - "The clear-on-detection path removes a stale option re-added AFTER the version is already stamped 4.0.0"
  artifacts:
    - path: "tests/Integration/UpgraderTest.php"
      provides: "upgrade_4_0_0 deletion (single/multisite), idempotency, wp_roles() regression guard, first-run grant"
      contains: "upgrade_4_0_0"
    - path: "tests/Integration/UninstallTest.php"
      provides: "multisite sitemeta governance-option deletion assertion"
      contains: "get_site_option"
    - path: "tests/Integration/MultisiteTest.php"
      provides: "multisite upgrade governance-option scope + super-admin capability intactness"
      contains: "governance_mode"
    - path: "tests/Integration/GovernanceTest.php"
      provides: "recovery-mode sole break-glass, lockout scenario, clear-on-detection post-stamp"
      contains: "RECOVERY_MODE"
  key_links:
    - from: "tests/Integration/GovernanceTest.php"
      to: "Admin::cleanup_inert_governance_mode_option"
      via: "post-4.0.0-stamp clear-on-detection assertion"
      pattern: "cleanup_inert_governance_mode_option"
    - from: "tests/Integration/UpgraderTest.php"
      to: "Upgrader::upgrade_4_0_0 + wp_roles() priming"
      via: "real-DB maybe_upgrade() with $GLOBALS['wp_roles'] nulled"
      pattern: "wp_roles|\\$GLOBALS\\['wp_roles'\\]"
---

<objective>
Add the real-DB integration coverage that proves the Phase 13 migration-safety guarantees:
the 4.0.0 upgrade routine (single-site + multisite + idempotency + WP 7.0 wp_roles()
regression guard), uninstall correctness on multisite sitemeta, multisite upgrade
capability intactness, recovery-mode as the sole break-glass, the first-run/no-super-admin
lockout scenario, and the clear-on-detection edge case where the option reappears after
the version is already stamped 4.0.0.

Purpose: These scenarios cannot be proven by unit tests (they need real options/sitemeta
tables, a real $GLOBALS['wp_roles'], and real users/roles). They close MIG-02, MIG-03,
MIG-04, MIG-06, and MIG-07.

Output: New/extended test methods across UpgraderTest, UninstallTest, MultisiteTest, and
GovernanceTest — no new files, no new framework installs.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-migration-safety-and-governance-audit/13-RESEARCH.md
@.planning/phases/13-migration-safety-and-governance-audit/13-VALIDATION.md
@.planning/phases/13-migration-safety-and-governance-audit/13-01-SUMMARY.md

@tests/Integration/UpgraderTest.php
@tests/Integration/UninstallTest.php
@tests/Integration/MultisiteTest.php
@tests/Integration/GovernanceTest.php

<interfaces>
<!-- Verified against live source 2026-06-16. Test helpers in tests/Integration/TestCase.php. -->
TestCase helpers:  make_admin( $password = ... ): WP_User;  update_wp_sudo_option( $key, $value );  get_wp_sudo_option( $key );
Upgrader:          public const VERSION_OPTION = 'wp_sudo_db_version';  public function maybe_upgrade(): void;
                   private function upgrade_4_0_0(): void;   // added in Plan 01 — deletes wp_sudo_governance_mode (both stores)
Admin:             public const GOVERNANCE_CAPS = array( ... );  public function cleanup_inert_governance_mode_option(): void;  // added in Plan 01
Governance:        wp_sudo_can( $cap, $user_id = null ): bool;  wp_sudo_is_recovery_mode(): bool;   // WP_SUDO_RECOVERY_MODE constant
Existing tests:    GovernanceTest::test_stale_compatibility_option_is_treated_as_strict();  UpgraderTest::test_3_3_0_backfill_survives_uninitialized_wp_roles_global() (try/finally $wp_roles snapshot pattern)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: upgrade_4_0_0 integration coverage + WP 7.0 wp_roles() regression guard + first-run grant</name>
  <files>tests/Integration/UpgraderTest.php</files>
  <behavior>
    Add real-DB tests (RED first — they exercise Plan 01's new routine; they must fail if
    the routine is absent or wrong, then pass against Plan 01's code):
    - test_upgrade_4_0_0_deletes_stale_governance_mode_option: stamp VERSION_OPTION='3.4.0',
      set wp_sudo_governance_mode='compatibility', run (new Upgrader())->maybe_upgrade();
      assert get_option('wp_sudo_governance_mode') === false and version stamped WP_SUDO_VERSION.
    - test_upgrade_4_0_0_is_idempotent_when_option_absent: stamp '3.4.0', option NOT set,
      run maybe_upgrade(); assert no error and version stamped.
    - test_upgrade_4_0_0_does_not_regress_wp_roles_priming: stamp '3.3.0', set option, snapshot
      $GLOBALS['wp_roles'] in try/finally (copy the existing test_3_3_0_backfill pattern),
      set $GLOBALS['wp_roles']=null, run maybe_upgrade(); assert no fatal and version stamped.
      This is the regression guard for the proven WP 7.0 fatal — DO NOT move the wp_roles() line.
    - test_first_activation_grants_manage_wp_sudo_to_administrator: make_admin(), run the
      activation/first-run grant path (the upgrade_3_3_0 backfill or activation hook used to
      grant manage_wp_sudo), assert the administrator holds manage_wp_sudo (MIG-06 first-run).
  </behavior>
  <action>
    Append the four test methods to tests/Integration/UpgraderTest.php following the existing
    structure (update_wp_sudo_option / maybe_upgrade / DB assertion; try/finally $wp_roles
    snapshot for the regression guard). Reuse make_admin(). RED then GREEN against Plan 01.
    Commit per the Pre-Commit Reviewer Workflow: `test(13): integration coverage for upgrade_4_0_0 + wp_roles regression + first-run grant (MIG-01, MIG-06)`.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit tests/Integration/UpgraderTest.php</automated>
  </verify>
  <done>upgrade_4_0_0 deletion, idempotency, the wp_roles() WP 7.0 regression guard, and the first-run manage_wp_sudo grant all have passing integration tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multisite upgrade + uninstall sitemeta coverage (MIG-03, MIG-04)</name>
  <files>tests/Integration/MultisiteTest.php, tests/Integration/UninstallTest.php</files>
  <behavior>
    MultisiteTest (real network):
    - test_multisite_upgrade_deletes_governance_mode_from_sitemeta: stamp VERSION_OPTION='3.4.0'
      (site-option scope), update_site_option('wp_sudo_governance_mode','compatibility'),
      run maybe_upgrade(); assert get_site_option('wp_sudo_governance_mode') === false.
    - test_multisite_upgrade_preserves_super_admin_capabilities: capture a super admin's
      capabilities before, run the upgrade, assert super-admin caps intact and no cross-site
      capability bleed (verify a per-site admin on a second blog did NOT gain network caps).
    UninstallTest (multisite path):
    - Extend test_multisite_uninstall_cleans_user_meta (or add a sibling test) to seed
      update_site_option('wp_sudo_governance_mode','compatibility') in arrange and assert
      get_site_option('wp_sudo_governance_mode') === false after uninstall — closes the
      RESEARCH Pitfall 5 multisite scope gap. Keep the existing single-site assertion (line ~166).
  </behavior>
  <action>
    Add the MultisiteTest methods following its existing network-setup conventions; extend the
    UninstallTest multisite test to seed and assert the sitemeta governance option deletion.
    RED then GREEN (the upgrade deletion proves Plan 01; uninstall is verify-then-lock against
    existing uninstall.php delete_site_option at line ~180). Commit per the Pre-Commit
    Reviewer Workflow: `test(13): multisite upgrade + uninstall sitemeta governance-option coverage (MIG-03, MIG-04)`.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit tests/Integration/MultisiteTest.php tests/Integration/UninstallTest.php</automated>
  </verify>
  <done>Multisite upgrade deletes the sitemeta option and preserves super-admin caps with no cross-site bleed; multisite uninstall seeds and asserts sitemeta governance-option deletion; single-site uninstall assertion retained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Recovery-mode sole break-glass + lockout scenario + clear-on-detection post-stamp (MIG-02, MIG-07)</name>
  <files>tests/Integration/GovernanceTest.php</files>
  <behavior>
    Add real-DB tests:
    - test_recovery_mode_is_sole_break_glass: with WP_SUDO_RECOVERY_MODE active, assert
      wp_sudo_can(...) grants access for the break-glass path; with it inactive and the user
      lacking the cap, assert access is denied — proving no OTHER break-glass remains after
      compatibility-mode removal (MIG-02). (Drive the constant per the suite's pattern for
      runtime constants; if it cannot be redefined mid-test, use wp_sudo_is_recovery_mode()
      filter/override hook or document the constant-define approach in the test.)
    - test_no_super_admin_lockout_is_recoverable: seed a state with no manage_wp_sudo holder
      (or no super admin on multisite), assert that WP_SUDO_RECOVERY_MODE restores access so
      an operator can re-grant — the documented lockout-safe escape hatch (MIG-07 lockout).
    - test_clear_on_detection_removes_option_when_already_at_4_0_0: stamp VERSION_OPTION='4.0.0'
      (routine will NOT re-run), update_option('wp_sudo_governance_mode','compatibility'),
      make_admin() with the GOVERNANCE_CAPS and wp_set_current_user, call
      (new Admin())->cleanup_inert_governance_mode_option(); assert
      get_option('wp_sudo_governance_mode') === false and the static cleared flag is set
      (read via reflection per the suite pattern) — the post-stamp self-heal path (MIG-07).
  </behavior>
  <action>
    Append the three test methods to tests/Integration/GovernanceTest.php, reusing make_admin()
    and the existing stale-option test as a template. For the recovery-mode constant, follow
    the project's established approach for WP_SUDO_RECOVERY_MODE in tests (check existing
    recovery-mode tests; do not add a production test shim — if the constant must be defined,
    define it in the test process per the suite convention, otherwise use the recovery-mode
    detection seam). RED then GREEN. Commit per the Pre-Commit Reviewer Workflow:
    `test(13): recovery-mode sole break-glass + lockout recovery + clear-on-detection post-stamp (MIG-02, MIG-07)`.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit tests/Integration/GovernanceTest.php</automated>
  </verify>
  <done>WP_SUDO_RECOVERY_MODE is proven the sole break-glass; the no-super-admin lockout is proven recoverable via recovery mode; clear-on-detection removes a stale option re-added after the 4.0.0 version stamp; all green.</done>
</task>

</tasks>

<verification>
- `composer test:integration` is green across UpgraderTest, UninstallTest, MultisiteTest, GovernanceTest.
- `composer test:unit` remains green (no regression from Plan 01).
- `composer analyse` (PHPStan L6) clean.
- The wp_roles() priming line in maybe_upgrade() is unchanged (regression guard passes).
</verification>

<success_criteria>
- MIG-01 (integration side): upgrade_4_0_0 deletion + idempotency + WP 7.0 regression guard proven on real DB.
- MIG-02: WP_SUDO_RECOVERY_MODE is the sole break-glass — proven.
- MIG-03: uninstall deletes the governance option on single-site AND multisite sitemeta — proven.
- MIG-04: multisite upgrade deletes the sitemeta option and preserves super-admin caps with no cross-site bleed — proven.
- MIG-06: first activation grants manage_wp_sudo to an administrator — proven.
- MIG-07: no-super-admin lockout recovery + clear-on-detection post-stamp — proven.
- Full unit + integration suites and PHPStan L6 green.
</success_criteria>

<output>
After completion, create `.planning/phases/13-migration-safety-and-governance-audit/13-03-SUMMARY.md`
</output>
