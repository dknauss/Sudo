---
phase: 04-wpgraphql-persisted-query-and-wsal-sensor
plan: 02
type: tdd
wave: 2
depends_on:
  - 04-01
files_modified:
  - bridges/wp-sudo-wsal-sensor.php
  - tests/Unit/WsalSensorBridgeTest.php
  - includes/class-admin.php
autonomous: true

must_haves:
  truths:
    - "WSAL integration is optional and activates only when WSAL APIs are present"
    - "WSAL sensor consumes existing WP Sudo audit hooks without altering hook contracts"
    - "Bridge is inert and side-effect free when WSAL is unavailable"
    - "WSAL mapping is explicit for gated/blocked/allowed/replayed/lockout lifecycle hooks"
  artifacts:
    - path: "bridges/wp-sudo-wsal-sensor.php"
      provides: "WSAL sensor bridge mapping WP Sudo hooks to WSAL events"
      contains: "wp_sudo_action_blocked"
    - path: "tests/Unit/WsalSensorBridgeTest.php"
      provides: "Unit coverage for WSAL presence/absence and event mapping contract"
      contains: "wsal"
  key_links:
    - from: "WP Sudo audit hooks"
      to: "WSAL event records"
      via: "bridge hook listeners"
      pattern: "wp_sudo_action_"
---

<objective>
Implement WSAL sensor bridge via TDD using WP Sudo audit hooks as source events.

Purpose: deliver first-party enterprise audit visibility without introducing hard dependency on WSAL.
Output: optional WSAL bridge + unit tests + admin discoverability hook/docs touchpoint.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/ROADMAP.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/04-wpgraphql-persisted-query-and-wsal-sensor/04-CONTEXT.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/04-wpgraphql-persisted-query-and-wsal-sensor/04-RESEARCH.md
@/Users/danknauss/Documents/GitHub/wp-sudo/includes/class-admin.php
@/Users/danknauss/Documents/GitHub/wp-sudo/docs/developer-reference.md
@/Users/danknauss/Documents/GitHub/wp-sudo/tests/Unit/WebAuthnBridgeTest.php
</context>

<tasks>

<task type="tdd">
  <name>Task 1 RED: Add failing bridge tests for WSAL integration contract</name>
  <files>tests/Unit/WsalSensorBridgeTest.php</files>
  <action>
Add failing tests that define expected WSAL bridge behavior.

Required coverage:
1. Bridge registers listeners for key WP Sudo audit hooks.
2. Bridge does not emit WSAL events when WSAL APIs/classes are unavailable.
3. Bridge maps hook payload fields (`user_id`, `rule_id`, `surface`) into structured WSAL event data when WSAL is available.
4. Hook contract is pass-through (bridge does not mutate WP Sudo hook args or flow).

Run focused tests (expect RED):
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:unit -- tests/Unit/WsalSensorBridgeTest.php --do-not-cache-result
composer analyse:phpstan
```

Commit: `test(04-02): add failing unit tests for wsal sensor bridge contract`
  </action>
  <verify>
WSAL bridge tests fail before bridge implementation exists.
  </verify>
  <done>RED baseline established for WSAL bridge behavior.</done>
</task>

<task type="tdd">
  <name>Task 2 GREEN: Implement optional WSAL bridge using audit hooks</name>
  <files>bridges/wp-sudo-wsal-sensor.php includes/class-admin.php</files>
  <action>
Implement bridge to satisfy Task 1.

Implementation requirements:
1. Bridge file loads safely and checks WSAL availability before emitting events.
2. Register listeners for relevant WP Sudo hooks (gated/blocked/allowed/replayed, lockout/failure/session/tamper as applicable).
3. Emit structured WSAL events with stable event IDs/context payload.
4. Keep bridge optional; no runtime errors when WSAL absent.
5. Add minimal admin help/docs mention so operators can discover the bridge.

Run verification:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:unit -- tests/Unit/WsalSensorBridgeTest.php --do-not-cache-result
composer analyse:phpstan
```

Commit: `feat(04-02): add optional wsal sensor bridge for wp sudo audit hooks`
  </action>
  <verify>
WSAL bridge tests pass with no static-analysis regressions.
  </verify>
  <done>Optional WSAL integration is implemented and hook-contract safe.</done>
</task>

<task type="tdd">
  <name>Task 3 REFACTOR: Stabilize event mapping and naming</name>
  <files>bridges/wp-sudo-wsal-sensor.php tests/Unit/WsalSensorBridgeTest.php</files>
  <action>
Refactor mapping constants/comments and keep naming consistent with existing bridge style.

Requirements:
1. Event mapping table is centralized and readable.
2. Comments clearly describe WSAL version assumptions and no-op behavior.
3. No behavior changes from Task 2.

Run checks:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:unit -- tests/Unit/WsalSensorBridgeTest.php --do-not-cache-result
composer analyse:phpstan
composer lint
```

Commit: `refactor(04-02): tidy wsal sensor bridge mapping and documentation`
  </action>
  <verify>
Bridge behavior remains stable and quality gates stay green.
  </verify>
  <done>WSAL bridge code is maintainable and implementation assumptions are explicit.</done>
</task>

</tasks>

<verification>
- `composer test:unit -- tests/Unit/WsalSensorBridgeTest.php --do-not-cache-result`
- `composer analyse:phpstan`
- `composer lint`
</verification>

<success_criteria>
- WSAL bridge works when WSAL is present
- Bridge is safely inert when WSAL is absent
- Audit hook contracts remain unchanged
</success_criteria>

<output>
After completion, create `/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/04-wpgraphql-persisted-query-and-wsal-sensor/04-02-SUMMARY.md`
</output>
