---
phase: 21-two-factor-source-refresh-and-guard-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md
  - bridges/wp-sudo-two-factor-lifecycle-bridge.php
  - docs/two-factor-integration.md
  - tests/Unit/TwoFactorLifecycleBridgeTest.php
  - .planning/phases/21-two-factor-source-refresh-and-guard-design/21-01-SUMMARY.md
autonomous: true
requirements:
  - TFA-SRC-01
  - TFA-SRC-02
  - TFA-GUARD-06
  - TFA-REST-01
  - TFA-REST-02
must_haves:
  truths:
    - "Current upstream WordPress/two-factor source is checked before implementation planning relies on routes, form fields, hooks, nonce/action names, meta keys, or provider-normalization behavior."
    - "Every third-party technical claim added or changed by this phase is source-cited with upstream URL, commit or revision, and checked date."
    - "The profile-provider guard design is narrow, idempotent, enrollment-aware, and explicitly not a broad profile-save gate."
    - "Existing REST lifecycle bridge behavior for backup-code generation and TOTP create/delete remains unchanged unless live upstream source forces a documented re-scope."
    - "Patchstack remains fixture-blocked and out of implementation/runtime-claim scope."
  artifacts:
    - path: ".planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md"
      provides: "Source evidence and guard predicate design for Phase 22"
      contains: "WordPress/two-factor"
    - path: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      provides: "Accurate bridge comments and source-citation posture for existing REST rules and future profile guard boundary"
      contains: "two-factor/1.0"
    - path: "tests/Unit/TwoFactorLifecycleBridgeTest.php"
      provides: "Targeted REST bridge assurance tests"
      contains: "two_factor.totp_manage"
  key_links:
    - from: "21-SOURCE-EVIDENCE.md"
      to: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      via: "current source evidence drives comment updates and preserves existing REST rule contract"
      pattern: "generate-backup-codes|/totp|_two_factor_enabled_providers|_two_factor_provider"
    - from: "21-SOURCE-EVIDENCE.md"
      to: "Phase 22 implementation"
      via: "guard predicate design names the exact lifecycle changes to test before behavior changes"
      pattern: "unrelated profile|no-op|primary provider|first enrollment"
---

<objective>
Refresh upstream `WordPress/two-factor` source evidence and design the classic profile-provider guard predicate without shipping behavior changes.

Purpose: Phase 22 will implement a security guard around Two Factor profile-provider lifecycle changes. Before that implementation, Phase 21 must prove the upstream contracts are current, preserve existing REST bridge coverage, and write a narrow guard design that avoids broad profile-save gating.

Output: A source-cited `21-SOURCE-EVIDENCE.md`, reconciled bridge comments if current comments are stale, targeted bridge-test validation, and a Phase 21 summary. If upstream drift changes any scoped contract, stop and re-scope instead of implementing or silently adapting.
</objective>

<execution_context>
@/Users/danknauss/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-two-factor-source-refresh-and-guard-design/21-CONTEXT.md
@.planning/phases/21-two-factor-source-refresh-and-guard-design/21-RESEARCH.md
@.planning/phases/21-two-factor-source-refresh-and-guard-design/21-VALIDATION.md
@bridges/wp-sudo-two-factor-lifecycle-bridge.php
@tests/Unit/TwoFactorLifecycleBridgeTest.php
@docs/two-factor-integration.md
@AGENTS.md

<interfaces>
Existing bridge contract:
- Bridge file: `bridges/wp-sudo-two-factor-lifecycle-bridge.php`
- Registration hook: `wp_sudo_gated_actions`
- Current REST rules: `two_factor.backup_codes_generate` and `two_factor.totp_manage`
- Existing target test: `tests/Unit/TwoFactorLifecycleBridgeTest.php`

Relevant WP Sudo matcher seam:
- Admin rules can define `admin['callback']`; `Gate::matches_admin_pagenow()` calls it after matching pagenow/action/method.
- Existing profile callbacks narrow broad profile forms; the future Two Factor predicate should follow that pattern.

Locked user decisions from `21-CONTEXT.md`:
- Require Two Factor nonce/action contract plus lifecycle fields before evaluating the future profile guard.
- Cover both self-profile and admin-edited target-user lifecycle changes.
- Do not gate normalized no-op provider submissions.
- Ignore unsupported/unknown provider keys after normalization to match upstream behavior.
- Gate first TOTP-backed enrollment when it creates a future sudo factor.
- Keep Patchstack as fixture-blocked/manual-test only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture current upstream Two Factor source evidence</name>
  <files>.planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md</files>
  <action>
    Fetch current upstream `WordPress/two-factor` source from primary GitHub sources and write the evidence artifact.

    1. Query the current upstream master commit:
       - `curl -fsSL https://api.github.com/repos/WordPress/two-factor/commits/master`
       - Extract the 40-character SHA.
    2. Fetch these exact raw files at that SHA:
       - `class-two-factor-core.php`
       - `providers/class-two-factor-backup-codes.php`
       - `providers/class-two-factor-totp.php`
    3. Inspect and record implementation-ready findings in `21-SOURCE-EVIDENCE.md`:
       - REST namespace constant;
       - backup-code REST route and methods;
       - TOTP REST route and methods;
       - REST `user_id` parameter posture;
       - classic profile render hooks and update hooks;
       - nonce field name and action;
       - enabled-provider and primary-provider field/meta names;
       - TOTP secret meta key;
       - provider normalization / supported-provider intersection behavior;
       - primary-provider update/delete behavior;
       - current-user/session side effects relevant to first enrollment or provider removal.
    4. Compare these findings with the Phase 19 seed evidence and current bridge comments.
       - If route/method/field/hook/meta-key/normalization drift changes Phase 22 implementation assumptions, stop and write `## RE-SCOPE REQUIRED` in the evidence file and summary; do not continue to Task 2 as if ready.
       - If no material drift exists, write `## Decision: proceed` in the evidence file.
    5. Include source URLs, SHA, and checked date for every upstream file used. Do not use training data for any Two Factor internals.
  </action>
  <verify>
    <automated>test -f .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md</automated>
    <automated>grep -Eq '[0-9a-f]{40}' .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md</automated>
    <automated>grep -E 'class-two-factor-core\.php|class-two-factor-backup-codes\.php|class-two-factor-totp\.php|checked 2026|Checked: 2026' .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md</automated>
    <automated>grep -E 'two-factor/1\.0|generate-backup-codes|/totp|_nonce_user_two_factor_options|user_two_factor_options|_two_factor_enabled_providers|_two_factor_provider|_two_factor_totp_key' .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md</automated>
    <automated>grep -Eq 'Decision: proceed|RE-SCOPE REQUIRED' .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md</automated>
  </verify>
  <done>`21-SOURCE-EVIDENCE.md` contains current source URLs, SHA/date, implementation-ready findings, and a proceed vs re-scope decision.</done>
</task>

<task type="auto">
  <name>Task 2: Write guard predicate design and reconcile stale comments</name>
  <files>.planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md, bridges/wp-sudo-two-factor-lifecycle-bridge.php, docs/two-factor-integration.md</files>
  <action>
    If Task 1 found material upstream drift and wrote `RE-SCOPE REQUIRED`, do not design around stale assumptions. Instead, document the blocker in the summary and stop for user/planning input.

    Otherwise, append or complete a `## Profile provider guard design` section in `21-SOURCE-EVIDENCE.md` that specifies the future Phase 22 predicate:
    - Match only classic profile saves (`profile.php` / `user-edit.php`, `action=update`, `POST`) when Two Factor's nonce/action contract and lifecycle fields are present.
    - Resolve the target user for self-profile and admin-edited profile saves.
    - Normalize submitted enabled providers by ignoring dummy empty values, ignoring unsupported/unknown provider keys after upstream-style normalization, intersecting with supported providers, and treating ordering as irrelevant.
    - Compare normalized submitted provider set and submitted primary provider against current user meta.
    - Gate meaningful lifecycle changes: enabling/disabling providers, changing primary provider, removing/replacing TOTP-backed state, and first TOTP-backed enrollment that creates a future sudo factor.
    - Do not gate unrelated profile saves or normalized no-op submissions.
    - Keep Patchstack out of scope.

    Then reconcile stale comments if they contradict the evidence/design. In particular, update bridge comments that still imply the future profile predicate excludes enrollment or that first enrollment is intentionally not part of the lifecycle boundary. Keep behavior unchanged: do not add the profile guard in Phase 21.

    Update `docs/two-factor-integration.md` only if the source refresh shows an existing source claim is stale or if a brief maintainer-facing note is needed to avoid contradiction before Phase 23. Keep broad public documentation work for Phase 23.
  </action>
  <verify>
    <automated>grep -E 'unrelated profile|no-op|enabled-provider|primary provider|first TOTP-backed enrollment|Patchstack' .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md</automated>
    <automated>! grep -n 'enrollment-excluding' bridges/wp-sudo-two-factor-lifecycle-bridge.php</automated>
    <automated>! grep -n 'First-time enrollment is intentionally not blocked here' bridges/wp-sudo-two-factor-lifecycle-bridge.php</automated>
    <automated>git diff -- bridges/wp-sudo-two-factor-lifecycle-bridge.php docs/two-factor-integration.md .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md</automated>
  </verify>
  <done>The evidence artifact contains the guard design and any stale bridge/docs comments are reconciled without implementing new guard behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Validate REST bridge preservation and summarize Phase 21</name>
  <files>tests/Unit/TwoFactorLifecycleBridgeTest.php, .planning/phases/21-two-factor-source-refresh-and-guard-design/21-01-SUMMARY.md</files>
  <action>
    Run targeted validation and write the phase summary.

    1. Run targeted bridge tests:
       - `./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/TwoFactorLifecycleBridgeTest.php`
    2. Run static/doc checks from `21-VALIDATION.md`.
    3. Run `git diff --check`.
    4. If bridge comments or docs changed PHP line counts, run `composer verify:metrics`; update `docs/current-metrics.md` only if required by the verifier. Do not guess counts.
    5. Write `.planning/phases/21-two-factor-source-refresh-and-guard-design/21-01-SUMMARY.md` with:
       - upstream SHA, checked date, and source URLs;
       - proceed vs re-scope decision;
       - guard predicate design summary;
       - files modified;
       - exact validation commands and results;
       - explicit note that Patchstack remains fixture-blocked and out of implementation scope;
       - explicit note that no product version bump or release tag was created.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/TwoFactorLifecycleBridgeTest.php</automated>
    <automated>grep -Eq 'Decision: proceed|RE-SCOPE REQUIRED' .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md</automated>
    <automated>grep -E 'Patchstack.*fixture|fixture-blocked.*Patchstack' .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md .planning/phases/21-two-factor-source-refresh-and-guard-design/21-01-SUMMARY.md</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Targeted bridge validation passes, Phase 21 evidence/design is summarized, and the summary clearly states whether Phase 22 can proceed or must re-scope.</done>
</task>

</tasks>

<verification>
Overall phase checks:
- `test -f .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md`
- `grep -Eq '[0-9a-f]{40}' .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md`
- `grep -E 'class-two-factor-core\.php|class-two-factor-backup-codes\.php|class-two-factor-totp\.php' .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md`
- `grep -E 'unrelated profile|no-op|enabled-provider|primary provider|first TOTP-backed enrollment' .planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md`
- `./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/TwoFactorLifecycleBridgeTest.php`
- `git diff --check`
</verification>

<success_criteria>
- TFA-SRC-01: Maintainer can see refreshed upstream source evidence before implementation changes rely on third-party details.
- TFA-SRC-02: Added/changed third-party claims include upstream URL, commit/revision, and checked date.
- TFA-GUARD-06: Guard predicate design is idempotent, enrollment-aware, and not a broad profile-save gate.
- TFA-REST-01 and TFA-REST-02: Existing REST bridge coverage is validated and remains unchanged unless a re-scope is explicitly documented.
- Patchstack remains fixture-blocked and no runtime support claim or bridge behavior is added.
</success_criteria>

<output>
After completion, create `.planning/phases/21-two-factor-source-refresh-and-guard-design/21-01-SUMMARY.md`.
</output>
