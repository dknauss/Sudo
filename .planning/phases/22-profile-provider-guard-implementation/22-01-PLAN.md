---
phase: 22-profile-provider-guard-implementation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-profile-provider-guard-implementation/22-DESIGN-BRIEF.md
  - bridges/wp-sudo-two-factor-lifecycle-bridge.php
  - tests/Unit/TwoFactorLifecycleBridgeTest.php
  - tests/bootstrap.php
autonomous: true
requirements:
  - TFA-GUARD-01
  - TFA-GUARD-02
  - TFA-GUARD-03
  - TFA-GUARD-04
  - TFA-GUARD-05
user_setup: []
must_haves:
  truths:
    - "Unrelated classic profile saves without upstream Two Factor lifecycle fields are not gated."
    - "Normalized no-op Two Factor provider resubmissions are not gated."
    - "Enabled-provider additions/removals and primary-provider changes match the profile guard rule."
    - "TOTP-backed first enrollment and removal/replacement lifecycle changes match the profile guard rule when they can create or alter a future sudo factor."
    - "Existing Two Factor REST backup-code and TOTP create/delete bridge rules remain unchanged."
  artifacts:
    - path: ".planning/phases/22-profile-provider-guard-implementation/22-DESIGN-BRIEF.md"
      provides: "Security-sensitive pre-implementation design review record"
      min_lines: 8
    - path: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      provides: "Optional upstream Two Factor REST and classic profile lifecycle gated rules"
      contains: "two_factor.profile_provider_lifecycle"
    - path: "tests/Unit/TwoFactorLifecycleBridgeTest.php"
      provides: "TDD coverage for profile guard false/true predicate cases and REST preservation"
      contains: "profile_provider_lifecycle"
    - path: "tests/bootstrap.php"
      provides: "Unit-test stub support for source-verified Two_Factor_Core provider normalization"
      contains: "get_supported_providers_for_user"
  key_links:
    - from: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      to: "wp_sudo_gated_actions"
      via: "add_filter registers optional bridge rules only when Two_Factor_Core exists"
      pattern: "add_filter\\(\\s*'wp_sudo_gated_actions'"
    - from: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      to: "profile.php/user-edit.php"
      via: "admin rule pagenow/actions/method plus callback predicate"
      pattern: "profile\\.php|user-edit\\.php|_nonce_user_two_factor_options"
    - from: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      to: "Two_Factor_Core::get_supported_providers_for_user"
      via: "upstream-style provider normalization before comparison"
      pattern: "get_supported_providers_for_user"
    - from: "tests/Unit/TwoFactorLifecycleBridgeTest.php"
      to: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      via: "captured wp_sudo_gated_actions callback and direct admin callback assertions"
      pattern: "capture_gated_actions_filter"
---

<objective>
Implement the optional upstream WordPress Two Factor classic profile-provider lifecycle guard with TDD.

Purpose: Close the profile-provider lifecycle gap without broad profile-save gating and without changing existing REST lifecycle bridge behavior.
Output: A reviewed design brief, failing-then-green unit tests, and a narrow `wp_sudo_gated_actions` admin rule in the existing optional bridge.
</objective>

<execution_context>
@/Users/danknauss/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-profile-provider-guard-implementation/22-CONTEXT.md
@.planning/phases/22-profile-provider-guard-implementation/22-VALIDATION.md
@.planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md
@.planning/phases/21-two-factor-source-refresh-and-guard-design/21-01-SUMMARY.md
@AGENTS.md
@CLAUDE.md
@bridges/wp-sudo-two-factor-lifecycle-bridge.php
@tests/Unit/TwoFactorLifecycleBridgeTest.php
@tests/bootstrap.php

<interfaces>
<!-- Existing rule seam used by this plan. Executor should use these contracts directly. -->

From `bridges/wp-sudo-two-factor-lifecycle-bridge.php`:
```php
add_filter(
	'wp_sudo_gated_actions',
	static function ( array $rules ): array {
		if ( ! class_exists( 'Two_Factor_Core' ) ) {
			return $rules;
		}
		// Existing REST rules: two_factor.backup_codes_generate and two_factor.totp_manage.
		return $rules;
	}
);
```

From `includes/class-gate.php` admin matching:
```php
// Gate checks admin['pagenow'], admin['actions'], admin['method'], then calls admin['callback'].
// If the callback returns false, the admin request is not gated.
```

Source-verified upstream Two Factor classic profile contract from Phase 21:
```text
pagenow: profile.php or user-edit.php
method: POST
action: update
nonce field: _nonce_user_two_factor_options
nonce action: user_two_factor_options
enabled providers field: _two_factor_enabled_providers[]
dummy enabled-provider value: empty string
primary provider field: _two_factor_provider
relevant user meta: _two_factor_enabled_providers, _two_factor_provider, _two_factor_totp_key
provider normalization: intersect submitted provider keys with Two_Factor_Core::get_supported_providers_for_user( $user_id )
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete security-sensitive design review before tests/code</name>
  <files>.planning/phases/22-profile-provider-guard-implementation/22-DESIGN-BRIEF.md</files>
  <action>Create `22-DESIGN-BRIEF.md` with a 3-8 sentence design brief covering: the hijacked-session/factor-lifecycle threat, the existing optional bridge file to change, the `wp_sudo_gated_actions` admin-rule approach, the exact cases that must be blocked, and the cases that must NOT be blocked. Then spawn the reviewer agent per `CLAUDE.md` with a factual critique prompt; do not ask it to approve. Incorporate any reviewer objections into the brief before touching tests or production code. Do not create or modify `reviewer-approved`; only a pre-commit reviewer may do that later.</action>
  <verify>
    <automated>test -s .planning/phases/22-profile-provider-guard-implementation/22-DESIGN-BRIEF.md &amp;&amp; grep -Eq 'Threat|Approach|Must NOT block|Reviewer' .planning/phases/22-profile-provider-guard-implementation/22-DESIGN-BRIEF.md</automated>
  </verify>
  <done>Design review is recorded, reviewer objections are addressed or explicitly documented, and no tests/production code were edited before this review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED unit tests for the profile-provider guard and REST preservation</name>
  <files>tests/Unit/TwoFactorLifecycleBridgeTest.php, tests/bootstrap.php</files>
  <behavior>
    - TFA-GUARD-01: A captured `two_factor.profile_provider_lifecycle` admin rule callback returns false for `profile.php`/`user-edit.php` saves without `_nonce_user_two_factor_options`, without `_two_factor_enabled_providers`, or with malformed non-array enabled providers.
    - TFA-GUARD-02: The callback returns false for unchanged normalized enabled-provider sets, ignoring the dummy empty value, unknown/unavailable provider keys, and provider ordering; unchanged primary provider is not gated.
    - TFA-GUARD-03: The callback returns true when normalized enabled-provider keys are added or removed.
    - TFA-GUARD-04: The callback returns true when `_two_factor_provider` changes after normalization.
    - TFA-GUARD-05: The callback returns true for first enrollment/removal/replacement scenarios involving `Two_Factor_Totp` and `_two_factor_totp_key` state that can create or alter a future sudo factor.
    - REST preservation: Existing `two_factor.backup_codes_generate` and `two_factor.totp_manage` route/method assertions remain unchanged except for allowing the new third profile rule.
  </behavior>
  <action>Extend `tests/bootstrap.php` only as needed for the unit stub to model source-verified upstream provider normalization, e.g. `Two_Factor_Core::$mock_supported_provider_keys` and `Two_Factor_Core::get_supported_providers_for_user( $user_id )` returning a keyed provider map. Extend `tests/Unit/TwoFactorLifecycleBridgeTest.php` first, before production edits. Keep the existing separate-process bridge test pattern. Add helper methods to set/reset `$_POST`, `$GLOBALS['pagenow']`, `get_current_user_id`, `wp_verify_nonce`, and `get_user_meta` stubs so each predicate case is isolated. Run the targeted bridge test and confirm it fails for the expected RED reason: the new profile lifecycle rule/callback behavior does not exist yet. Do not edit `bridges/wp-sudo-two-factor-lifecycle-bridge.php` in this task.</action>
  <verify>
    <automated>bash -lc 'set +e; ./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/TwoFactorLifecycleBridgeTest.php --filter TwoFactorLifecycleBridgeTest 2&gt;&amp;1 | tee /tmp/wp-sudo-phase22-red.log; status=${PIPESTATUS[0]}; test "$status" -ne 0; grep -Eq "two_factor\\.profile_provider_lifecycle|profile provider|Failed asserting|Rule .* was not registered" /tmp/wp-sudo-phase22-red.log'</automated>
  </verify>
  <done>RED tests encode all five Phase 22 guard requirements plus REST preservation and fail only because production bridge behavior is not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the narrow profile-provider lifecycle guard until tests pass</name>
  <files>bridges/wp-sudo-two-factor-lifecycle-bridge.php, tests/Unit/TwoFactorLifecycleBridgeTest.php, tests/bootstrap.php</files>
  <behavior>
    - The optional bridge remains inert when `Two_Factor_Core` is absent.
    - Existing REST rules for backup codes and TOTP REST create/delete remain route/method compatible with Phase 21 evidence.
    - New admin rule id is stable and scoped, recommended: `two_factor.profile_provider_lifecycle`.
    - The admin rule uses `pagenow` `profile.php`/`user-edit.php`, `actions` `update`, `method` `POST`, and a callback predicate for the source-verified Two Factor lifecycle fields.
    - The callback gates only proven meaningful normalized lifecycle changes and fails open for absent/malformed upstream data where no meaningful change can be proven.
  </behavior>
  <action>Implement the bridge in `bridges/wp-sudo-two-factor-lifecycle-bridge.php` using the existing `wp_sudo_gated_actions` filter; do not add custom blocking logic, JavaScript, UI, core built-in rules, new dependencies, Patchstack code, or release/version metadata. Add a profile admin rule with a clear label such as `Change Two Factor profile settings`, category `users`, `ajax` null, `rest` null, and a POST replay `stash` allowlist containing the default replay fields plus `user_id`, `_nonce_user_two_factor_options`, `_two_factor_enabled_providers`, and `_two_factor_provider` (do not invent unverified TOTP secret POST field names). In the callback: verify the nonce field/action with `wp_verify_nonce`; resolve target user from `profile.php` via `get_current_user_id()` and from `user-edit.php` via posted `user_id`; require array-shaped `_two_factor_enabled_providers`; fetch supported providers through `Two_Factor_Core::get_supported_providers_for_user( $target_user_id )` when callable; normalize submitted/current enabled-provider sets by intersecting with supported keys, dropping empty values, sanitizing scalar strings, sorting, and comparing order-insensitively; normalize submitted/current primary provider so only enabled providers count; compare `_two_factor_totp_key` state only to the extent source-verified fields allow, and ensure `Two_Factor_Totp` first enrollment/removal/replacement cases in the tests return true. Update stale bridge comments that say classic profile provider changes are "not yet gated"; keep public docs and metrics deferred to Phase 23. Before any code commit, run a pre-commit reviewer agent per `CLAUDE.md` on the staged code/test changes and do not commit unless the reviewer writes `reviewer-approved`.</action>
  <verify>
    <automated>./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/TwoFactorLifecycleBridgeTest.php --filter TwoFactorLifecycleBridgeTest</automated>
  </verify>
  <done>Targeted bridge tests pass; profile lifecycle callback returns false for unrelated/no-op saves and true for provider, primary-provider, and TOTP-backed lifecycle changes; REST bridge assertions remain green; reviewer approval exists before code commit.</done>
</task>

</tasks>

<verification>
Run targeted bridge tests after each test/bridge edit. Confirm the RED test run fails before implementation and the same targeted command passes after implementation.
</verification>

<success_criteria>
- All five Phase 22 guard requirements are encoded in unit tests and pass.
- `bridges/wp-sudo-two-factor-lifecycle-bridge.php` is still the only production file changed.
- Existing REST bridge route/method behavior remains green.
- No Patchstack runtime support, browser UI, product release tag, version bump, or public documentation expansion is included.
</success_criteria>

<output>
After completion, create `.planning/phases/22-profile-provider-guard-implementation/22-01-SUMMARY.md`
</output>
