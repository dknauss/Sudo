---
phase: 22-profile-provider-guard-implementation
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified: []
autonomous: true
requirements:
  - TFA-GUARD-01
  - TFA-GUARD-02
  - TFA-GUARD-03
  - TFA-GUARD-04
  - TFA-GUARD-05
user_setup: []
must_haves:
  truths:
    - "The completed bridge passes the targeted Two Factor lifecycle unit test file."
    - "The repository unit suite, PHPStan analysis, and PHPCS lint are green after the guard implementation."
    - "Phase 22 did not add Patchstack runtime claims or product release/version changes."
    - "REST lifecycle bridge behavior for backup-code generation and TOTP create/delete remains green."
  artifacts:
    - path: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      provides: "Final optional Two Factor lifecycle bridge behavior"
      contains: "two_factor.profile_provider_lifecycle"
    - path: "tests/Unit/TwoFactorLifecycleBridgeTest.php"
      provides: "Final unit coverage for Phase 22 guard and REST preservation"
      contains: "TwoFactorLifecycleBridgeTest"
  key_links:
    - from: "composer test"
      to: "tests/Unit/TwoFactorLifecycleBridgeTest.php"
      via: "full unit suite includes targeted bridge tests"
      pattern: "TwoFactorLifecycleBridgeTest"
    - from: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      to: "Phase 21 source evidence"
      via: "comments and constants remain source-verified against c515462d51ac92941685e39293673c08538e16c8"
      pattern: "c515462d51ac92941685e39293673c08538e16c8"
---

<objective>
Validate the completed Phase 22 implementation and enforce scope boundaries.

Purpose: Prove the guard is green under the repo's required validation commands while preserving release posture and deferred documentation boundaries.
Output: Validation evidence in the plan summary; no additional code or documentation changes.
</objective>

<execution_context>
@/Users/danknauss/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-profile-provider-guard-implementation/22-CONTEXT.md
@.planning/phases/22-profile-provider-guard-implementation/22-VALIDATION.md
@.planning/phases/22-profile-provider-guard-implementation/22-01-SUMMARY.md
@.planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md
@AGENTS.md
@CLAUDE.md
@docs/current-metrics.md
@docs/release-status.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run required automated validation commands</name>
  <files></files>
  <action>Run validation as separate commands, not chained: targeted bridge PHPUnit, full unit suite, PHPStan, and PHPCS. If any command fails, fix only Phase 22 implementation/test defects in the files changed by Plan 01, then rerun the failed command and the targeted bridge test. Do not run full Playwright E2E unless the user asks; this phase changes PHP request-gating predicates and unit coverage is the planned verification seam.</action>
  <verify>
    <automated>./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/TwoFactorLifecycleBridgeTest.php --filter TwoFactorLifecycleBridgeTest</automated>
    <automated>composer test</automated>
    <automated>composer analyse</automated>
    <automated>composer lint</automated>
  </verify>
  <done>Targeted bridge tests, full unit tests, PHPStan, and PHPCS all pass after the profile-provider guard implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Verify scope boundaries and release posture</name>
  <files></files>
  <action>Confirm Phase 22 did not implement deferred ideas or release actions. Public docs and canonical metrics are Phase 23 unless the user explicitly re-scopes; record any expected `docs/current-metrics.md` drift in the summary for Phase 23 instead of updating it here. Confirm no Patchstack runtime support/claims were added, no product release/version bump was made, and no changes were made to `CHANGELOG.md`, `readme.txt`, `docs/two-factor-integration.md`, `docs/two-factor-ecosystem.md`, `docs/current-metrics.md`, or `docs/release-status.md` in this phase.</action>
  <verify>
    <automated>bash -lc 'git diff --name-only | grep -Ev "^(bridges/wp-sudo-two-factor-lifecycle-bridge\\.php|tests/Unit/TwoFactorLifecycleBridgeTest\\.php|tests/bootstrap\\.php|\\.planning/phases/22-profile-provider-guard-implementation/22-DESIGN-BRIEF\\.md|\\.planning/phases/22-profile-provider-guard-implementation/22-[0-9]{2}-SUMMARY\\.md)$" &amp;&amp; exit 1 || exit 0'</automated>
    <automated>bash -lc '! git diff -- bridges/wp-sudo-two-factor-lifecycle-bridge.php tests/Unit/TwoFactorLifecycleBridgeTest.php tests/bootstrap.php | grep -Ei "Patchstack|Stable tag|WP_SUDO_VERSION|Version:"'</automated>
  </verify>
  <done>Only Phase 22 implementation/test/planning-summary files changed; no Patchstack runtime claim, product version bump, release tag prep, or Phase 23 documentation work was added.</done>
</task>

</tasks>

<verification>
Standard validation must be green before `$gsd-verify-work`: targeted bridge PHPUnit, `composer test`, `composer analyse`, and `composer lint`. If implementation changes canonical counts, note that `docs/current-metrics.md` update is intentionally deferred to Phase 23 per `22-CONTEXT.md`.
</verification>

<success_criteria>
- All Phase 22 requirement tests pass.
- Existing REST bridge behavior remains covered and green.
- Scope boundaries from `22-CONTEXT.md` are preserved.
- Phase summary records exact validation commands and results.
</success_criteria>

<output>
After completion, create `.planning/phases/22-profile-provider-guard-implementation/22-02-SUMMARY.md`
</output>
