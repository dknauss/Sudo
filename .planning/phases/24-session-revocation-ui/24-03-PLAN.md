---
phase: 24-session-revocation-ui
plan: 03
type: tdd
wave: 3
depends_on: ["24-02"]
files_modified:
  - includes/class-admin.php
  - tests/Unit/AdminTest.php
autonomous: false
requirements: [REVK-04]
must_haves:
  truths:
    - "The Access-tab capability-holder table has no 'Revoke Session' column or button; capability administration and session revocation are visually and functionally separate."
    - "The now-orphaned wp_ajax_handle_revoke_session AJAX action registration is removed, leaving the single admin_post_ handler (plan 02) as the one UI revocation entry point."
    - "No remaining caller references the removed button markup, the AJAX action constant, or the localized revokeSessionAction — the full suite stays green."
  artifacts:
    - path: "includes/class-admin.php"
      provides: "Access-tab table without the Revoke Session button cell; AJAX_REVOKE_SESSION registration removed; consolidated single revocation entry point."
  key_links:
    - from: "Access-tab holder table render"
      to: "(removed) Revoke Session button"
      via: "absence — capability admin no longer conflated with session revocation"
      pattern: "wp-sudo-revoke-session"
---

<objective>
Complete the separation of session revocation from capability administration (REVK-04):
remove the Access-tab "Revoke Session" button, and now that the admin-post path from
plan 02 is the single revocation entry point, remove the orphaned
`wp_ajax_handle_revoke_session` AJAX action registration. Run the full gate.

Purpose: This wave is intentionally LAST — the AJAX action can only be removed safely
once the admin-post handler (plan 02) exists as the replacement entry point. Removing it
earlier would leave a revocation gap. After this, capability administration (holder table
+ grant form) and session revocation (Users list) are cleanly separate concerns.

Output: Access-tab table without the session-revoke button; the orphaned AJAX
registration removed; the consolidated single-entry-point shape; full suite + gate green.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-session-revocation-ui/24-CONTEXT.md
@.planning/phases/24-session-revocation-ui/24-RESEARCH.md
@.planning/phases/24-session-revocation-ui/24-VALIDATION.md
@.planning/phases/24-session-revocation-ui/24-01-SUMMARY.md
@.planning/phases/24-session-revocation-ui/24-02-SUMMARY.md

<interfaces>
<!-- Exact removal sites, extracted from the live codebase. -->

Access-tab "Revoke Session" button cell to REMOVE (includes/class-admin.php:1421-1424,
the entire <td> wrapping it at ~1420-1429):
```php
<td>
  <button type="button" class="button wp-sudo-revoke-session"
    data-user-id="<?php echo (int) $holder['user']->ID; ?>"
    data-nonce="<?php echo esc_attr( $nonce ); ?>"
  ><?php esc_html_e( 'Revoke Session', 'wp-sudo' ); ?></button>
</td>
```
Also remove the matching table header cell for that column if one exists, so the table
shape stays valid.

Orphaned AJAX action registration to REMOVE (includes/class-admin.php:309):
```php
add_action( 'wp_ajax_' . self::AJAX_REVOKE_SESSION, array( $this, 'handle_revoke_session' ), 10, 0 );
```
Related symbols to evaluate for removal once the action is gone (only if no longer
referenced anywhere): the `public const AJAX_REVOKE_SESSION` (class-admin.php:79), the
`handle_revoke_session()` method (now superseded by the admin-post handler + factored
core), and the localized `'revokeSessionAction' => self::AJAX_REVOKE_SESSION` entry
(class-admin.php:1019) plus its JS consumer in admin/js/wp-sudo-admin.js. Grep first;
remove only what is genuinely dead. Do NOT remove the factored revoke_session_core()
(plan 01) — it is the live path.

Existing AdminTest reference that will move/remove
(tests/Unit/AdminTest.php:3075 test_register_adds_revoke_session_ajax_hook) — update or
delete it to assert the action is NO LONGER registered, and update/relocate the existing
handle_revoke_session tests to target the consolidated admin-post path if the AJAX method
is removed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove Access-tab "Revoke Session" button (REVK-04)</name>
  <files>includes/class-admin.php, tests/Unit/AdminTest.php</files>
  <behavior>
    - Test: the rendered Access-tab holder-table markup contains NO 'wp-sudo-revoke-session'
      button (presence/absence assertion, or a grep-backed assertion that the class/label
      string is gone from the render path).
    - Test: the holder table still renders capability rows and the capability "Revoke"
      (wp-sudo-revoke-cap) control — capability administration is untouched; only the
      SESSION button is removed.
  </behavior>
  <action>
    1. Write the failing absence test first (watch fail — it currently passes because the
       button exists, so the test asserts its absence and fails until removal).
    2. Remove the `<td>...wp-sudo-revoke-session...</td>` cell (class-admin.php ~1420-1429)
       and its corresponding column header cell so the table shape stays valid. Leave the
       capability "Revoke" control and grant form intact.
    3. Update any AdminTest assertion that expected the session button to instead assert
       its absence.
    No production shims. Assert via the render path output captured in the test.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/AdminTest.php</automated>
  </verify>
  <done>
    Access-tab holder table has no Revoke Session button/column; capability controls
    intact. Test asserts absence and passes. ONE green commit after reviewer approval.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove orphaned wp_ajax_handle_revoke_session + dead symbols; consolidate entry point</name>
  <files>includes/class-admin.php, tests/Unit/AdminTest.php</files>
  <behavior>
    - Test: the AJAX action `wp_ajax_{AJAX_REVOKE_SESSION}` is NO LONGER registered (the
      existing test_register_adds_revoke_session_ajax_hook is inverted/removed).
    - Test: after removal, no remaining production reference to the AJAX action constant,
      the localized revokeSessionAction, or handle_revoke_session() exists (grep-backed
      assertion or a registration-snapshot assertion).
    - Test: the admin-post revocation path (plan 02) remains the single working entry point
      — its existing plan-02 tests stay green.
  </behavior>
  <action>
    1. Grep the whole repo for AJAX_REVOKE_SESSION, handle_revoke_session, revokeSessionAction,
       and wp-sudo-revoke-session to confirm what is now dead after plan 02 + task 1 above.
    2. Write/adjust the failing test first: invert test_register_adds_revoke_session_ajax_hook
       to assert the action is NOT added (watch fail), and add an assertion that no
       production reference to the orphaned symbols remains.
    3. Remove the `add_action( 'wp_ajax_' . self::AJAX_REVOKE_SESSION, ... )` registration
       (class-admin.php:309). Then remove genuinely-dead symbols ONLY if grep confirms no
       remaining caller: the handle_revoke_session() method, the AJAX_REVOKE_SESSION const,
       the localized 'revokeSessionAction' entry (class-admin.php:1019) and its JS consumer
       in admin/js/wp-sudo-admin.js. KEEP revoke_session_core() (the live factored path).
    4. Update/relocate the former handle_revoke_session unit tests so the cap/rate-limit/
       audit-hook behavior is still asserted via the consolidated admin-post path (those
       assertions must not be lost — they protect REVK-03).
    No production shims; remove the symbol, do not guard it. If admin/js/wp-sudo-admin.js
    loses its only session-revoke consumer, remove that JS branch too (do not leave dead JS).
  </action>
  <verify>
    <automated>./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/AdminTest.php</automated>
  </verify>
  <done>
    The orphaned AJAX action and its dead symbols are removed; the admin-post handler is the
    single revocation entry point; cap/rate/audit assertions are preserved on the live path.
    Tests green. ONE green commit after reviewer approval.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full gate + manual UI verification (REVK-01..05)</name>
  <what-built>
    The complete phase: shared revoke-all method + factored core (plan 01), Users-list row
    action + revoke-all interstitial + distinct messages (plan 02), and the Access-tab
    button + orphaned AJAX removal (this plan).
  </what-built>
  <how-to-verify>
    1. Run the full automated gate (must all be green BEFORE the final code commit):
       `composer test` ; `composer analyse` ; `composer lint`.
       Also run `composer verify:metrics` if any canonical count changed
       (surfaces/hooks/fields) and update docs/current-metrics.md if so.
    2. Manual (per 24-VALIDATION.md, on a Studio/wp-env multisite):
       - As an operator WITH revoke_wp_sudo_sessions and an active sudo session: the
         "Revoke sudo session" row action appears only for users with a live session and
         not on your own row; clicking it revokes immediately and returns to users.php with
         a dismissible success notice; the row drops from "Sudo Active".
       - As a cap-holder WITHOUT an active sudo session: the row action is still visible;
         clicking it shows the distinct "Start a sudo session before revoking others" notice.
       - As an operator WITHOUT the cap: the row action and revoke-all button are absent.
       - Revoke-all: visible only when active count > 0; opens an interstitial stating the
         exact count with Confirm/Cancel; Confirm revokes all current-site sessions except
         your own and returns with a success+count notice.
       - Each blocked path shows its distinct, dismissible, assistive-tech-announced notice.
       - Access tab: no "Revoke Session" button; capability controls unchanged.
    3. Confirm the phase does NOT bump WP_SUDO_VERSION / readme.txt Stable tag (v4.5 makes
       no product release — per STATE.md Key Decisions).
  </how-to-verify>
  <action>
    Run the full automated gate (composer test; composer analyse; composer lint) and the
    manual UI verification steps in <how-to-verify> on a Studio/wp-env multisite. Update
    docs/current-metrics.md only if a canonical count changed. Report results; on failure,
    capture issues for a gap-closure plan rather than committing.
  </action>
  <verify>
    <automated>composer test && composer analyse && composer lint</automated>
  </verify>
  <done>
    Full automated gate green; manual UI checks for REVK-01..05 pass; no version/tag bump.
  </done>
  <resume-signal>Type "verified" or describe issues to fold into a gap-closure plan.</resume-signal>
</task>

</tasks>

<verification>
- `./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/AdminTest.php` green.
- `composer test`, `composer analyse` (PHPStan L6), `composer lint` all green before the final commit.
- Pre-Commit Reviewer APPROVED before each code commit; reviewer writes the flag, not the executor.
- No production reference to the removed button/AJAX symbols remains (grep clean).
</verification>

<success_criteria>
- Access-tab holder table has no Revoke Session button/column; capability admin intact.
- Orphaned wp_ajax_handle_revoke_session removed; admin-post handler is the single entry point.
- cap/rate/audit assertions preserved on the live consolidated path.
- Full automated gate + manual UI verification green; reviewer-approved; ONE green commit per code task.
</success_criteria>

<output>
After completion, create `.planning/phases/24-session-revocation-ui/24-03-SUMMARY.md`.
</output>
