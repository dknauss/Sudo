---
phase: 19-2fa-bridge-planning-and-compatibility-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bridges/wp-sudo-two-factor-lifecycle-bridge.php
  - docs/two-factor-integration.md
  - tests/Unit/TwoFactorLifecycleBridgeTest.php
autonomous: true
requirements:
  - 2FA-01
  - 2FA-03
must_haves:
  truths:
    - "Maintainers can tell that WordPress/two-factor challenge-time validation is already built in, while factor lifecycle gating is a separate bridge concern."
    - "The existing Two Factor lifecycle REST bridge is documented as the current starting point for recovery-code generation and TOTP setup/delete, not as speculative future work."
    - "The plan for classic profile-provider changes is implementation-ready but does not ship an untested overbroad profile guard."
    - "Every edited third-party technical claim includes refreshed live-source evidence with URL, commit or revision, and checked date."
  artifacts:
    - path: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      provides: "Source-refreshed bridge comments for recovery-code and TOTP REST lifecycle gating scope"
      contains: "two_factor.backup_codes_generate"
    - path: "docs/two-factor-integration.md"
      provides: "Implementation-ready upstream Two Factor lifecycle bridge plan, including REST routes and profile-provider guard test matrix"
      contains: "Profile provider changes"
    - path: "tests/Unit/TwoFactorLifecycleBridgeTest.php"
      provides: "Unit coverage for current REST bridge rules, extended only if the source refresh changes route/method expectations or doc claims need a tighter test"
      contains: "two_factor.totp_manage"
  key_links:
    - from: "docs/two-factor-integration.md"
      to: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      via: "Bridge install/reference section"
      pattern: "wp-sudo-two-factor-lifecycle-bridge\\.php"
    - from: "tests/Unit/TwoFactorLifecycleBridgeTest.php"
      to: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      via: "requires bridge file and asserts registered REST rules"
      pattern: "generate-backup-codes|two-factor/1\\.0/totp"
---

<objective>
Make the upstream WordPress/two-factor lifecycle bridge implementation-ready without
pretending all factor lifecycle surfaces are already covered.

Purpose: Phase 19 must convert the existing REST lifecycle bridge and fresh upstream
source research into an executable next-step plan for recovery-code generation, TOTP
setup/delete, and classic profile provider changes.

Output: Source-refreshed bridge comments/docs, an explicit profile-provider guard plan
with TDD cases, and any necessary unit-test tightening for the existing REST bridge.
</objective>

<execution_context>
@/Users/danknauss/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-2fa-bridge-planning-and-compatibility-matrix/19-CONTEXT.md
@.planning/phases/19-2fa-bridge-planning-and-compatibility-matrix/19-RESEARCH.md
@.planning/phases/19-2fa-bridge-planning-and-compatibility-matrix/19-VALIDATION.md
@./CLAUDE.md
@./AGENTS.md
@bridges/wp-sudo-two-factor-lifecycle-bridge.php
@tests/Unit/TwoFactorLifecycleBridgeTest.php
@docs/two-factor-integration.md

<locked_decisions>
- Treat `bridges/wp-sudo-two-factor-lifecycle-bridge.php` as an existing upstream Two Factor lifecycle bridge starting point, not greenfield speculation.
- Keep upstream Two Factor profile-provider changes as a separately planned/tested guard problem.
- Do not ship an effect-level profile-provider guard in this plan.
- Do not claim Patchstack or other third-party support from source inspection alone.
- Re-check live third-party source before editing method names, meta keys, hooks, routes, or field names.
</locked_decisions>

<interfaces>
<!-- Key contracts verified from local source during planning; executor must refresh third-party source before editing claims. -->

Existing bridge:
```php
add_filter( 'wp_sudo_gated_actions', static function ( array $rules ): array { ... } );
$rules[] = array(
    'id' => 'two_factor.backup_codes_generate',
    'rest' => array(
        'route' => '#^/two-factor/1\.0/generate-backup-codes$#',
        'methods' => array( 'POST' ),
    ),
);
$rules[] = array(
    'id' => 'two_factor.totp_manage',
    'rest' => array(
        'route' => '#^/two-factor/1\.0/totp$#',
        'methods' => array( 'POST', 'DELETE' ),
    ),
);
```

Existing unit test expectations:
```php
TwoFactorLifecycleBridgeTest::test_registers_two_well_formed_rules_when_two_factor_present()
TwoFactorLifecycleBridgeTest::test_routes_and_methods_match_the_verified_two_factor_endpoints()
```

Source-refresh commands to run before editing third-party details:
```bash
TWOF_SHA=$(git ls-remote https://github.com/WordPress/two-factor.git refs/heads/master | awk '{print $1}')
echo "WordPress/two-factor master: $TWOF_SHA checked $(date -u +%F)"
curl -fsSL "https://raw.githubusercontent.com/WordPress/two-factor/$TWOF_SHA/providers/class-two-factor-backup-codes.php" | grep -nE "register_rest_route|generate-backup-codes|_two_factor_backup_codes"
curl -fsSL "https://raw.githubusercontent.com/WordPress/two-factor/$TWOF_SHA/providers/class-two-factor-totp.php" | grep -nE "register_rest_route|/totp|CREATABLE|DELETABLE|_two_factor_totp_key"
curl -fsSL "https://raw.githubusercontent.com/WordPress/two-factor/$TWOF_SHA/class-two-factor-core.php" | grep -nE "ENABLED_PROVIDERS_USER_META_KEY|PROVIDER_USER_META_KEY|personal_options_update|edit_user_profile_update|update_user_meta"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh upstream Two Factor source evidence and update REST bridge posture</name>
  <files>bridges/wp-sudo-two-factor-lifecycle-bridge.php, docs/two-factor-integration.md</files>
  <action>
    Run the source-refresh commands in the interfaces block first. Replace stale `38cd183`/April 2026 lifecycle wording in `docs/two-factor-integration.md` with the refreshed commit SHA and checked date. Update the lifecycle section from "Planned" to an honest current posture: challenge-time validation is built in; the REST lifecycle bridge exists for recovery-code generation and TOTP create/delete; profile-form provider changes remain a planned guard. Update bridge file comments only if the refreshed source confirms or changes the route/method/meta-key details. Do not change bridge behavior unless the refreshed source proves the current route/methods are stale; if behavior changes, add/update tests before implementation per TDD.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/TwoFactorLifecycleBridgeTest.php</automated>
    <automated>grep -n "wp-sudo-two-factor-lifecycle-bridge\.php\\|generate-backup-codes\\|two-factor/1.0/totp" docs/two-factor-integration.md</automated>
  </verify>
  <done>The integration docs and bridge comments distinguish built-in challenge validation from existing REST lifecycle bridge coverage, cite refreshed WordPress/two-factor source, and no stale `38cd183` lifecycle claim remains unless explicitly labeled historical.</done>
</task>

<task type="auto">
  <name>Task 2: Write implementation-ready profile-provider guard plan without shipping the guard</name>
  <files>docs/two-factor-integration.md</files>
  <action>
    Add a narrow "Profile provider changes" implementation plan under the Two Factor lifecycle section. It must specify the future guard problem in executable terms: profile.php/user-edit.php `action=update`, Two Factor POST fields/nonces to verify from refreshed source, existing user meta to compare (`_two_factor_enabled_providers`, `_two_factor_provider`, and TOTP key state if classic form writes it), and an idempotent predicate that gates only meaningful lifecycle changes. Include TDD cases before any future production guard: unrelated profile save is not gated; enabled-provider set changes are gated; primary provider changes are gated; removal/replacement of an enrolled TOTP factor is gated; first enrollment policy is explicit and tested before implementation. State that CLAUDE.md design review is required before any future security guard code.
  </action>
  <verify>
    <automated>grep -n "Profile provider changes\\|unrelated profile\\|primary provider\\|first enrollment\\|design review" docs/two-factor-integration.md</automated>
  </verify>
  <done>`docs/two-factor-integration.md` contains an implementation-ready, TDD-first profile-provider guard plan and explicitly does not ship or recommend an overbroad profile-save gate.</done>
</task>

<task type="auto">
  <name>Task 3: Validate upstream bridge readiness and preserve source-citation discipline</name>
  <files>tests/Unit/TwoFactorLifecycleBridgeTest.php, docs/two-factor-integration.md</files>
  <action>
    If Task 1 changed route/method behavior or tightened the docs claim beyond existing coverage, update `tests/Unit/TwoFactorLifecycleBridgeTest.php` first so the test fails against stale behavior and passes after any bridge change. If no behavior changed, leave the test file untouched and record in the summary that existing bridge tests were sufficient. Run the local gates below. Ensure the eventual task commit message includes the refreshed WordPress/two-factor source URL(s), commit SHA, and checked date because this plan edits third-party technical details.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/TwoFactorLifecycleBridgeTest.php</automated>
    <automated>composer test</automated>
    <automated>composer analyse</automated>
    <automated>composer lint</automated>
    <automated>composer verify:metrics</automated>
  </verify>
  <done>Unit tests and Composer validation pass; any third-party technical details added or changed are source-refreshed and ready to cite in the commit message.</done>
</task>

</tasks>

<verification>
- `./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/TwoFactorLifecycleBridgeTest.php`
- `composer test`
- `composer analyse`
- `composer lint`
- `composer verify:metrics`
- Manual source-citation review: edited WordPress/two-factor claims include URL, commit SHA, and checked date.
</verification>

<success_criteria>
- 2FA-01: Recovery-code generation, TOTP setup/delete, and profile-provider changes are all represented as implementation-ready lifecycle surfaces.
- 2FA-03: The docs distinguish built-in challenge validation, existing shippable REST bridge code, and future TDD/design-review-only profile guard work.
- No new Patchstack or profile-provider production guard code ships from this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/19-2fa-bridge-planning-and-compatibility-matrix/19-01-SUMMARY.md`. Include the refreshed WordPress/two-factor source commit/date and whether `tests/Unit/TwoFactorLifecycleBridgeTest.php` required changes.
</output>
