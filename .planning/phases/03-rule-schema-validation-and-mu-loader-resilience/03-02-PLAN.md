---
phase: 03-rule-schema-validation-and-mu-loader-resilience
plan: 02
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - mu-plugin/wp-sudo-loader.php
  - tests/Unit/PluginTest.php
  - tests/MANUAL-TESTING.md
autonomous: true

must_haves:
  truths:
    - "MU loader no longer relies solely on hardcoded basename/path assumptions"
    - "Loader remains inert when plugin is not active"
    - "Loader can resolve active plugin state in non-canonical directory layouts"
    - "Failure path is diagnosable for operators"
  artifacts:
    - path: "mu-plugin/wp-sudo-loader.php"
      provides: "Basename/path fallback resolution and safe loading behavior"
      contains: "wp_sudo_basename"
    - path: "tests/Unit/PluginTest.php"
      provides: "Unit coverage for MU-loader path/basename resilience behavior"
      contains: "mu loader"
  key_links:
    - from: "mu-plugin/wp-sudo-loader.php"
      to: "active_plugins / active_sitewide_plugins"
      via: "dynamic basename/path resolution"
      pattern: "wp-sudo.php"
---

<objective>
Harden MU loader startup against non-standard plugin directory layouts via TDD.

Purpose: ensure early hook registration remains reliable when plugin slug/path differs from canonical `wp-sudo/wp-sudo.php`.
Output: resilient MU loader resolution logic + test coverage + manual verification guidance.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/ROADMAP.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/03-rule-schema-validation-and-mu-loader-resilience/03-CONTEXT.md
@/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/03-rule-schema-validation-and-mu-loader-resilience/03-RESEARCH.md
@/Users/danknauss/Documents/GitHub/wp-sudo/mu-plugin/wp-sudo-loader.php
@/Users/danknauss/Documents/GitHub/wp-sudo/mu-plugin/wp-sudo-gate.php
@/Users/danknauss/Documents/GitHub/wp-sudo/includes/class-admin.php
@/Users/danknauss/Documents/GitHub/wp-sudo/tests/Unit/PluginTest.php
@/Users/danknauss/Documents/GitHub/wp-sudo/tests/MANUAL-TESTING.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1 RED: Add failing tests for basename/path fallback behavior</name>
  <files>tests/Unit/PluginTest.php</files>
  <action>
Add failing tests that encode expected loader resilience behavior.

Required test coverage:
1. Loader recognizes active plugin when basename comes from `WP_SUDO_PLUGIN_BASENAME`.
2. Loader recognizes active plugin when canonical `wp-sudo/wp-sudo.php` is absent but an equivalent basename ending in `/wp-sudo.php` exists.
3. Loader remains inert when no active plugin match is found.
4. Loader’s unresolved path behavior is explicit (diagnosable) rather than silent no-op.

Run focused tests (expect RED):
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:unit -- tests/Unit/PluginTest.php --filter "mu|loader|basename|active_plugins" --do-not-cache-result
composer analyse:phpstan
```

Commit: `test(03-02): add failing unit tests for mu-loader basename/path fallback`
  </action>
  <verify>
New MU-loader resilience tests fail before production updates.
  </verify>
  <done>RED baseline established for MU loader fallback handling.</done>
</task>

<task type="tdd">
  <name>Task 2 GREEN: Implement resilient MU loader resolution</name>
  <files>mu-plugin/wp-sudo-loader.php</files>
  <action>
Implement minimal loader changes to satisfy Task 1.

Implementation requirements:
1. Derive basename/path using fallback order (constant-derived, loader-derived, canonical fallback).
2. Preserve existing active-plugin checks (site + network) with improved basename matching.
3. Maintain inert behavior when plugin is inactive.
4. Add explicit diagnosable signal for unresolved plugin path state.
5. Preserve early `register_early()` behavior when load succeeds.

Run verification:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:unit -- tests/Unit/PluginTest.php --filter "mu|loader|basename|active_plugins" --do-not-cache-result
composer analyse:phpstan
```

Commit: `feat(03-02): harden mu-loader basename and path resolution`
  </action>
  <verify>
Focused MU-loader tests pass; no static-analysis regressions.
  </verify>
  <done>MU loader is resilient to non-canonical plugin directory layouts.</done>
</task>

<task type="tdd">
  <name>Task 3 REFACTOR: Document manual verification for non-canonical loader scenarios</name>
  <files>tests/MANUAL-TESTING.md</files>
  <action>
Add concise manual test coverage for MU loader resilience:
1. Canonical install path behavior.
2. Renamed plugin-directory behavior.
3. Inactive plugin + present shim behavior.
4. Failure signal verification path.

Run verification:
```bash
cd /Users/danknauss/Documents/GitHub/wp-sudo
composer test:unit -- tests/Unit/PluginTest.php --do-not-cache-result
composer analyse:phpstan
composer lint
```

Commit: `docs(03-02): add manual verification steps for mu-loader resilience`
  </action>
  <verify>
Manual guide includes MU-loader fallback checks and quality gates remain green.
  </verify>
  <done>MU-loader behavior is documented for repeatable operator verification.</done>
</task>

</tasks>

<verification>
- `composer test:unit -- tests/Unit/PluginTest.php --filter "mu|loader|basename|active_plugins" --do-not-cache-result`
- `composer analyse:phpstan`
- `composer lint`
</verification>

<success_criteria>
- MU loader works in non-canonical plugin layouts
- Inactive-plugin guard behavior remains correct
- Failure states are diagnosable
</success_criteria>

<output>
After completion, create `/Users/danknauss/Documents/GitHub/wp-sudo/.planning/phases/03-rule-schema-validation-and-mu-loader-resilience/03-02-SUMMARY.md`
</output>
