---
phase: 14-wordpress-org-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
  - docs/ROADMAP.md
  - docs/llm-lies-log.md
  - docs/wporg-submission-checklist.md
autonomous: true
requirements: [ORG-04, ORG-06, ORG-07]
must_haves:
  truths:
    - "SECURITY.md states the vulnerability reporting channel, the supported-versions policy, whether GitHub issues are acceptable for initial security contact, and the security-fix changelog-wording convention"
    - "docs/ROADMAP.md no longer claims request-stash pattern-based redaction is open/unbuilt; it reflects the shipped suffix-based redaction and cites class-request-stash.php"
    - "If the llm-lies-log convention covers doc-drift (RESEARCH judged this status-drift, not confabulation), the correction is optionally noted there — not required for completion"
    - "docs/wporg-submission-checklist.md exists and covers validation steps, asset specs, SVN trunk/tags layout, and the version-sync gate"
  artifacts:
    - path: "SECURITY.md"
      provides: "Complete security disclosure policy"
      contains: "Supported Versions"
    - path: "docs/ROADMAP.md"
      provides: "Corrected request-stash redaction status"
      contains: "SENSITIVE_KEY_SUFFIXES"
    - path: "docs/wporg-submission-checklist.md"
      provides: "In-repo WordPress.org submission checklist"
      contains: "Stable tag"
  key_links:
    - from: "docs/ROADMAP.md redaction status"
      to: "includes/class-request-stash.php"
      via: "verified citation"
      pattern: "SENSITIVE_KEY_SUFFIXES"
---

<objective>
Three independent documentation deliverables (no code): complete the security
disclosure policy (ORG-04), reconcile the stale request-stash redaction status
against the shipped code (ORG-06), and capture an in-repo WordPress.org
submission checklist (ORG-07).
</objective>

<reconciliation_note>
⚠️ Re-read each target on the live tree before editing — Codex has been committing docs (`ee4558a` added `docs/codex-comprehensive-review-2026-06-20.md` and touched `docs/current-metrics.md`). It may have already touched SECURITY.md or ROADMAP.md. Verify current state first; do not redo or revert Codex's edits without checking.
</reconciliation_note>

<context>
@.planning/phases/14-wordpress-org-readiness/14-CONTEXT.md
@.planning/phases/14-wordpress-org-readiness/14-RESEARCH.md
@.planning/phases/14-wordpress-org-readiness/14-VALIDATION.md
@./CLAUDE.md

<interfaces>
<!-- RESEARCH-verified 2026-06-19; RE-VERIFY live. -->
- SECURITY.md exists (~1 KB). Gaps per research: explicit GitHub-issues-for-security stance, supported-versions policy, security-fix changelog-wording convention.
- ORG-06: docs/ROADMAP.md (research cited lines ~42-44) claims redaction is "exact-key based and misses provider-specific names" — STALE. Live `includes/class-request-stash.php` ships `SENSITIVE_KEY_SUFFIXES` (~28 suffix patterns, `@since 2.11.0`, `is_sensitive_key()`). Corroborated by docs/archive/phase3-stash-minimization-spec.md and CHANGELOG. Residual: bare camelCase keys with no suffix separator are not matched — but new pattern-redaction is OUT OF SCOPE (REQUIREMENTS); this is doc reconciliation only.
- docs/llm-lies-log.md: append corrections per its existing entry format.
- ORG-07: no submission checklist exists in docs/ (research confirmed). docs/release-status.md and docs/documentation-remediation-checklist.md are precedents for structure/home.
- Canonical sources (CLAUDE.md — cite, don't guess): docs/release-status.md (release state/stable tag), docs/current-metrics.md (counts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete SECURITY.md (ORG-04)</name>
  <files>SECURITY.md</files>
  <action>
    Read the current SECURITY.md, then fill the gaps WITHOUT discarding existing content:
    - Vulnerability reporting channel (the preferred private channel; if GitHub private vulnerability reporting / security advisories is enabled, state it).
    - Whether public GitHub Issues are acceptable for initial security contact (state the policy explicitly either way).
    - A "Supported Versions" policy (which versions receive security fixes).
    - The security-fix changelog-wording convention (how security fixes are described in CHANGELOG/readme without leaking exploit detail).
    Keep it concise and accurate; do not invent a channel that does not exist — confirm the real reporting path before stating it.
  </action>
  <verify>
    <automated>grep -qi "Supported Versions" SECURITY.md && grep -qi "issue" SECURITY.md && grep -qi "report" SECURITY.md && echo "SECURITY.md covers reporting + supported versions + issues stance"</automated>
  </verify>
  <done>SECURITY.md states reporting channel, supported-versions policy, GitHub-issues stance, and the security-changelog convention; existing content preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile request-stash redaction status (ORG-06)</name>
  <files>docs/ROADMAP.md, docs/llm-lies-log.md</files>
  <action>
    1. Re-read `includes/class-request-stash.php` to confirm the shipped behavior (suffix-based via `SENSITIVE_KEY_SUFFIXES`, `is_sensitive_key()`, `@since 2.11.0`) and the true residual (bare keys with no suffix separator).
    2. Correct the stale docs/ROADMAP.md redaction claim to reflect shipped suffix-based redaction, citing `class-request-stash.php`/`SENSITIVE_KEY_SUFFIXES`. State the residual factually and note that new pattern-redaction is intentionally out of scope (REQUIREMENTS).
    3. OPTIONAL: RESEARCH §ORG-06 judged this status-drift (a stale roadmap marker), not a fabricated fact, and suggested no new llm-lies-log entry is needed. If you (or the log's convention) consider a stale "open" claim about already-shipped code worth recording, append a brief entry: what the doc claimed, what shipped, the verification source, the date. Otherwise the ROADMAP correction alone satisfies ORG-06.
    Per CLAUDE.md confabulation rules: cite the live source; do not restate from memory.
  </action>
  <verify>
    <automated>grep -q "SENSITIVE_KEY_SUFFIXES\|class-request-stash" docs/ROADMAP.md && echo "redaction status reconciled with code citation"</automated>
  </verify>
  <done>docs/ROADMAP.md reflects shipped suffix-based redaction with a code citation; llm-lies-log entry added only if the convention warrants it (optional per RESEARCH).</done>
</task>

<task type="auto">
  <name>Task 3: Create the WordPress.org submission checklist (ORG-07)</name>
  <files>docs/wporg-submission-checklist.md</files>
  <action>
    Create docs/wporg-submission-checklist.md capturing the submission process (documents it; does NOT submit). Include:
    - Pre-submission validation: readme validator pass, Plugin Check (PCP) green, version-sync gate (the CLAUDE.md 4-location + Stable tag checklist), `composer test`/`analyse`/`lint` green.
    - Asset requirements: banner (1544x500, 772x250), icon (128, 256), screenshots + captions matching readme `== Screenshots ==`.
    - SVN layout: trunk vs tags/, assets/ vs `.wordpress-org/`, what goes in the distributed zip vs repo-only.
    - Brand/header note: Plugin Name "Sudo Security", slug `wp-sudo` (link the divergence doc from Plan 14-02).
    - A final "do not submit until" gate referencing docs/release-status.md as the canonical release-state source.
    Link to it from docs/ (e.g. the docs index / release-status.md) so it is discoverable.
  </action>
  <verify>
    <automated>test -f docs/wporg-submission-checklist.md && grep -qi "Stable tag\|version" docs/wporg-submission-checklist.md && grep -qi "SVN\|trunk\|tags" docs/wporg-submission-checklist.md && echo "submission checklist created"</automated>
  </verify>
  <done>docs/wporg-submission-checklist.md exists covering validation steps, asset specs, SVN trunk/tags layout, and the version-sync gate; discoverable from docs/.</done>
</task>

</tasks>

<verification>
- SECURITY.md: reporting channel + supported-versions + GitHub-issues stance + security-changelog convention all present.
- docs/ROADMAP.md: redaction status corrected with a `class-request-stash.php` citation (docs/llm-lies-log.md entry optional per RESEARCH).
- docs/wporg-submission-checklist.md exists with validation steps, asset specs, SVN layout, version-sync gate.
- Markdown link/anchor check (the repo's CI markdown job) passes for new/edited docs.
</verification>

<success_criteria>
- ORG-04: SECURITY.md states reporting channel, supported-versions policy, GitHub-issues-for-security stance, and security-fix changelog convention.
- ORG-06: docs/ROADMAP.md redaction status matches shipped suffix-based redaction, verified against class-request-stash.php; optionally logged in llm-lies-log.md.
- ORG-07: an initial WordPress.org submission checklist exists in repo docs.
</success_criteria>

<output>
After completion, create `.planning/phases/14-wordpress-org-readiness/14-03-SUMMARY.md`. Note the exact redaction residual wording used and the verification source cited.
</output>
