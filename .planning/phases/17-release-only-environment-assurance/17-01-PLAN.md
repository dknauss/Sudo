---
phase: 17-release-only-environment-assurance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/release-environment-log.md
  - tests/MANUAL-TESTING.md
  - docs/wporg-submission-checklist.md
  - docs/release-status.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
must_haves:
  truths:
    - "Maintainers can find one durable per-package environment record for the current package."
    - "The v4.2.2 manual environment matrix is represented honestly as deferred where lanes were not rerun."
    - "Deferred release-grade lanes name an owner, timing, and blocker/waiver posture."
    - "WordPress.org readiness docs remain executable while clearly stating publication is delayed/on hold."
    - "Release docs distinguish pre-tag/core release gates from WordPress.org-only submission/upload gates."
  artifacts:
    - path: "docs/release-environment-log.md"
      provides: "Durable per-version manual environment evidence and deferral log"
      contains: "v4.2.2"
    - path: "tests/MANUAL-TESTING.md"
      provides: "Executable manual matrix procedure linked to the durable log"
      contains: "release-environment-log.md"
    - path: "docs/wporg-submission-checklist.md"
      provides: "Executable readiness checklist with pre-tag vs WordPress.org-only gate split"
      contains: "Pre-tag/core release gates"
    - path: "docs/release-status.md"
      provides: "Canonical release-state link to environment log and gate taxonomy"
      contains: "Release environment assurance"
  key_links:
    - from: "tests/MANUAL-TESTING.md"
      to: "docs/release-environment-log.md"
      via: "relative Markdown link near Release Environment Matrix Checklist"
      pattern: "release-environment-log\.md"
    - from: "docs/wporg-submission-checklist.md"
      to: "docs/release-status.md"
      via: "canonical release-state reference"
      pattern: "release-status\.md"
    - from: "docs/release-status.md"
      to: "docs/release-environment-log.md"
      via: "canonical sources or release assurance section"
      pattern: "release-environment-log\.md"
---

<objective>
Create the release-only environment assurance documentation trail for Phase 17.

Purpose: Make the current package's manual environment matrix executable and recordable without implying that WordPress.org publication, upload, managed-host provisioning, or a new tag is happening now.
Output: A durable `docs/release-environment-log.md`, linked manual testing guidance, split pre-tag versus WordPress.org-only release gates, and updated planning state when the docs are complete.
</objective>

<execution_context>
@/Users/danknauss/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-release-only-environment-assurance/17-CONTEXT.md
@.planning/phases/17-release-only-environment-assurance/17-RESEARCH.md
@.planning/phases/17-release-only-environment-assurance/17-VALIDATION.md
@docs/wporg-submission-checklist.md
@tests/MANUAL-TESTING.md
@docs/release-status.md

<locked_decisions>
- Add `docs/release-environment-log.md` as the dedicated release environment log.
- Keep executable smoke instructions in `tests/MANUAL-TESTING.md`; do not duplicate the full manual procedure in the log.
- Record the current `v4.2.2` package honestly as deferred where the matrix is not being rerun now.
- Deferrals require owner, timing, and whether the deferral blocks the next public tag/publication decision unless explicitly waived.
- Split release-readiness gates into pre-tag/core release gates and WordPress.org-only submission/upload gates.
- Preserve the WordPress.org publication posture as intentionally delayed/on hold.
</locked_decisions>

<out_of_scope>
- Do not submit or upload anything to WordPress.org.
- Do not create or upload WordPress.org SVN assets.
- Do not cut a release tag.
- Do not provision or require a managed-host environment.
- Do not require browser automation, screenshots, Playwright, full E2E, or manual smoke execution in this phase.
</out_of_scope>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create durable release environment log and link manual matrix</name>
  <files>docs/release-environment-log.md, tests/MANUAL-TESTING.md</files>
  <action>Create `docs/release-environment-log.md` as the record of record for release-grade manual environment matrix outcomes. Include: purpose/non-submission disclaimer; status-by-package table; current `v4.2.2` row dated 2026-06-29 with overall status `Deferred`; detail table rows for Apache stack, managed WordPress host, and minimum supported WordPress version with owner `Maintainer`, timing `Before next public tag/publication decision`, and blocker status `Yes unless explicitly waived`; required evidence fields for future completed lanes; deferral/failure policy requiring owner/timing/follow-up or waiver. Then update `tests/MANUAL-TESTING.md` near `Release Environment Matrix Checklist` to point runners to `../docs/release-environment-log.md` for durable per-version outcomes and clarify this manual guide remains the executable procedure, not the record of record. Do not copy full smoke steps into the log.</action>
  <verify>
    <automated>test -f docs/release-environment-log.md</automated>
    <automated>grep -n "v4\.2\.2\|Deferred\|Owner\|Timing\|Before next public tag/publication decision" docs/release-environment-log.md</automated>
    <automated>grep -n "release-environment-log\.md\|record of record\|executable procedure" tests/MANUAL-TESTING.md</automated>
  </verify>
  <done>`docs/release-environment-log.md` exists, records `v4.2.2` deferrals without claiming lanes were rerun, and `tests/MANUAL-TESTING.md` links procedure to durable evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Split release gates in WordPress.org and release-status docs</name>
  <files>docs/wporg-submission-checklist.md, docs/release-status.md</files>
  <action>Update `docs/wporg-submission-checklist.md` so its operational checks are grouped into clear `Pre-tag/core release gates` and `WordPress.org-only submission/upload gates` sections. Pre-tag/core gates must include version sync, `composer test`, `composer analyse`, `composer lint`, `composer verify:metrics`, `composer verify:i18n`, pre-release external-claim audit, changelog/readme/release-status sanity, package metadata sanity, and the release environment matrix/log gate. WordPress.org-only gates must include readme validator, Plugin Check against a clean production package, SVN layout/upload, listing assets, screenshot/caption parity, slug-lock decision, and final publication approval. Keep existing executable details; reorganize rather than delete. Preserve repeated language that publication is delayed/on hold and that this document describes process only. Update `docs/release-status.md` to link `docs/release-environment-log.md`, describe the current `v4.2.2` release-environment deferral posture, and summarize the pre-tag/core versus WordPress.org-only gate split without changing current version facts.</action>
  <verify>
    <automated>grep -n "Pre-tag/core release gates\|WordPress.org-only submission/upload gates\|delayed/on hold" docs/wporg-submission-checklist.md</automated>
    <automated>grep -n "release-environment-log\.md\|Pre-tag/core\|WordPress.org-only\|v4\.2\.2" docs/release-status.md</automated>
  </verify>
  <done>Checklist remains executable and on-hold, and canonical release status links the environment log while distinguishing tag gates from WordPress.org-only gates.</done>
</task>

<task type="auto">
  <name>Task 3: Run docs validation and finalize Phase 17 planning state</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>After Tasks 1-2 are complete, run the docs-only validation commands. If the documentation changes do not alter canonical metrics, run `composer verify:metrics` anyway because `docs/release-status.md` is canonical release documentation and record the result in the summary. Read through the four affected docs to confirm they tell one consistent story: procedure in `tests/MANUAL-TESTING.md`, evidence/deferrals in `docs/release-environment-log.md`, executable WordPress.org checklist in `docs/wporg-submission-checklist.md`, and canonical release posture in `docs/release-status.md`. Then update `.planning/REQUIREMENTS.md` to mark REL-01, REL-02, and REL-03 complete; update `.planning/ROADMAP.md` Phase 17 status/plans to show one plan completed by this implementation; update `.planning/STATE.md` last activity/current position so Phase 17 is complete and Phase 18 is the next planned phase. Do not mark WordPress.org submission complete or started.</action>
  <verify>
    <automated>git diff --check</automated>
    <automated>composer verify:metrics</automated>
    <automated>grep -n "\[x\] \*\*REL-01\|\[x\] \*\*REL-02\|\[x\] \*\*REL-03" .planning/REQUIREMENTS.md</automated>
    <automated>grep -n "Phase 17.*Complete\|17-01-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <done>Patch hygiene and metrics verification pass, the four docs are semantically consistent, and planning files show Phase 17 complete without implying publication/submission.</done>
</task>

</tasks>

<verification>
Run the plan-level checks after all tasks:

```bash
git diff --check
composer verify:metrics
grep -n "v4\.2\.2\|Deferred\|Owner\|Timing" docs/release-environment-log.md
grep -n "release-environment-log\.md" tests/MANUAL-TESTING.md docs/release-status.md
grep -n "Pre-tag/core release gates\|WordPress.org-only submission/upload gates\|delayed/on hold" docs/wporg-submission-checklist.md
```

Manual read-through requirement: confirm no edited doc says a managed-host run, WordPress.org upload/submission, publication approval, or new tag happened in Phase 17.
</verification>

<success_criteria>
- `docs/release-environment-log.md` is the durable per-version evidence/deferral log and includes an honest `v4.2.2` deferral record.
- `tests/MANUAL-TESTING.md` remains the executable manual matrix and links to the durable log.
- `docs/wporg-submission-checklist.md` separates pre-tag/core gates from WordPress.org-only gates while remaining ready to execute later.
- `docs/release-status.md` links the environment log and preserves current version/publication facts.
- REL-01, REL-02, and REL-03 are marked complete in planning only after validation passes.
</success_criteria>

<output>
After completion, create `.planning/phases/17-release-only-environment-assurance/17-01-SUMMARY.md` with files changed, verification results, and the explicit note that WordPress.org submission remains delayed/on hold.
</output>
