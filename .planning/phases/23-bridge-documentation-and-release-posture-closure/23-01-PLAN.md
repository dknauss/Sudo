---
phase: 23-bridge-documentation-and-release-posture-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/two-factor-integration.md
  - docs/two-factor-ecosystem.md
  - docs/current-metrics.md
  - CHANGELOG.md
  - docs/release-status.md
autonomous: true
requirements:
  - TFA-DOC-01
  - TFA-DOC-02
  - TFA-DOC-03
must_haves:
  truths:
    - "Maintainer can tell apart built-in challenge-time Two Factor validation, optional REST lifecycle bridge gating, and optional classic profile-provider lifecycle gating."
    - "Patchstack Security remains documented only as fixture-blocked/manual-test work, with no runtime compatibility or shipped bridge claim."
    - "Canonical metrics match live repository counts before any other document references changed counts."
    - "Release-facing notes describe unreleased main behavior without implying a product version bump, stable-tag update, WordPress.org publication, or product git tag."
    - "Targeted Two Factor bridge tests and standard repository checks pass for the documentation/metrics closure."
  artifacts:
    - path: "docs/two-factor-integration.md"
      provides: "Maintainer-facing explanation of challenge-time validation, REST lifecycle bridge coverage, and classic profile-provider lifecycle guard boundaries"
      contains: "profile-provider lifecycle"
    - path: "docs/two-factor-ecosystem.md"
      provides: "Compatibility matrix updated from candidate language to shipped optional WordPress/two-factor bridge behavior while preserving Patchstack fixture-blocked status"
      contains: "fixture-blocked"
    - path: "docs/current-metrics.md"
      provides: "Canonical refreshed test, assertion, size, and architectural counts"
      contains: "Last verified:"
    - path: "CHANGELOG.md"
      provides: "Concise Unreleased note for release-facing Two Factor lifecycle bridge behavior"
      contains: "Two Factor"
    - path: "docs/release-status.md"
      provides: "Canonical current-main release posture showing unreleased Two Factor bridge work without product release metadata changes"
      contains: "Latest tagged release"
  key_links:
    - from: "docs/two-factor-integration.md"
      to: ".planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md"
      via: "source-evidence citation with upstream WordPress/two-factor commit c515462d51ac92941685e39293673c08538e16c8 checked 2026-06-30"
      pattern: "c515462d51ac92941685e39293673c08538e16c8"
    - from: "docs/two-factor-integration.md"
      to: "bridges/wp-sudo-two-factor-lifecycle-bridge.php"
      via: "bridge behavior description names the optional bridge file and the REST/profile lifecycle surfaces it gates"
      pattern: "wp-sudo-two-factor-lifecycle-bridge\\.php"
    - from: "docs/two-factor-ecosystem.md"
      to: "Patchstack Security"
      via: "compatibility matrix and ecosystem notes retain fixture-blocked/manual runtime testing wording"
      pattern: "Patchstack.*fixture|fixture.*Patchstack"
    - from: "docs/current-metrics.md"
      to: "composer verify:metrics"
      via: "metrics file updated before release/readiness docs and verified by repository script"
      pattern: "composer verify:metrics"
    - from: "docs/release-status.md"
      to: "CHANGELOG.md"
      via: "both describe unreleased main behavior without changing WP_SUDO_VERSION, plugin Version, readme.txt Stable tag, or product git tag"
      pattern: "Unreleased|unreleased"
---

<objective>
Update Phase 23 documentation, metrics, and release posture after the Phase 22 optional Two Factor profile-provider lifecycle guard.

Purpose: close the v4.4.0 Two Factor Lifecycle Bridge milestone with accurate maintainer/release docs and canonical metrics, while preserving explicit unsupported boundaries.
Output: refreshed Two Factor integration/ecosystem documentation, canonical metrics, and concise unreleased/release-status notes with validation evidence.
</objective>

<execution_context>
@/Users/danknauss/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-bridge-documentation-and-release-posture-closure/23-CONTEXT.md
@.planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md
@.planning/phases/22-profile-provider-guard-implementation/22-01-SUMMARY.md
@.planning/phases/22-profile-provider-guard-implementation/22-02-SUMMARY.md
@.planning/phases/22-profile-provider-guard-implementation/22-VERIFICATION.md
@bridges/wp-sudo-two-factor-lifecycle-bridge.php
@tests/Unit/TwoFactorLifecycleBridgeTest.php
@docs/two-factor-integration.md
@docs/two-factor-ecosystem.md
@docs/current-metrics.md
@CHANGELOG.md
@docs/release-status.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Two Factor bridge documentation and ecosystem posture</name>
  <files>docs/two-factor-integration.md, docs/two-factor-ecosystem.md</files>
  <action>
    Update `docs/two-factor-integration.md` so the Two Factor lifecycle section clearly distinguishes three surfaces:
    1. built-in challenge-time Two Factor validation through `Two_Factor_Core` provider rendering/preprocessing/code validation during WP Sudo challenges;
    2. optional bridge REST lifecycle gating for `POST /two-factor/1.0/generate-backup-codes` and `POST`/`DELETE /two-factor/1.0/totp`;
    3. optional bridge classic profile-provider lifecycle gating for meaningful `profile.php` / `user-edit.php` provider-set, primary-provider, and TOTP-backed lifecycle changes.

    Replace stale candidate/future wording with Phase 22 reality, but keep the guard boundary narrow and idempotent: unrelated profile saves, malformed upstream no-op submissions, normalized no-op provider resubmissions, and unverified classic-form TOTP secret replacement must not be described as gated.

    Cite the Phase 21 source posture for third-party technical claims: WordPress/two-factor master commit `c515462d51ac92941685e39293673c08538e16c8`, checked 2026-06-30, with links to durable upstream files or a direct reference to `.planning/phases/21-two-factor-source-refresh-and-guard-design/21-SOURCE-EVIDENCE.md`. Preserve the known limits from `23-CONTEXT.md`: REST `sudo_required` in-place recovery remains future work; direct WP-CLI/PHP recovery-code generation remains governed by non-interactive policy.

    Update `docs/two-factor-ecosystem.md` so the compatibility matrix no longer treats WordPress/two-factor profile-provider changes as a bridge candidate. It should show the optional bridge as shipped for both verified REST lifecycle routes and the classic profile-provider lifecycle guard. Keep Patchstack Security as fixture-blocked/manual runtime testing only pending a paid Patchstack-enabled fixture; do not add Patchstack bridge code, runtime support claims, or compatibility language that implies tested runtime behavior.
  </action>
  <verify>
    <automated>grep -nE "challenge-time|REST lifecycle|profile-provider lifecycle|c515462d51ac92941685e39293673c08538e16c8" docs/two-factor-integration.md</automated>
    <automated>grep -nE "profile-provider|bridge exists|fixture-blocked|Patchstack" docs/two-factor-ecosystem.md</automated>
    <automated>! grep -nE "profile provider changes.*candidate|profile-provider.*candidate|Patchstack.*runtime compatibility|Patchstack.*supported" docs/two-factor-ecosystem.md docs/two-factor-integration.md</automated>
  </verify>
  <done>`docs/two-factor-integration.md` explains the three Two Factor surfaces accurately, `docs/two-factor-ecosystem.md` promotes only the verified WordPress/two-factor bridge work, and Patchstack remains explicitly fixture-blocked/manual-test-only.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh canonical metrics and release-facing current-main notes</name>
  <files>docs/current-metrics.md, CHANGELOG.md, docs/release-status.md</files>
  <action>
    Refresh `docs/current-metrics.md` before touching release-facing prose. Regenerate live counts with the verification commands already documented in that file and the repository instructions, including:
    - `composer test:unit` / `composer test` for unit test and assertion counts;
    - `rg -c "function test" tests/Integration/*.php | awk -F: '{sum+=$2} END{print sum}'` for integration test methods;
    - `ls tests/Unit/*.php | wc -l` and `ls tests/Integration/*.php | wc -l` for test files;
    - production/test/total PHP line-count commands from `docs/current-metrics.md`;
    - architectural fact commands in `docs/current-metrics.md` if relevant.

    Update `Last verified` to the actual execution date and adjust verification notes to reflect the fresh commands. Then run `composer verify:metrics` and fix any drift it reports.

    Add concise release-facing notes only where warranted by actual Phase 22 behavior:
    - In `CHANGELOG.md` Unreleased, add one bullet for the optional Two Factor profile-provider lifecycle guard. Do not create a new product release heading.
    - In `docs/release-status.md`, update `Last verified` and current `main` / Unreleased `main` work to mention the Two Factor profile-provider lifecycle bridge and refreshed metrics. Keep latest tagged release `4.2.2`, runtime version constant `4.2.2`, WordPress.org publication delayed/on-hold, and readme stable tag `4.2.2`.

    Explicitly do not bump `WP_SUDO_VERSION`, plugin header `Version:`, `tests/bootstrap.php` version constant, `phpstan-bootstrap.php` version constant, `readme.txt` Stable tag, or create a product git tag.
  </action>
  <verify>
    <automated>composer verify:metrics</automated>
    <automated>grep -nE "Two Factor|profile-provider|lifecycle bridge" CHANGELOG.md docs/release-status.md</automated>
    <automated>grep -nE "Latest tagged release: `4\\.2\\.2`|Runtime version constant: `4\\.2\\.2`|Stable tag `4\\.2\\.2`" docs/release-status.md</automated>
    <automated>git diff -- wp-sudo.php tests/bootstrap.php phpstan-bootstrap.php readme.txt | grep -E "WP_SUDO_VERSION|Version:|Stable tag" &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>`docs/current-metrics.md` matches live counts and passes `composer verify:metrics`; Unreleased/release-status notes mention the new bridge behavior without any version, tag, stable-tag, or publication action.</done>
</task>

<task type="auto">
  <name>Task 3: Run final validation and scope-boundary checks</name>
  <files>.planning/phases/23-bridge-documentation-and-release-posture-closure/23-01-SUMMARY.md</files>
  <action>
    Run the Phase 23 validation gate as separate commands and record exact outcomes in the summary:
    - targeted bridge PHPUnit: `./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/TwoFactorLifecycleBridgeTest.php --filter TwoFactorLifecycleBridgeTest`;
    - `composer test`;
    - `composer analyse`;
    - `composer lint`;
    - `composer verify:metrics`;
    - `git diff --check`.

    Add a scope-boundary check before summarizing:
    - confirm no product version/stable-tag metadata changed in `wp-sudo.php`, `tests/bootstrap.php`, `phpstan-bootstrap.php`, or `readme.txt`;
    - confirm docs still describe Patchstack as fixture-blocked/manual-test-only and do not claim runtime support;
    - confirm no browser/admin UI behavior changed. No Playwright/E2E run is required unless an unexpected UI/screenshot-visible change was made.

    Create the required summary file using the template. This is docs/metrics-only work, so reviewer approval is not required unless unexpected code changes were introduced.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Unit/TwoFactorLifecycleBridgeTest.php --filter TwoFactorLifecycleBridgeTest</automated>
    <automated>composer test</automated>
    <automated>composer analyse</automated>
    <automated>composer lint</automated>
    <automated>composer verify:metrics</automated>
    <automated>git diff --check</automated>
    <automated>test -f .planning/phases/23-bridge-documentation-and-release-posture-closure/23-01-SUMMARY.md</automated>
  </verify>
  <done>All targeted and standard validation commands pass, scope boundaries are recorded, and `23-01-SUMMARY.md` contains exact command outcomes plus confirmation that Patchstack/runtime/version boundaries remain unchanged.</done>
</task>

</tasks>

<verification>
Run the full plan verification commands from Task 3. Additionally inspect the final diff to ensure this phase only changes documentation/metrics/planning files and does not modify PHP production/test code, package release metadata, or browser UI assets.
</verification>

<success_criteria>
- `docs/two-factor-integration.md` accurately separates challenge-time provider delegation, REST lifecycle bridge coverage, and classic profile-provider lifecycle gating.
- `docs/two-factor-ecosystem.md` and release/readiness posture keep Patchstack Security fixture-blocked/manual-test-only unless a paid fixture has actually been tested.
- `docs/current-metrics.md` is regenerated from live commands and passes `composer verify:metrics`.
- `CHANGELOG.md` and `docs/release-status.md` describe unreleased current-main bridge behavior without implying a new tag, version bump, stable-tag update, or WordPress.org publication.
- Targeted bridge PHPUnit, unit suite, static analysis, PHPCS lint, metrics verification, and whitespace diff checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/23-bridge-documentation-and-release-posture-closure/23-01-SUMMARY.md`
</output>
