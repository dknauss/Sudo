---
phase: 12-breaking-changes-and-floor-bump
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - CHANGELOG.md
  - readme.txt
  - docs/developer-reference.md
autonomous: true
requirements: [BRK-07]
must_haves:
  truths:
    - "CHANGELOG.md has a 4.0.0 'Breaking changes' block naming all three breaks (sudo_can removal, compatibility-mode removal, WP 6.4 / PHP 8.2 floors)"
    - "readme.txt has a '= 4.0.0 =' entry in the Upgrade Notice section summarizing the three breaks with action items"
    - "docs/developer-reference.md has a durable 'Migrating to 4.0' section covering the removed APIs and raised minimums"
    - "Migration notes accurately describe the code that plans 12-01 and 12-02 actually shipped (no confabulation)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "4.0.0 breaking-changes block"
      contains: "Breaking"
    - path: "readme.txt"
      provides: "= 4.0.0 = Upgrade Notice entry"
      contains: "4.0.0"
    - path: "docs/developer-reference.md"
      provides: "Migrating to 4.0 section"
      contains: "Migrating to 4.0"
  key_links:
    - from: "docs/developer-reference.md Migrating to 4.0"
      to: "wp_sudo_can() / WP_SUDO_RECOVERY_MODE"
      via: "sudo_can() -> wp_sudo_can() guidance; compatibility -> strict guidance"
      pattern: "wp_sudo_can"
---

<objective>
Write the integrator migration notes for the v4.0.0 breaking changes in three durable, discoverable places — and nowhere else (no new standalone file).

Purpose: BRK-07 — integrators who used `sudo_can()`, relied on `compatibility` governance mode, or run below WP 6.4 / PHP 8.2 must be able to discover what changed and what to do, both at upgrade time (CHANGELOG, readme Upgrade Notice) and durably (developer reference).

Output: A CHANGELOG breaking-changes block, a readme Upgrade Notice entry, and a "Migrating to 4.0" section in the developer reference. This plan is DOCS-ONLY — it touches only `.md` and `readme.txt`, so per CLAUDE.md the pre-commit reviewer agent is NOT required (the hook auto-detects docs-only and skips the approval check).

DEPENDS ON 12-01 and 12-02: the migration notes describe exactly what those plans removed/changed. Write the prose AFTER reading their SUMMARYs so the notes match shipped code (anti-confabulation per CLAUDE.md).

ASSUMPTION (state in SUMMARY): the notes use `4.0.0` as the documented breaking-change version (the migration target), but this plan does NOT bump `WP_SUDO_VERSION` or `Stable tag` (still 3.4.0). The actual version/tag bump happens at release/tagging.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-breaking-changes-and-floor-bump/12-RESEARCH.md
@.planning/phases/12-breaking-changes-and-floor-bump/12-CONTEXT.md
@CLAUDE.md

# Read the prior-plan SUMMARYs to describe shipped code accurately (anti-confabulation):
@.planning/phases/12-breaking-changes-and-floor-bump/12-01-SUMMARY.md
@.planning/phases/12-breaking-changes-and-floor-bump/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CHANGELOG breaking-changes block + readme Upgrade Notice (BRK-07 part 1)</name>
  <files>CHANGELOG.md, readme.txt</files>
  <action>
First read 12-01-SUMMARY.md and 12-02-SUMMARY.md so the wording matches what actually shipped.
  - CHANGELOG.md: in the Unreleased / 4.0.0 section, add a bold "Breaking changes" subsection (bold heading, NOT a new `##` level — match the file's existing structure). Cover, as separate bullets:
      * `sudo_can()` removed — use `wp_sudo_can()` (search-replace; the function no longer exists).
      * `compatibility` governance mode removed — governance is now always strict; `WP_SUDO_RECOVERY_MODE` is the only break-glass. A stale `wp_sudo_governance_mode = 'compatibility'` is treated as strict and triggers a persistent admin notice until the option is cleared (cleanup ships in a later release).
      * Minimum WordPress raised to 6.4.
      * Minimum PHP raised to 8.2.
  - readme.txt: add a `= 4.0.0 =` entry under `== Upgrade Notice ==` — a short, high-level operator-facing summary of the three breaks with action items (replace sudo_can calls; expect strict governance + the migration notice if you used compatibility mode; ensure your host runs WP 6.4 / PHP 8.2). Do NOT change `Stable tag` (stays 3.4.0).
Match the existing CHANGELOG/readme formatting conventions in the files. Verify counts/claims against the SUMMARYs, not training data.
This is a DOCS-ONLY change (.md + readme.txt) — no reviewer agent needed; the pre-commit hook skips the approval check for docs-only commits.
Commit: `docs(12-03): add 4.0.0 breaking-changes CHANGELOG block and readme Upgrade Notice`
  </action>
  <verify>
    <automated>grep -iq "breaking" CHANGELOG.md && echo CHANGELOG_OK</automated>
    <automated>grep -q "= 4.0.0 =" readme.txt && echo UPGRADE_NOTICE_OK</automated>
  </verify>
  <done>
- CHANGELOG.md has a 4.0.0 breaking-changes block naming sudo_can removal, compatibility-mode removal, and the WP 6.4 / PHP 8.2 floors.
- readme.txt `== Upgrade Notice ==` has a `= 4.0.0 =` entry with action items; `Stable tag` unchanged.
- Wording reflects shipped code per the SUMMARYs.
  </done>
</task>

<task type="auto">
  <name>Task 2: "Migrating to 4.0" section in developer reference (BRK-07 part 2)</name>
  <files>docs/developer-reference.md</files>
  <action>
Add a durable "Migrating to 4.0" section to docs/developer-reference.md (where the removed hook/function contracts already live). Cover:
  - `sudo_can()` -> `wp_sudo_can()`: identical signature `(string $cap, ?int $user_id = null): bool`; the alias was removed, so any remaining `sudo_can()` call is a fatal undefined-function error. Provide the search-replace guidance.
  - `wp_sudo_governance_mode = 'compatibility'`: the mode was removed; the option value is now treated as strict (`user_can($user_id, $cap)`), and a persistent admin notice + `_doing_it_wrong('wp_sudo_governance_mode', ..., '4.0.0')` warns managers until the option is normalized/removed (that cleanup ships in a later release). `WP_SUDO_RECOVERY_MODE` is the sole break-glass.
  - New minimums: WordPress 6.4 and PHP 8.2; note what 6.4 unlocks (e.g. `wp_get_admin_notice()` is unconditionally available, so no compat shim is needed).
  - Note that the prior `sudo_can()` deprecation docblock promised removal in 4.0.0 — that promise is now delivered.
Match the file's existing heading style. Confirm every contract detail against 12-01-SUMMARY.md / live code — do not invent signatures (anti-confabulation per CLAUDE.md).
DOCS-ONLY — no reviewer agent needed.
Commit: `docs(12-03): add Migrating to 4.0 section to developer reference`
  </action>
  <verify>
    <automated>grep -q "Migrating to 4.0" docs/developer-reference.md && echo MIGRATION_SECTION_OK</automated>
    <automated>grep -q "wp_sudo_can" docs/developer-reference.md && echo API_REF_OK</automated>
  </verify>
  <done>
- docs/developer-reference.md has a "Migrating to 4.0" section covering the `sudo_can()` removal, compatibility-mode removal (now strict + notice), and the WP 6.4 / PHP 8.2 minimums.
- The `wp_sudo_can()` signature and behavior described match the shipped code per the SUMMARY.
  </done>
</task>

</tasks>

<verification>
- `grep -i "breaking" CHANGELOG.md` finds the 4.0.0 block.
- `grep "= 4.0.0 =" readme.txt` finds the Upgrade Notice entry.
- `grep "Migrating to 4.0" docs/developer-reference.md` finds the durable section.
- All three deliverables describe what plans 12-01/12-02 actually shipped (verified against their SUMMARYs).
- No new standalone migration file created.
</verification>

<success_criteria>
- BRK-07: removed APIs (`sudo_can()`, `compatibility` mode) and raised WP 6.4 / PHP 8.2 minimums are documented in CHANGELOG (breaking block), readme.txt (Upgrade Notice), and developer-reference.md (Migrating to 4.0) — and nowhere else.
- Stable tag / WP_SUDO_VERSION untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/12-breaking-changes-and-floor-bump/12-03-SUMMARY.md`.
Record the three deliverables written, confirmation they match shipped code per the prior SUMMARYs, and the assumption that the version/tag was not bumped.
</output>
