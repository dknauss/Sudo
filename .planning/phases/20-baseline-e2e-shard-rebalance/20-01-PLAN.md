---
phase: 20-baseline-e2e-shard-rebalance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/e2e.yml
  - docs/e2e-runtime-review.md
autonomous: true
requirements:
  - E2E-01
  - E2E-02
  - E2E-03
  - E2E-04
  - E2E-05
  - E2E-06
  - E2E-07
must_haves:
  truths:
    - "Maintainer can see a fresh GitHub Actions runtime snapshot collected before the workflow edit."
    - "The rebalance decision names the current long pole and shortest suitable destination group from refreshed Actions data."
    - "The required baseline workflow still has exactly four E2E matrix groups and the final required `E2E Tests` gate."
    - "A small low-risk slice leaves `E2E Tests 1/4 (challenge-basic-admin)` without deleting, skipping, or manualizing any spec coverage."
    - "GitHub CI passes after the workflow change, including all four baseline E2E groups and the final gate."
    - "`docs/e2e-runtime-review.md` records the implementation, validation run, and keep/revert decision."
  artifacts:
    - path: ".github/workflows/e2e.yml"
      provides: "Four-group required baseline E2E matrix with rebalanced test slice"
      contains: "group-total: 4"
    - path: "docs/e2e-runtime-review.md"
      provides: "Authoritative runtime evidence, implementation note, and CI validation result"
      contains: "Phase 20"
  key_links:
    - from: "docs/e2e-runtime-review.md"
      to: ".github/workflows/e2e.yml"
      via: "documented refreshed runtime snapshot drives selected destination group"
      pattern: "E2E Tests 1/4.*long pole|destination group"
    - from: ".github/workflows/e2e.yml"
      to: "GitHub required checks"
      via: "matrix group names and final `E2E Tests` gate remain unchanged in semantics"
      pattern: "name: \"E2E Tests \\$\\{\\{ matrix.group-index \\}\\}/\\$\\{\\{ matrix.group-total \\}\\}"
    - from: "GitHub Actions CI run"
      to: "docs/e2e-runtime-review.md"
      via: "run URL and group durations are recorded after CI completes"
      pattern: "https://github.com/dknauss/Sudo/actions/runs/"
---

<objective>
Refresh current GitHub Actions E2E group runtime evidence, rebalance one small low-risk test slice out of `E2E Tests 1/4 (challenge-basic-admin)` within the existing four-group `.github/workflows/e2e.yml` matrix, and document/verify the result.

Purpose: Reduce the required E2E long pole without adding another `wp-env` startup floor, weakening required checks, or losing coverage.
Output: Updated `.github/workflows/e2e.yml` and `docs/e2e-runtime-review.md` with pre-edit evidence, implementation details, and post-change GitHub CI validation.
</objective>

<execution_context>
@/Users/danknauss/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-baseline-e2e-shard-rebalance/20-CONTEXT.md
@.planning/phases/20-baseline-e2e-shard-rebalance/20-VALIDATION.md
@docs/e2e-runtime-review.md
@.github/workflows/e2e.yml
@AGENTS.md
@CLAUDE.md

<interfaces>
Current baseline matrix contract in `.github/workflows/e2e.yml`:
```yaml
e2e-group:
  name: "E2E Tests ${{ matrix.group-index }}/${{ matrix.group-total }} (${{ matrix.group-name }})"
  strategy:
    matrix:
      include:
        - group-index: 1
          group-total: 4
          group-name: challenge-basic-admin
        - group-index: 2
          group-total: 4
          group-name: challenge-2fa-ui
        - group-index: 3
          group-total: 4
          group-name: challenge-lockout-surfaces
        - group-index: 4
          group-total: 4
          group-name: challenge-replay-multisite

e2e-tests:
  name: "E2E Tests"
  needs: [changes, e2e-group]
```

Current group-1 command shape:
```yaml
npm run test:e2e -- tests/e2e/specs/admin-bar-deactivate.spec.ts tests/e2e/specs/admin-bar-timer.spec.ts tests/e2e/specs/challenge.spec.ts --grep 'ABAR|TIMR|CHAL-0[1-5]'
npm run test:e2e -- tests/e2e/specs/challenge.spec.ts --grep 'CHAL-09|CHAL-10|CHAL-11'
```
</interfaces>

Locked decisions and boundaries:
- Use GitHub Actions job timings as source of truth; do not use local Playwright timing assumptions.
- Keep exactly four baseline E2E groups and the same final required `E2E Tests` gate.
- Do not remove specs, skip surfaces, or move coverage to manual-only validation.
- Do not tune scheduled/manual smoke workflows in this phase.
- This is not a plugin version bump, product release tag, or WordPress.org submission step.
- Branch hygiene: if currently on `main`, create/use `ci/e2e-shard-rebalance`; do not use agent-name prefixes such as `codex/`, `claude/`, or `copilot/`.
- Prefer a no-new-code workflow/doc change. If pre-commit treats workflow YAML as non-doc, follow the reviewer workflow; never write `reviewer-approved` yourself.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh Actions runtime evidence before editing</name>
  <files>docs/e2e-runtime-review.md</files>
  <action>
    If the current branch is `main`, create and switch to `ci/e2e-shard-rebalance` before making file changes. Before touching `.github/workflows/e2e.yml`, refresh the required baseline `E2E Tests` runtime evidence from GitHub Actions. Use `gh run list` and `gh run view` against `dknauss/Sudo` for `.github/workflows/e2e.yml`; include only successful comparable `E2E Tests 1/4` through `4/4` jobs from successful runs, and exclude `Detect code changes` plus the exact final gate job named `E2E Tests`. Record a new `## Phase 20 refresh` section in `docs/e2e-runtime-review.md` with:
    - collection timestamp and query commands used;
    - exact run IDs and URLs reviewed;
    - per-group durations for all four groups, not only an aggregate;
    - identified current long pole from those refreshed durations;
    - selected destination group, choosing the shortest suitable existing group from refreshed data;
    - explicit note that local Playwright timing assumptions were not used.

    For auditability, record the exact `gh run view {run_id} -R dknauss/Sudo --json jobs` command used for each reviewed run, or record one command plus the exact run IDs it was run against. Do not rely on old Phase 18 evidence except as historical context after the new Phase 20 refresh section.

    Do not dispatch or tune scheduled/manual smoke workflows (`e2e-nginx-multisite.yml`, `e2e-sqlite.yml`, `e2e-visual.yml`) in this phase.
  </action>
  <verify>
    <automated>gh run list -R dknauss/Sudo --workflow e2e.yml --limit 12 --json databaseId,displayTitle,event,headBranch,status,conclusion,createdAt,updatedAt,url</automated>
    <automated>PHASE20_REFRESH_RUN_ID=$(python3 -c "import re, pathlib; text=pathlib.Path('docs/e2e-runtime-review.md').read_text(); sec=text.split('## Phase 20 refresh',1)[1].split('\\n## ',1)[0]; ids=re.findall(r'(?:run ID|run id|databaseId|actions/runs/)[^0-9]*(\\d{6,})', sec); assert ids, 'Phase 20 refresh must include at least one exact run ID/URL'; print(ids[0])"); gh run view "$PHASE20_REFRESH_RUN_ID" -R dknauss/Sudo --json jobs</automated>
    <automated>python3 -c "import re, pathlib; text=pathlib.Path('docs/e2e-runtime-review.md').read_text(); assert '## Phase 20 refresh' in text, 'missing Phase 20 refresh heading'; sec=text.split('## Phase 20 refresh',1)[1].split('\\n## ',1)[0]; checks={'collection timestamp': r'(Collected|Collection timestamp|Timestamp).*20\\d\\d-', 'run IDs/URLs': r'(run ID|run id|databaseId|actions/runs/)', 'group 1 duration': r'E2E Tests 1/4[\\s\\S]{0,160}(duration|seconds|minutes|s\\b|min\\b)', 'group 2 duration': r'E2E Tests 2/4[\\s\\S]{0,160}(duration|seconds|minutes|s\\b|min\\b)', 'group 3 duration': r'E2E Tests 3/4[\\s\\S]{0,160}(duration|seconds|minutes|s\\b|min\\b)', 'group 4 duration': r'E2E Tests 4/4[\\s\\S]{0,160}(duration|seconds|minutes|s\\b|min\\b)', 'current long pole': r'(current )?long pole', 'destination group': r'destination group', 'local timing disclaimer': r'local Playwright timing assumptions were not used'}; missing=[name for name,pat in checks.items() if not re.search(pat, sec, re.I)]; assert not missing, 'Phase 20 refresh missing: '+', '.join(missing)"</automated>
  </verify>
  <done>`docs/e2e-runtime-review.md` contains fresh pre-edit Actions evidence and a data-backed destination-group decision for the workflow edit.</done>
</task>

<task type="auto">
  <name>Task 2: Move one low-risk slice within the four-group matrix</name>
  <files>.github/workflows/e2e.yml, docs/e2e-runtime-review.md</files>
  <action>
    Edit only the required baseline matrix in `.github/workflows/e2e.yml`. Keep four `group-index` entries, keep every `group-total: 4`, keep the `e2e-tests` final gate named exactly `"E2E Tests"`, and keep the existing required-check semantics.

    Default candidate slice: move `tests/e2e/specs/admin-bar-timer.spec.ts` (the `TIMR` slice) out of group 1 into the shortest suitable destination group identified in Task 1. If Task 1 evidence shows group 2 remains shortest, add the timer spec to group 2; otherwise add it to the refreshed shortest suitable group. Preserve coverage by removing both `TIMR` and `tests/e2e/specs/admin-bar-timer.spec.ts` from group 1 only when the timer spec and `TIMR` grep are present in the documented destination group. Do not delete, skip, or manualize any test spec. If the refreshed evidence makes a different group-1 slice clearly safer/smaller, document that reason in `docs/e2e-runtime-review.md` before editing and update the static verification command to assert that alternate moved slice leaves group 1 and appears in the documented destination group.

    Update the nearby workflow comment or `docs/e2e-runtime-review.md` to explain that the rebalance stays within four groups because each group pays the fixed `wp-env` startup floor. In `docs/e2e-runtime-review.md`, record the moved slice and destination as a parseable line like `Destination group: E2E Tests N/4 (group-name)` so verification can prove the slice moved to the documented group. Do not edit product version metadata, release docs, WordPress.org submission docs, or scheduled/manual smoke workflows.
  </action>
  <verify>
    <automated>git diff --check</automated>
    <automated>python3 -c "import re, pathlib; s=pathlib.Path('.github/workflows/e2e.yml').read_text(); docs=pathlib.Path('docs/e2e-runtime-review.md').read_text(); starts=list(re.finditer(r'^\\s*- group-index:\\s*(\\d+)\\s*$', s, re.M)); assert len(starts)==4, f'expected exactly four matrix groups, found {len(starts)}'; bounds=[(int(m.group(1)), m.start(), starts[i+1].start() if i+1 < len(starts) else s.find('\\n    steps:', m.end())) for i,m in enumerate(starts)]; groups={idx:s[start:end] for idx,start,end in bounds}; assert set(groups)=={1,2,3,4}, f'unexpected group indexes: {sorted(groups)}'; assert all('group-total: 4' in block for block in groups.values()), 'every group must retain group-total: 4'; assert 'TIMR' not in groups[1] and 'admin-bar-timer.spec.ts' not in groups[1], 'group 1 must no longer include TIMR/admin-bar-timer'; dm=re.search(r'Destination group:\\s*E2E Tests\\s+(\\d)/4', docs, re.I); assert dm, 'docs must include parseable Destination group: E2E Tests N/4'; dest=int(dm.group(1)); assert dest in {2,3,4}, f'destination must be another group, got {dest}'; assert 'TIMR' in groups[dest] and 'admin-bar-timer.spec.ts' in groups[dest], 'documented destination group must contain TIMR/admin-bar-timer'; assert 'name: \"E2E Tests\"' in s, 'final gate name changed'; assert re.search(r'e2e-tests:[\\s\\S]*?name:\\s*\"E2E Tests\"[\\s\\S]*?needs:\\s*\\[changes, e2e-group\\]', s), 'final gate must remain name \"E2E Tests\" with needs [changes, e2e-group]'; diff='\\n'.join(line[1:] for line in __import__('subprocess').check_output(['git','diff','--','.github/workflows/e2e.yml'], text=True).splitlines() if line.startswith('+') and not line.startswith('+++')); forbidden=['test.skip','--grep-invert','manual-only','skip']; assert not any(term in diff for term in forbidden), 'workflow diff must not add skip/manual-only behavior'"</automated>
    <automated>git diff -- .github/workflows/e2e.yml docs/e2e-runtime-review.md</automated>
  </verify>
  <done>The workflow still has exactly four baseline groups and the final gate, with one documented low-risk slice moved out of group 1 and no coverage loss.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the rebalance with required GitHub CI and document the result</name>
  <files>docs/e2e-runtime-review.md</files>
  <action>
    Commit the workflow/docs change with a conventional commit message after local static checks pass. If the pre-commit hook requires reviewer approval because `.github/workflows/e2e.yml` is non-doc, run the normal reviewer workflow and never create `reviewer-approved` yourself. Push `ci/e2e-shard-rebalance` (or the current implementation branch) and create a PR if one does not already exist so required GitHub CI runs against the changed workflow. Use `gh` to watch or inspect the relevant run/checks until `E2E Tests 1/4`, `E2E Tests 2/4`, `E2E Tests 3/4`, `E2E Tests 4/4`, and the final `E2E Tests` gate have completed. If GitHub authentication is missing, stop and request the normal auth checkpoint; otherwise do not substitute local Playwright runs for the required CI evidence.

    Capture the exact implementation branch, PR number/URL, and GitHub Actions run ID for the validation run. Inspect that exact run with `gh run view {run_id} -R dknauss/Sudo --json databaseId,workflowName,displayTitle,event,headBranch,headSha,conclusion,url,jobs` so the evidence cannot drift to a later unrelated run. After CI completes, append/update a `## Phase 20 validation` section in `docs/e2e-runtime-review.md` with the implementation branch/PR, exact run ID and run URL, all four group conclusions, all four observed group durations, final gate result, and a keep/revert judgment. Keep the change only if CI is green and the observed long-pole result is directionally consistent enough to justify the rebalance; if CI fails or timing gets materially worse, document the failure and revert the workflow edit while preserving the evidence note.
  </action>
  <verify>
    <automated>gh pr checks --watch --required</automated>
    <automated>PHASE20_VALIDATION_RUN_ID=$(python3 -c "import re, pathlib; text=pathlib.Path('docs/e2e-runtime-review.md').read_text(); sec=text.split('## Phase 20 validation',1)[1].split('\\n## ',1)[0]; ids=re.findall(r'(?:run ID|run id|databaseId|actions/runs/)[^0-9]*(\\d{6,})', sec); assert ids, 'Phase 20 validation must include exact run ID/URL'; print(ids[0])"); gh run view "$PHASE20_VALIDATION_RUN_ID" -R dknauss/Sudo --json databaseId,workflowName,displayTitle,event,headBranch,headSha,conclusion,url,jobs</automated>
    <automated>python3 -c "import re, pathlib; text=pathlib.Path('docs/e2e-runtime-review.md').read_text(); assert '## Phase 20 validation' in text, 'missing Phase 20 validation heading'; sec=text.split('## Phase 20 validation',1)[1].split('\\n## ',1)[0]; group=lambda n: rf'E2E Tests {n}/4(?=[\\s\\S]{{0,260}}(success|failure|cancelled|skipped|conclusion))(?=[\\s\\S]{{0,260}}(duration|seconds|minutes|s\\b|min\\b))'; checks={'implementation branch': r'(implementation branch|branch):', 'PR': r'(PR|pull request).*https://github.com/dknauss/Sudo/pull/|\\bPR\\s*#?\\d+', 'run ID': r'(run ID|run id|databaseId|actions/runs/)\\D*\\d{6,}', 'run URL': r'https://github.com/dknauss/Sudo/actions/runs/\\d+', 'group 1 conclusion/duration': group(1), 'group 2 conclusion/duration': group(2), 'group 3 conclusion/duration': group(3), 'group 4 conclusion/duration': group(4), 'final gate result': r'final (E2E Tests )?gate (result|conclusion)|E2E Tests[^\\n]*(final gate)', 'keep/revert decision': r'\\b(keep|revert)\\b.*\\bdecision\\b|\\bdecision\\b.*\\b(keep|revert)\\b'}; missing=[name for name,pat in checks.items() if not re.search(pat, sec, re.I)]; assert not missing, 'Phase 20 validation missing: '+', '.join(missing)"</automated>
  </verify>
  <done>Required GitHub CI is green for all four baseline E2E groups and the final gate, and the runtime review records the validation evidence plus keep/revert decision.</done>
</task>

</tasks>

<verification>
Overall phase checks:
- `git diff --check` passes.
- `.github/workflows/e2e.yml` contains exactly four baseline matrix groups and every group still has `group-total: 4`.
- The exact final required gate job remains named `"E2E Tests"` and still depends on `changes` plus `e2e-group`.
- No E2E spec is deleted, skipped, or moved to manual-only validation.
- `docs/e2e-runtime-review.md` includes pre-edit refresh evidence, implementation rationale, CI run URL, group durations/conclusions, and keep/revert decision.
- Required GitHub CI passes for `E2E Tests 1/4` through `4/4` and the final `E2E Tests` gate.
</verification>

<success_criteria>
- Fresh Actions evidence identifies the current long pole and shortest suitable destination group before the workflow edit.
- One small low-risk group-1 slice is redistributed within the existing four-group matrix.
- Required-check semantics and coverage are preserved.
- The implementation is validated by GitHub CI, not local timing assumptions.
- Documentation explains what changed, why it stayed within four groups, and whether observed CI timing supports keeping the rebalance.
</success_criteria>

<output>
After completion, create `.planning/phases/20-baseline-e2e-shard-rebalance/20-01-SUMMARY.md`.
</output>
