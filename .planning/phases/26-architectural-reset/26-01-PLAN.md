# Phase 26 Plan 01 — Establish the new planning authority

## Objective

Make the action-gate program the only current GSD direction and prevent older
documents from silently defining implementation work.

## Tasks

### 1. Establish program files

- [x] Replace stale `.planning/PROJECT.md`.
- [x] Replace stale `.planning/REQUIREMENTS.md`.
- [x] Replace stale `.planning/ROADMAP.md`.
- [x] Replace stale `.planning/STATE.md`.
- [x] Add the architecture charter.
- [x] Record the initial artifact disposition.

### 2. Reconcile active documentation

- [ ] Add the architecture charter/program to `docs/README.md`.
- [ ] Update `docs/ROADMAP.md` so the public direction and GSD phase order agree.
- [ ] Add interim supersession banners to the broad proposal/spec.
- [ ] Move working proposal notes to the archive and update inbound links.
- [ ] Remove active claims that the registry is settled or required.
- [ ] Remove active claims that a request stash/carry is part of the target.
- [ ] State the Cut-1 extensibility limitation: third-party actions cannot
  register with a private two-effect core catalog.

### 3. Reconcile demonstrators

- [ ] Write a preservation/successor notice for `consequential-actions`
  describing what remains evidentially useful, what is superseded, and which
  immutable tag retains the original MVP.
- [ ] Identify the exact `poc/install-package-gate` tests/findings Phase 28 will
  reuse.
- [ ] Keep Slice A runnable without converting its historical proof model in
  place; plan the action-bound successor beside it.
- [ ] Identify the exact in-editor interaction patterns Phase 30 may reuse without
  inheriting its trust assumptions.
- [ ] Mark `wip/coregate-unit1` as a pinned evidence branch, not a merge target.

### 4. Reconcile GitHub work

- [ ] Map core-gate issues to Phases 27–32 or deferred work.
- [ ] Separate research-program blockers from old plugin backlog priorities.
- [ ] Close or relabel issues whose architecture was retired.
- [ ] Create no implementation issue until its parent phase exit gate is met.
- [ ] Track preflight-oracle, atomic-redemption, and invisible-fallback
  observability requirements in Phase 27/29/30 rather than as post-hoc hardening.

## Verification

- `rg` finds no active document that calls registry work a prerequisite.
- `rg` finds no active target architecture that calls for automatic replay.
- Every active detailed design points to the charter or carries a superseded banner.
- `docs/README.md`, `docs/ROADMAP.md`, `.planning/PROJECT.md`, and
  `.planning/STATE.md` agree on the current program and Phase 26.
- Documentation link validation and source/metrics checks remain green.

## Not in this plan

- Production PHP or JavaScript changes.
- A core patch.
- Proof protocol code.
- A release, tag, or GitHub milestone mutation.
