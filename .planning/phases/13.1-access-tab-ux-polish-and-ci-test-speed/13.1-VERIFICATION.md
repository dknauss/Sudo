---
phase: 13.1-access-tab-ux-polish-and-ci-test-speed
verified: 2026-06-28T06:04:43Z
status: gaps_found
score: 8/10 plan must-haves verified; 5/6 roadmap success criteria fully verified
gaps:
  - truth: "The Grant Capability form provides a searchable administrator-scoped user picker, not merely a numeric ID replacement"
    status: partial
    reason: "The numeric input is gone and real administrator users populate a select, but no explicit searchable picker/autocomplete/listbox behavior is implemented or tested."
    artifacts:
      - path: "includes/class-admin.php"
        issue: "Renders a plain `<select id=\"wp-sudo-grant-user\">`; no searchable UI enhancement is present."
      - path: "admin/js/wp-sudo-admin.js"
        issue: "Reads the select value, but provides no search/filter behavior."
      - path: "tests/Unit/AdminTest.php"
        issue: "Asserts select markup but does not assert searchable-picker behavior."
    missing:
      - "Implement or document an accepted native-select typeahead interpretation; otherwise add explicit searchable picker/autocomplete behavior and tests."
  - truth: "docs/current-metrics.md is current after the phase changes"
    status: failed
    reason: "`composer verify:metrics` reports drift in assertions and PHP line counts."
    artifacts:
      - path: "docs/current-metrics.md"
        issue: "Canonical metrics are stale: unit assertions docs=2606 actual=2616; production PHP lines docs=15953 actual=15992; tests PHP lines docs=32444 actual=32471; combined and total PHP counts also drift."
    missing:
      - "Update docs/current-metrics.md from live counts and re-run `composer verify:metrics`."
---

# Phase 13.1: Access-tab UX polish and CI test-speed Verification Report

**Phase Goal:** The Settings → Sudo Access tab grant UI is operator-friendly — a searchable admin-scoped user picker replaces the raw numeric user-ID field, and capability choices read in plain English with the slug demoted to secondary text — without weakening server-side authorization. Separately, CI wall-clock and runner-minutes drop by trimming integration-matrix fan-out, caching the WordPress test library, and rebalancing E2E shards, with coverage tradeoffs documented.
**Verified:** 2026-06-28T06:04:43Z  
**Status:** gaps_found  
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Operator selects the grant target from real administrator users; no numeric ID input remains | ✓ VERIFIED | `includes/class-admin.php:1375-1385` calls `get_users()` with `role => administrator`; `includes/class-admin.php:1442-1448` renders `<select id="wp-sudo-grant-user">` with user IDs; unit test asserts no `type="number" id="wp-sudo-grant-user"` at `tests/Unit/AdminTest.php:3031-3035`. |
| 2 | User picker is searchable as required by ROADMAP/ACC-01 | ✗ FAILED | Grep found no Select2/Chosen/autocomplete/listbox/datalist/search code for the grant user picker; the implementation is a plain `<select>`. |
| 3 | Capability options read in plain English with raw slug retained as secondary text | ✓ VERIFIED | `CAP_LABELS` map exists at `includes/class-admin.php:103-108`; capability options keep `value="<?php echo esc_attr( $cap ); ?>"` and render label plus slug at `includes/class-admin.php:1455-1461`; unit test asserts label and exact slug option values at `tests/Unit/AdminTest.php:3036-3040`. |
| 4 | Empty/placeholder user selection is rejected before AJAX | ✓ VERIFIED | Placeholder option value is `0` at `includes/class-admin.php:1443`; JS parses the selected value and returns before `sendAccessAction()` when `! userId || userId < 1` at `admin/js/wp-sudo-admin.js:358-371`. |
| 5 | Server-side authorization remains unchanged | ✓ VERIFIED | `handle_grant_cap()` still calls `check_ajax_referer( 'wp_sudo_access', '_nonce' )`, checks `wp_sudo_can( 'manage_wp_sudo' )`, casts `$_POST['user_id']` to int, sanitizes cap, and validates against `GOVERNANCE_CAPS` at `includes/class-admin.php:1559-1572`. Local targeted PHPUnit passed: 5 tests / 16 assertions. |
| 6 | #88 E2E grant-flow spec uses the new markup and ACCESS-01/02 remain intact | ✓ VERIFIED | `tests/e2e/specs/access-grant.spec.ts:95,115` uses `page.selectOption('#wp-sudo-grant-user', String(targetId))`; target user is created as `--role=administrator` at lines 56-63. GitHub Actions E2E run `27863473541` completed successfully on 2026-06-20 with all four groups green. |
| 7 | a11y labels and announcements preserved | ✓ VERIFIED | `<label for="wp-sudo-grant-user">` and `<label for="wp-sudo-grant-cap">` remain at `includes/class-admin.php:1439,1452`; live result span remains `role="status" aria-live="polite"` at line 1470; `wp.a11y.speak()` guard remains at `admin/js/wp-sudo-admin.js:254-255`. |
| 8 | Integration matrix is 7 documented representative lanes | ✓ VERIFIED | `.github/workflows/phpunit.yml:162-211` includes 7 integration lanes: five single-site MySQL lanes, one multisite PHP 8.3/WP 7.0 lane, and one MariaDB LTS lane. Dropped multisite lanes are documented in comments at lines 164-174. |
| 9 | WordPress test library cache is wired and config regenerates fresh | ✓ VERIFIED | `.github/workflows/phpunit.yml:259-265` uses `actions/cache` for `/tmp/wordpress-tests-lib/includes`, `/tmp/wordpress-tests-lib/data`, and `/tmp/wordpress`; `.github/workflows/phpunit.yml:272` removes `wp-tests-config.php` before running the installer. `bin/install-wp-tests.sh` skips existing core/tests directories and regenerates config when missing. |
| 10 | access-grant spec is assigned to an E2E group and heavy 2FA group is rebalanced | ✓ VERIFIED | `.github/workflows/e2e.yml:83-92` keeps 2FA in group 2 and places `access-grant.spec.ts` in group 3. Workflow comments note the fixed wp-env startup floor at lines 73-74. GitHub Actions E2E run `27863473541` succeeded. |
| 11 | docs/current-metrics.md remains canonical/current | ✗ FAILED | `composer verify:metrics` failed: docs have 2606 assertions vs actual 2616, production PHP lines 15953 vs 15992, tests PHP lines 32444 vs 32471, and aggregate PHP counts drifted. |

**Score:** 8/10 plan must-haves verified; 5/6 roadmap success criteria fully verified.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `includes/class-admin.php` | Admin-scoped grant user picker + capability label map; no auth change | ⚠️ PARTIAL | Substantive and wired for dropdown/labels/auth preservation, but the roadmap/ACC-01 searchable-picker requirement is not explicitly implemented. |
| `admin/js/wp-sudo-admin.js` | Access module reads picker value and rejects invalid selection | ✓ VERIFIED | Reads `#wp-sudo-grant-user`, parses value, rejects `0`, focuses field, announces validation, and submits unchanged `user_id`/`cap` shape. |
| `tests/Unit/AdminTest.php` | Render test asserts new markup; grant handler tests remain | ✓ VERIFIED | Render test stubs administrator `get_users()` and asserts picker/labels; local targeted PHPUnit passed. |
| `tests/e2e/specs/access-grant.spec.ts` | E2E spec uses selectOption and admin target | ✓ VERIFIED | Target is administrator-role; ACCESS-01/ACCESS-02 still assert no-session blocked and with-session grant lands. |
| `.github/workflows/phpunit.yml` | 7 integration lanes + dropped-coverage docs + WP test cache | ✓ VERIFIED | YAML parses; 7 integration includes; cache paths and dropped-lane comments present. |
| `.github/workflows/e2e.yml` | access-grant assigned to group; shard rebalance noted | ✓ VERIFIED | YAML parses; `access-grant.spec.ts` appears exactly once in E2E group commands; group 2 no longer carries CHAL-09/10/11. |
| `docs/current-metrics.md` | Correct CI Matrix Snapshot and current metrics | ✗ FAILED | CI Matrix Snapshot and E2E count are present, but live metrics drift causes `composer verify:metrics` to fail. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `render_access_tab()` | Administrator users | `get_users( array( 'role' => 'administrator', ... ) )` | ✓ WIRED | `includes/class-admin.php:1375-1385`. |
| Grant UI | `#wp-sudo-grant-user` value | JS `parseInt(userInput.value, 10)` | ✓ WIRED | `admin/js/wp-sudo-admin.js:358-376`; unchanged request body `{ user_id: userId, cap: ... }`. |
| Capability labels | Exact capability slug values | `<option value="$cap" title="$cap">Label ($cap)</option>` | ✓ WIRED | `includes/class-admin.php:1455-1461`. |
| Grant handler | Existing authorization contract | nonce + `wp_sudo_can` + `GOVERNANCE_CAPS` | ✓ WIRED | `includes/class-admin.php:1559-1572`. |
| E2E workflow | `tests/e2e/specs/access-grant.spec.ts` | Group 3 `npm run test:e2e -- ... access-grant.spec.ts ...` | ✓ WIRED | `.github/workflows/e2e.yml:90-92`; GH run `27863473541` success. |
| PHPUnit workflow | WP test lib cache | `actions/cache` + installer paths | ✓ WIRED | `.github/workflows/phpunit.yml:259-273`; GH run `27863473547` success with 7 integration jobs. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| ACC-01 | 13.1-01 | Replace numeric user-ID input with searchable user picker populated from real administrator users; reject invalid selections; server `(int) $_POST['user_id']` unchanged. | ✗ PARTIAL | Numeric input replaced with admin-populated select and validation/server cast preserved, but no explicit searchable picker/search/autocomplete behavior exists. |
| ACC-02 | 13.1-01 | Plain-English capability labels with raw slug secondary; option values stay exact slugs. | ✓ SATISFIED | `CAP_LABELS` and option rendering at `includes/class-admin.php:103-108,1455-1461`; unit assertions cover labels and values. |
| ACC-03 | 13.1-01 | Server auth untouched; E2E grant-flow updated and green; a11y preserved. | ✓ SATISFIED | Handler checks unchanged; E2E spec uses selectOption/admin target; GH E2E run `27863473541` success; labels/live region/speak present. |
| CIS-01 | 13.1-02 | Trim integration matrix from 10 to ~7 lanes and record dropped coverage. | ✓ SATISFIED | `.github/workflows/phpunit.yml` has 7 integration includes and dropped-lane comments. |
| CIS-02 | 13.1-02 | Cache WordPress test library across integration lanes so downloads/install are skipped on cache hit. | ✓ SATISFIED | `actions/cache` covers test-lib/core directories; installer skips existing directories; config is regenerated fresh. |
| CIS-03 | 13.1-02 | Rebalance E2E shards to lower critical path; note realized improvement and wp-env floor. | ✓ SATISFIED | E2E group 2 is rebalanced, access-grant is in group 3, fixed startup floor comment exists, and post-change GH run `27863473541` shows group 2 completed before group 1. |

No orphaned Phase 13.1 requirements found: REQUIREMENTS.md maps exactly ACC-01, ACC-02, ACC-03, CIS-01, CIS-02, and CIS-03 to Phase 13.1.

### Automated Checks Run

| Check | Result | Evidence |
|---|---|---|
| Previous verification check | ✓ No previous verification | No `*-VERIFICATION.md` existed in the phase directory before this report. |
| Project skills discovery | ✓ None | `.claude/skills/` and `.agents/skills/` are absent. |
| Targeted PHPUnit | ✓ Passed | `./vendor/bin/phpunit tests/Unit/AdminTest.php --filter 'test_render_settings_page_renders_access_tab|test_handle_grant_cap'` → 5 tests / 16 assertions. |
| Workflow YAML parse | ✓ Passed | Python `yaml.safe_load()` parsed `.github/workflows/phpunit.yml` and `.github/workflows/e2e.yml`. |
| Metrics verification | ✗ Failed | `composer verify:metrics` reports stale assertion and PHP line counts in `docs/current-metrics.md`. |
| GitHub Actions evidence | ✓ Passed | `gh run view 27863473547` (PHPUnit) and `gh run view 27863473541` (E2E) both report `conclusion: success` for 2026-06-20 runs. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `docs/current-metrics.md` | n/a | Stale canonical metrics | 🛑 Blocker | Project verification requires `composer verify:metrics`; current doc drift undermines canonical metrics claims. |
| `includes/class-admin.php` | 1442 | Plain select for requirement wording "searchable user picker" | 🛑 Blocker | If "searchable" means more than native select typeahead, ACC-01 is not fully achieved. |

Other grep hits (`placeholder` examples and `return null`) in `includes/class-admin.php` are pre-existing request tester/help-path code and not blockers for this phase.

### Human Verification Required

None required before addressing the automated gaps. After the searchable-picker decision/fix, a browser/accessibility smoke pass is still appropriate:

1. **Access tab picker UX**
   - **Test:** Open Settings → Sudo → Access with multiple administrator users and try to locate a target user by typing/searching.
   - **Expected:** The target can be found without knowing a numeric ID; empty selection blocks before the AJAX request.
   - **Why human:** Searchability and operator feel are UI/UX behaviors.

2. **Assistive technology feedback**
   - **Test:** With a screen reader or browser accessibility tooling, trigger empty selection and successful grant messages.
   - **Expected:** The label associations and `aria-live`/`wp.a11y.speak` feedback are announced clearly.
   - **Why human:** Programmatic grep verifies wiring, not actual AT announcement quality.

### Gaps Summary

1. **ACC-01 searchability is not fully implemented**
   - The Access tab now uses a real administrator dropdown and removes the numeric input, but the ROADMAP/REQUIREMENTS contract says "searchable user picker." The code has no explicit search/autocomplete/listbox behavior or tests.

2. **Canonical metrics drift blocks project verification**
   - `docs/current-metrics.md` includes the CI Matrix Snapshot/E2E count, but live repository metrics no longer match. Update the stale assertion and PHP line counts and re-run `composer verify:metrics`.

---

_Verified: 2026-06-28T06:04:43Z_  
_Verifier: Claude (gsd-verifier)_
