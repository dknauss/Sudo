---
phase: 13.1-access-tab-ux-polish-and-ci-test-speed
verified: 2026-06-28T16:14:46Z
status: passed
score: 11/11 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: "8/10 plan must-haves verified; 5/6 roadmap success criteria fully verified"
  gaps_closed:
    - "The Grant Capability form now provides an explicit searchable administrator-scoped user picker, not merely a plain dropdown."
    - "docs/current-metrics.md now matches live repository metrics and composer verify:metrics passes."
  gaps_remaining: []
  regressions: []
---

# Phase 13.1: Access-tab UX Polish and CI Test-speed Verification Report

**Phase Goal:** Access-tab UX polish + CI test-speed, including gap closure for searchable Access picker, canonical metrics verification, and public Access tab screenshot refresh/visual verification.
**Verified:** 2026-06-28T16:14:46Z  
**Status:** passed  
**Re-verification:** Yes — after gap closure

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Operator selects the grant target from real administrator users; no numeric ID input remains | ✓ VERIFIED | `includes/class-admin.php:1375-1383` calls `get_users()` with `role => administrator`; `includes/class-admin.php:1444-1449` renders user IDs as select option values; `tests/Unit/AdminTest.php:3040` asserts the old numeric input is absent. |
| 2 | Grant target control is explicitly searchable, not a plain dropdown | ✓ VERIFIED | Gap-closure commit `711dfaf` added `input type="search" id="wp-sudo-grant-user-search"` with `aria-controls="wp-sudo-grant-user"` at `includes/class-admin.php:1442`; the select has `data-searchable="true"` at line 1444. |
| 3 | Searchability is progressive enhancement and preserves existing select/user_id submission | ✓ VERIFIED | `admin/js/wp-sudo-admin.js:353-383` filters existing select options by visible name/login and auto-selects only a single match; the grant handler still reads `parseInt( userInput.value, 10 )` and submits `{ user_id: userId, cap: ... }` at lines 389-413. |
| 4 | ACC-01 has automated regression coverage for searchable-picker markup | ✓ VERIFIED | `tests/Unit/AdminTest.php:3031-3039` asserts search input, placeholder/help text, searchable select, option value, and `data-search-text`; lines 3047-3050 assert JS wiring and `user_id: userId`. Targeted PHPUnit passed: 5 tests / 24 assertions. |
| 5 | Capability options read in plain English with raw slug retained as secondary text | ✓ VERIFIED | `CAP_LABELS` remains in `includes/class-admin.php:103-108`; options retain exact slug values and display label plus slug at `includes/class-admin.php:1458-1462`; unit assertions cover labels/values at `tests/Unit/AdminTest.php:3041-3045`. |
| 6 | Empty/placeholder user selection is rejected before AJAX | ✓ VERIFIED | Placeholder option remains value `0` at `includes/class-admin.php:1445`; JS returns before `sendAccessAction()` when `! userId || userId < 1` at `admin/js/wp-sudo-admin.js:394-404`. |
| 7 | Server-side authorization remains unchanged | ✓ VERIFIED | `handle_grant_cap()` still performs nonce verification, `wp_sudo_can( 'manage_wp_sudo' )`, integer `$_POST['user_id']`, `sanitize_key()`, and `GOVERNANCE_CAPS` validation at `includes/class-admin.php:1562-1576`. `composer test:unit` and `composer analyse` passed. |
| 8 | #88 E2E grant-flow spec remains wired to the new markup and ACCESS-01/02 exist | ✓ VERIFIED | `npx playwright test --config tests/e2e/playwright.config.ts --list` lists ACCESS-01 and ACCESS-02 in `access-grant.spec.ts`; `.github/workflows/e2e.yml:90-92` runs `access-grant.spec.ts` in group 3. Latest `E2E Tests` run `28312201395` concluded success on 2026-06-28. |
| 9 | a11y labels and announcements preserved | ✓ VERIFIED | User/cap labels remain at `includes/class-admin.php:1439,1455`; search input controls the select via `aria-controls`; live status remains `role="status" aria-live="polite"` at line 1473; JS still calls `announce()`/`wp.a11y.speak`. |
| 10 | CI test-speed scope is implemented: 7 integration lanes, cache, and E2E rebalance | ✓ VERIFIED | Parsed `.github/workflows/phpunit.yml`: 7 integration lanes, 1 multisite lane, 1 MariaDB lane; dropped-lane rationale at lines 163-175; WordPress test cache and fresh config regeneration at lines 258-273. Parsed `.github/workflows/e2e.yml`: 4 groups, `access-grant.spec.ts` appears once in group 3 and 2FA remains in group 2. |
| 11 | Canonical metrics and public Access screenshot are current | ✓ VERIFIED | Gap-closure commits `73fe7cb` and `d5fd0f6`; `composer verify:metrics` passes; `.wordpress-org/screenshot-6.png` is a valid 1280×960 PNG and local visual inspection shows Settings → Sudo → Access with the visible search field. `readme.md`, `README.md`, and `readme.txt` reference/caption screenshot 6. |

**Score:** 11/11 must-haves verified.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `includes/class-admin.php` | Admin-scoped searchable grant picker + capability labels + unchanged auth | ✓ VERIFIED | Search input/select markup is substantive and wired; administrator users populate exact ID option values; handler authorization checks remain intact. |
| `admin/js/wp-sudo-admin.js` | Search/filter behavior and unchanged grant submission contract | ✓ VERIFIED | Search filters options via `data-search-text`; select remains source of truth for `user_id`; invalid placeholder returns before AJAX. |
| `tests/Unit/AdminTest.php` | Render regression coverage and grant-handler coverage | ✓ VERIFIED | Asserts searchable markup, option values, JS wiring, and old numeric input absence; targeted and full unit suites passed. |
| `tests/e2e/specs/access-grant.spec.ts` | ACCESS-01/02 grant-flow regression spec | ✓ VERIFIED | Listed by Playwright; workflow group 3 invokes it. |
| `.github/workflows/phpunit.yml` | 7-lane integration matrix and WordPress test-library cache | ✓ VERIFIED | YAML parses; matrix count verified; `actions/cache` and `rm -f /tmp/wordpress-tests-lib/wp-tests-config.php` present. |
| `.github/workflows/e2e.yml` | Rebalanced E2E groups with access-grant included | ✓ VERIFIED | YAML parses; 4 groups verified; access-grant appears exactly once in group commands. |
| `docs/current-metrics.md` | Canonical metrics aligned with live counts | ✓ VERIFIED | `composer verify:metrics` reports the file is in sync; unit assertions are 2624 and E2E count is 64 tests in 15 files. |
| `.wordpress-org/screenshot-6.png` | Current Access tab screenshot | ✓ VERIFIED | Valid PNG, 1280×960; visual inspection confirms the searchable picker is visible. |
| `readme.md` | GitHub-facing Access screenshot reference | ✓ VERIFIED | References `.wordpress-org/screenshot-6.png` / Access tab at lines 44-48. |
| `README.md` | Repository README screenshot reference | ✓ VERIFIED | References `.wordpress-org/screenshot-6.png` / Access tab at lines 44-48. |
| `readme.txt` | WordPress.org screenshot caption/list entry | ✓ VERIFIED | Screenshot 6 caption says “Access tab — manage dedicated Sudo governance capabilities and revoke sessions” at line 178. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `render_access_tab()` | Administrator users | `get_users( role => administrator )` | ✓ WIRED | `includes/class-admin.php:1375-1383`. |
| Search input | Grant select | `aria-controls`, `data-search-text`, JS input listener | ✓ WIRED | Markup at `includes/class-admin.php:1442-1449`; filtering at `admin/js/wp-sudo-admin.js:353-377`. |
| Grant handler JS | Existing AJAX contract | Reads select value, submits `user_id` and `cap` | ✓ WIRED | `admin/js/wp-sudo-admin.js:389-413`. |
| Capability labels | Exact capability slug values | Option `value="$cap"` with label + slug text | ✓ WIRED | `includes/class-admin.php:1458-1462`. |
| Grant endpoint | Existing authorization contract | Nonce + `wp_sudo_can` + `GOVERNANCE_CAPS` | ✓ WIRED | `includes/class-admin.php:1562-1576`. |
| E2E workflow | `access-grant.spec.ts` | Group 3 test command | ✓ WIRED | `.github/workflows/e2e.yml:90-92`; latest E2E workflow success observed via `gh run list --workflow e2e.yml`. |
| PHPUnit workflow | WordPress test-library cache | `actions/cache` + fresh config removal before installer | ✓ WIRED | `.github/workflows/phpunit.yml:258-273`; latest PHPUnit workflow success observed via `gh run list`. |
| README/readme docs | Refreshed screenshot asset | Stable screenshot 6 reference/caption | ✓ WIRED | `readme.md`, `README.md`, and `readme.txt` all point readers to Access screenshot 6. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| ACC-01 | 13.1-01, 13.1-03 | Replace numeric user-ID input with a searchable user picker populated from real administrator users; reject invalid selections; preserve server `(int) $_POST['user_id']`. | ✓ SATISFIED | Search input + admin select at `includes/class-admin.php:1442-1449`; JS filtering and unchanged select submission at `admin/js/wp-sudo-admin.js:353-413`; unit coverage at `tests/Unit/AdminTest.php:3031-3050`. |
| ACC-02 | 13.1-01 | Plain-English capability labels with raw slug secondary; option values stay exact slugs. | ✓ SATISFIED | `CAP_LABELS` and option rendering at `includes/class-admin.php:103-108,1458-1462`; unit assertions at `tests/Unit/AdminTest.php:3041-3045`. |
| ACC-03 | 13.1-01, 13.1-03 | Server auth untouched; E2E grant-flow updated and green; a11y preserved; screenshot not misleading after UI change. | ✓ SATISFIED | Handler checks unchanged; ACCESS-01/02 listed; latest E2E run success; labels/live region/search controls present; screenshot 6 refreshed and visually verified. |
| CIS-01 | 13.1-02, 13.1-03 | Trim integration matrix from 10 to about 7 representative lanes and record dropped coverage. | ✓ SATISFIED | Parsed workflow has 7 integration lanes; dropped multisite lanes documented in `.github/workflows/phpunit.yml:163-175`. |
| CIS-02 | 13.1-02, 13.1-03 | Cache WordPress test library across integration lanes so downloads/install are skipped on cache hit. | ✓ SATISFIED | `actions/cache` covers `/tmp/wordpress-tests-lib/includes`, `/tmp/wordpress-tests-lib/data`, and `/tmp/wordpress`; config is removed before installer so credentials regenerate. |
| CIS-03 | 13.1-02, 13.1-03 | Rebalance E2E shards to lower critical path; note realized improvement and wp-env floor. | ✓ SATISFIED | Group comments note fixed wp-env startup floor; CHAL-09/10/11 moved out of group 2; access-grant assigned to group 3; latest E2E workflow succeeded. |

No orphaned Phase 13.1 requirements found: `.planning/REQUIREMENTS.md` maps exactly ACC-01, ACC-02, ACC-03, CIS-01, CIS-02, and CIS-03 to Phase 13.1, and all six are accounted for above.

### Verification Commands and Evidence

| Check | Result | Evidence |
|---|---|---|
| Previous verification | ✓ Re-verification mode | Prior `13.1-VERIFICATION.md` had `status: gaps_found` with two gaps: missing explicit searchability and stale metrics. |
| Project instructions | ✓ Loaded | Read `CLAUDE.md`; no project `.claude/skills/` or `.agents/skills/` SKILL.md files were present. |
| Artifact/key-link helper | ℹ️ Manual fallback used | `gsd-tools verify artifacts/key-links` returned “No must_haves...” for nested frontmatter, so artifacts and links were verified manually. |
| Targeted PHPUnit | ✓ Passed | `./vendor/bin/phpunit tests/Unit/AdminTest.php --filter 'test_render_settings_page_renders_access_tab|test_handle_grant_cap'` → 5 tests, 24 assertions. |
| Full unit suite | ✓ Passed | `composer test:unit` → 888 tests, 2624 assertions. |
| Static analysis | ✓ Passed | `composer analyse` → PHPStan/Psalm command completed with no blocking errors. |
| Metrics verification | ✓ Passed | `composer verify:metrics` → “Metrics verified: docs/current-metrics.md is in sync.” |
| Workflow YAML parse | ✓ Passed | Python `yaml.safe_load()` parsed `.github/workflows/phpunit.yml` and `.github/workflows/e2e.yml`. |
| Workflow matrix checks | ✓ Passed | Parsed `phpunit.yml`: 7 integration lanes, 1 multisite lane, 1 MariaDB lane. Parsed `e2e.yml`: 4 groups, `access-grant.spec.ts` appears once in group 3. |
| E2E test inventory | ✓ Passed | `npx playwright test --config tests/e2e/playwright.config.ts --list` → 64 tests in 15 files, including ACCESS-01/02. |
| Screenshot asset | ✓ Passed | `file`/PIL confirmed `.wordpress-org/screenshot-6.png` is 1280×960 PNG; local visual inspection showed the Access tab search field. |
| Commit evidence | ✓ Passed | Gap closure commits exist: `711dfaf` searchable picker, `73fe7cb` canonical metrics, `d5fd0f6` screenshot refresh, `c8ce754` summary tracking. |
| GitHub Actions evidence | ✓ Passed | `gh run list` showed latest PHPUnit run `28312201407` success and latest E2E Tests run `28312201395` success for 2026-06-28. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `includes/class-admin.php` | 2287, 2292, 2397 | `return null` | ℹ️ Info | Pre-existing helper/tester paths outside this phase’s Access grant picker and CI changes; not a blocker. |

No blocker anti-patterns were found in the phase-modified artifacts. No TODO/FIXME/placeholder/coming-soon stubs were found in the relevant modified files.

### Human Verification Required

None blocking. The refreshed screenshot was inspected locally and shows the intended Access tab search field. Full browser interaction was also represented by the latest successful E2E workflow.

### Gaps Summary

All previously reported gaps are closed:

1. **ACC-01 searchability** — closed by `711dfaf`; the Access grant control now has explicit search input/filter behavior and regression tests.
2. **Canonical metrics drift** — closed by `73fe7cb`; `composer verify:metrics` passes.
3. **Public screenshot freshness** — closed by `d5fd0f6`; screenshot 6 is valid PNG data and visually shows the search field.

Phase 13.1 goal is achieved.

---

_Verified: 2026-06-28T16:14:46Z_  
_Verifier: Claude (gsd-verifier)_
