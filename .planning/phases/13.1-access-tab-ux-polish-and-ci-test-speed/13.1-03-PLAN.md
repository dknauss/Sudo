---
phase: 13.1-access-tab-ux-polish-and-ci-test-speed
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - includes/class-admin.php
  - admin/js/wp-sudo-admin.js
  - tests/Unit/AdminTest.php
  - docs/current-metrics.md
  - .wordpress-org/screenshot-6.png
  - readme.md
  - README.md
  - readme.txt
autonomous: true
gap_closure: true
requirements: [ACC-01, ACC-03, CIS-01, CIS-02, CIS-03]
user_setup: []

must_haves:
  truths:
    - "The Access tab grant target control is explicitly searchable, not merely a raw numeric field or plain dropdown."
    - "Searchability is progressive enhancement: administrators can search by visible name/login while the existing select/user_id submission contract remains the source of truth."
    - "ACC-01 is covered by automated tests that fail if the searchable affordance disappears."
    - "docs/current-metrics.md matches live repository assertion and PHP line counts, and `composer verify:metrics` passes."
    - "The public Access tab screenshot and README/readme screenshot references are refreshed or visually verified in a browser-capable session so documentation is not misleading after the UI change."
  artifacts:
    - path: "includes/class-admin.php"
      provides: "Searchable administrator-scoped grant picker markup while preserving exact user ID option values"
      contains: "wp-sudo-grant-user-search"
    - path: "admin/js/wp-sudo-admin.js"
      provides: "Client-side search/filter behavior for the administrator picker"
      contains: "wp-sudo-grant-user-search"
    - path: "tests/Unit/AdminTest.php"
      provides: "Rendered-output regression coverage for explicit searchable-picker markup"
      contains: "wp-sudo-grant-user-search"
    - path: "docs/current-metrics.md"
      provides: "Canonical current metrics aligned with live counts"
      contains: "Unit assertions"
    - path: ".wordpress-org/screenshot-6.png"
      provides: "Current Access tab screenshot showing the searchable grant picker UI"
      contains: "binary PNG"
    - path: "readme.md"
      provides: "GitHub-facing screenshot reference for the Access tab"
      contains: ".wordpress-org/screenshot-6.png"
    - path: "README.md"
      provides: "Repository README screenshot reference for the Access tab"
      contains: ".wordpress-org/screenshot-6.png"
    - path: "readme.txt"
      provides: "WordPress.org screenshot caption/list entry for screenshot 6"
      contains: "Access tab"
  key_links:
    - from: "admin/js/wp-sudo-admin.js"
      to: "select#wp-sudo-grant-user"
      via: "search input filters/selects existing option values; grant handler still reads the select value"
      pattern: "wp-sudo-grant-user"
    - from: "includes/class-admin.php"
      to: "tests/Unit/AdminTest.php"
      via: "rendered-output assertions for search input/list behavior and exact option values"
      pattern: "wp-sudo-grant-user-search"
    - from: "docs/current-metrics.md"
      to: "bin/verify-metrics.sh"
      via: "`composer verify:metrics` live-count comparison"
      pattern: "composer verify:metrics"
    - from: "readme.md / README.md / readme.txt"
      to: ".wordpress-org/screenshot-6.png"
      via: "Access tab screenshot reference/caption"
      pattern: "screenshot-6\\.png|Access tab"
---

<objective>
Close the two unresolved Phase 13.1 verification gaps without duplicating completed base-plan work.

**Purpose:** ACC-01 requires a searchable user picker, not only an administrator `<select>`, and the canonical metrics document must pass the project metrics verifier.

**Output:** A minimal progressive-enhancement search affordance for the existing administrator picker, test coverage for that affordance, updated live metrics in `docs/current-metrics.md`, and an actually refreshed or browser-verified Access tab screenshot package for README/readme surfaces.

Do not change the server-side grant/revoke authorization contract from Plan 01. The picker remains convenience UI only; `handle_grant_cap()` must still receive `user_id` as the selected numeric option value and validate it with the existing nonce, `wp_sudo_can( 'manage_wp_sudo' )`, and `GOVERNANCE_CAPS` path.
</objective>

<execution_context>
@/Users/danknauss/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-CONTEXT.md
@.planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-RESEARCH.md
@.planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-VERIFICATION.md
@.planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-01-SUMMARY.md
@.planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-02-SUMMARY.md
@./AGENTS.md
@./CLAUDE.md

<interfaces>
Existing Access picker contract from `includes/class-admin.php`:
```php
$grant_users = get_users(
	array(
		'role'    => 'administrator',
		'orderby' => 'display_name',
		'order'   => 'ASC',
		'fields'  => 'all',
		'number'  => -1,
	)
);
...
<select id="wp-sudo-grant-user">
	<option value="0">— Select a user —</option>
	<option value="{ID}">{display_name} ({user_login})</option>
</select>
```

Existing grant JS contract from `admin/js/wp-sudo-admin.js`:
```js
var userInput = document.getElementById( 'wp-sudo-grant-user' );
var userId    = userInput ? parseInt( userInput.value, 10 ) : 0;
...
sendAccessAction( wpSudoAdmin.grantAction, nonce, { user_id: userId, cap: ... }, ... );
```

Existing rendered-output test in `tests/Unit/AdminTest.php` stubs `get_users()` with an administrator user `Ada Admin (ada)` and asserts the `<select>` and exact numeric option value. Extend this test; do not replace it with a looser assertion.

Verification gap from `13.1-VERIFICATION.md`:
- ACC-01 partial: plain `<select>` exists, but no explicit searchable/autocomplete/listbox behavior or tests.
- Metrics failed: `docs/current-metrics.md` has unit assertions `2606` vs actual `2616`, production PHP lines `15953` vs actual `15992`, tests PHP lines `32444` vs actual `32471`, production+tests `48397` vs actual `48463`, total repo PHP lines `48669` vs actual `48735`.

Project instruction from `AGENTS.md`:
- Any phase that significantly changes UI must include a README/readme screenshot refresh task. Phase 13.1 changes the public Access tab UI, and `readme.md` / `README.md` reference `.wordpress-org/screenshot-6.png`; `readme.txt` captions screenshot 6 as the Access tab.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit searchable administrator picker behavior for ACC-01</name>
  <files>includes/class-admin.php, admin/js/wp-sudo-admin.js, tests/Unit/AdminTest.php</files>
  <behavior>
    - Test 1: Rendered Access tab contains an explicit search field for the grant picker, with `id="wp-sudo-grant-user-search"`, `type="search"`, `aria-controls="wp-sudo-grant-user"`, and helper text telling operators they can search by name or username.
    - Test 2: The existing administrator-scoped `<select id="wp-sudo-grant-user">` remains present with `value="0"` placeholder and exact numeric administrator option values such as `value="7"`.
    - Test 3: The search enhancement is wired to the existing select rather than changing the AJAX payload; static checks find `wp-sudo-grant-user-search` in JS and the grant action still sends `{ user_id: userId, cap: ... }`.
  </behavior>
  <action>
    Follow TDD:
    1. RED: Extend `tests/Unit/AdminTest.php::test_render_settings_page_renders_access_tab` before production changes. Keep the current assertions for `<select id="wp-sudo-grant-user">`, placeholder, `Ada Admin (ada)`, no numeric input, and capability labels. Add assertions for the new search input/helper/searchable metadata. Run the targeted PHPUnit command and confirm it fails because the search markup is missing.
    2. GREEN markup: In `includes/class-admin.php::render_access_tab()`, add a small progressive-enhancement search control immediately before the grant-user `<select>`. Use no external dependency. Preferred markup:
       - `<input type="search" id="wp-sudo-grant-user-search" class="regular-text" autocomplete="off" aria-controls="wp-sudo-grant-user" placeholder="Search administrators by name or username">`
       - keep `<label for="wp-sudo-grant-user">User</label>` for the actual submitted select;
       - add a short `.description` paragraph explaining search filters the administrator list;
       - add `data-searchable="true"` to the select;
       - add per-option `data-search-text` containing the lower/searchable display name and login (and email only if already safely available on the `WP_User`; do not expose new sensitive fields unnecessarily).
       Preserve every option `value` as the integer user ID and preserve the placeholder `value="0"`. Do not modify `handle_grant_cap()` or `handle_revoke_cap()`.
    3. GREEN behavior: In `admin/js/wp-sudo-admin.js`, add dependency-free progressive enhancement near the grant-form setup:
       - locate `#wp-sudo-grant-user-search` and `#wp-sudo-grant-user`;
       - on `input`, case-insensitively filter the select options by `data-search-text` / visible text;
       - keep the placeholder visible;
       - if exactly one administrator option matches, select it; if zero or multiple match, leave/reset selection to `0` so the existing invalid-user guard still blocks;
       - on `change` of the select, update the search text to the selected option label (except placeholder) so mouse/keyboard selection stays coherent.
       Avoid replacing the native select with a custom inaccessible listbox; this is a minimal explicit search/filter affordance to close the gap.
    4. Run targeted and full local quality checks. If PHPStan flags the markup helper as unreachable or redundant, simplify rather than suppress. If PHPCS complains about line length/escaping, fix with standard WordPress escaping.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit tests/Unit/AdminTest.php --filter test_render_settings_page_renders_access_tab</automated>
    <automated>node -e "const fs=require('fs'); const php=fs.readFileSync('includes/class-admin.php','utf8'); const js=fs.readFileSync('admin/js/wp-sudo-admin.js','utf8'); if(!php.includes('wp-sudo-grant-user-search')) throw new Error('missing searchable picker markup'); if(!/type=\"search\"/.test(php)) throw new Error('missing search input type'); if(!/aria-controls=\"wp-sudo-grant-user\"/.test(php)) throw new Error('missing aria-controls'); if(!js.includes('wp-sudo-grant-user-search')) throw new Error('missing search behavior JS'); if(!/user_id:\\s*userId/.test(js)) throw new Error('grant payload no longer sends selected userId'); console.log('searchable picker static checks OK');"</automated>
    <automated>composer test:unit</automated>
    <automated>composer analyse</automated>
    <automated>composer lint</automated>
  </verify>
  <done>ACC-01 has an explicit searchable/filterable administrator picker with automated markup and JS wiring checks; the existing selected numeric user ID contract is unchanged; unit tests, PHPStan, and PHPCS pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh canonical metrics from live counts and pass verify:metrics</name>
  <files>docs/current-metrics.md</files>
  <action>
    Update only canonical metrics that live verification proves are stale. Do not guess counts.
    1. Run the live commands before editing:
       - `composer test:unit` to confirm current test/assertion totals.
       - `find ./includes ./wp-sudo.php ./uninstall.php ./mu-plugin ./bridges -type f -name "*.php" -print0 | xargs -0 wc -l | tail -1 | awk '{print $1}'`
       - `find ./tests -type f -name "*.php" -print0 | xargs -0 wc -l | tail -1 | awk '{print $1}'`
       - `find . -type f -name "*.php" ! -path "*/vendor/*" ! -path "*/vendor_test/*" ! -path "*/.tmp/*" ! -path "*/.git/*" -print0 | xargs -0 wc -l | tail -1 | awk '{print $1}'`
    2. Update `docs/current-metrics.md` with the verified values from the commands above and/or the latest `composer verify:metrics` mismatch output. Treat the drift values quoted in `13.1-VERIFICATION.md` as historical evidence only, not as constants to copy: Task 1 changes PHP/test line counts, so the current live values may differ by the time this task runs. Recompute the test-to-production ratio from the live line counts and format consistently with the existing table.
    3. Update `Last verified` and the relevant verification notes to 2026-06-28. Keep the existing CI Matrix Snapshot and E2E count unless live commands show they changed.
    4. Run `composer verify:metrics`. If it still fails, update the remaining mismatched values from the command output and rerun until it passes.
  </action>
  <verify>
    <automated>composer verify:metrics</automated>
    <automated>php -r '$doc=file_get_contents("docs/current-metrics.md"); $prod=(int) trim(shell_exec("find ./includes ./wp-sudo.php ./uninstall.php ./mu-plugin ./bridges -type f -name \"*.php\" -print0 | xargs -0 wc -l | tail -1 | awk '\''{print $1}'\''")); $tests=(int) trim(shell_exec("find ./tests -type f -name \"*.php\" -print0 | xargs -0 wc -l | tail -1 | awk '\''{print $1}'\''")); foreach (array(number_format($prod), number_format($tests)) as $needle) { if (false === strpos($doc, $needle)) { fwrite(STDERR, "docs/current-metrics.md missing live count $needle\n"); exit(1); } } echo "current metrics contain live PHP line counts\n";'</automated>
  </verify>
  <done>`docs/current-metrics.md` matches live assertion and PHP line counts, records the update date, and `composer verify:metrics` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh or verify Access tab screenshot documentation</name>
  <files>.wordpress-org/screenshot-6.png, readme.md, README.md, readme.txt</files>
  <action>
    Satisfy the AGENTS.md UI screenshot planning requirement for this Access tab UI change.
    1. Inspect the current screenshot references:
       - `readme.md` and `README.md` should continue to reference `.wordpress-org/screenshot-6.png` for the Access tab image.
       - `readme.txt` screenshot list entry 6 should still describe the Access tab accurately.
    2. Refresh `.wordpress-org/screenshot-6.png` so it shows the updated Settings → Sudo → Access tab with the searchable grant-user picker from Task 1. Use the existing project/local WordPress screenshot workflow if available. This step requires browser-capable visual inspection/capture (`codex-browser` or `codex-browser-handoff`) unless the executor can prove the existing screenshot already visually shows the updated searchable picker. If the execution session is not browser-capable and cannot visually verify the screenshot, mark this task/plan BLOCKED rather than complete; documenting deferral in the SUMMARY is not sufficient to satisfy `<done>`.
    3. If the screenshot visual content changes, keep the filename stable (`.wordpress-org/screenshot-6.png`) so WordPress.org and README references do not need churn. Update only alt/caption text if the new searchable picker makes the existing wording inaccurate.
    4. Do not add unrelated screenshots or renumber screenshot entries. Do not update volatile project metrics here; Task 2 owns metrics.
  </action>
  <verify>
    <automated>test -f .wordpress-org/screenshot-6.png && file .wordpress-org/screenshot-6.png | grep -q 'PNG image data'</automated>
    <automated>grep -q '.wordpress-org/screenshot-6.png' readme.md && grep -q '.wordpress-org/screenshot-6.png' README.md && grep -Eq '^6\\. Access tab' readme.txt</automated>
    <automated>git diff -- .wordpress-org/screenshot-6.png readme.md README.md readme.txt | grep -E 'screenshot-6\\.png|Access tab|Binary files|\\.wordpress-org/screenshot-6\\.png' >/dev/null || grep -Eq 'screenshot-6\\.png.*(visually verified|refreshed)|Access tab screenshot.*(visually verified|refreshed)' .planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-03-SUMMARY.md</automated>
  </verify>
  <done>`screenshot-6.png` exists as a valid PNG, the README/readme references/caption remain accurate for the updated Access tab, and the screenshot has either been refreshed or visually verified in a browser-capable session after the searchable picker change. A non-browser session may not mark this task done by deferring capture; it must block and request a fresh `codex-browser` or `codex-browser-handoff` session.</done>
</task>

</tasks>

<verification>
- ACC-01 gap closure: targeted AdminTest render test passes and static JS/markup check proves the search input is present and wired to the existing select/user_id payload.
- Regression safety: `composer test:unit`, `composer analyse`, and `composer lint` pass.
- Metrics gap closure: `composer verify:metrics` passes after `docs/current-metrics.md` is updated from live counts.
- Screenshot/doc gap closure: `.wordpress-org/screenshot-6.png` is a valid PNG, README/readme screenshot references still point to screenshot 6, and the SUMMARY records the refreshed screenshot or browser-capable visual verification.
- Manual/browser requirement: execute Task 3 in a browser-capable Codex session (`codex-browser` or `codex-browser-handoff`) if the current session cannot capture or visually verify the Access tab screenshot. Do not complete this plan with screenshot capture merely deferred.
</verification>

<success_criteria>
- The verification truth "Grant Capability form provides a searchable administrator-scoped user picker" is fully satisfied by explicit search/filter UI and tests.
- The Access tab still submits the selected numeric administrator user ID through the existing AJAX contract; no grant/revoke authorization handler changes are made.
- `docs/current-metrics.md` is no longer stale; `composer verify:metrics` passes.
- The Access tab public screenshot asset and README/readme references are refreshed or visually verified per AGENTS.md so public docs do not silently drift from the UI; deferral alone is not accepted as completion.
- No unrelated Phase 13.1 base-plan work is duplicated or reshuffled.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-03-SUMMARY.md`.
Record the RED test failure for the searchable picker, final verification command outputs, the exact live metrics values used, and the screenshot refresh or browser-capable visual verification outcome for `.wordpress-org/screenshot-6.png`.
</output>
