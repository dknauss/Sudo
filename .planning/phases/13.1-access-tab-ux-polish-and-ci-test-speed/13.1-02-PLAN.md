---
phase: 13.1-access-tab-ux-polish-and-ci-test-speed
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .github/workflows/phpunit.yml
  - .github/workflows/e2e.yml
  - docs/current-metrics.md
autonomous: true
requirements: [CIS-01, CIS-02, CIS-03]
user_setup: []

must_haves:
  truths:
    - "The integration matrix runs a documented representative subset (7 lanes: the 5 MS=false lanes retained, one MS=true representative combo, MariaDB LTS) instead of all 10 — dropped coverage recorded in the workflow"
    - "Each integration lane skips the WordPress test-library download/install on an actions/cache hit, while wp-tests-config.php credentials are always regenerated fresh"
    - "access-grant.spec.ts is assigned to an E2E group and actually executes in CI (ACCESS-01/02 run), and the heavy challenge-2fa-ui group's critical-path time is lowered"
    - "docs/current-metrics.md CI Matrix Snapshot is corrected to the real lane configuration and E2E count BEFORE the workflow changes commit"
  artifacts:
    - path: ".github/workflows/phpunit.yml"
      provides: "Trimmed integration matrix (8 lanes) with dropped-coverage comment + actions/cache for WP test lib"
      contains: "actions/cache"
    - path: ".github/workflows/e2e.yml"
      provides: "access-grant.spec.ts assigned to a group; challenge-2fa-ui rebalanced"
      contains: "access-grant.spec.ts"
    - path: "docs/current-metrics.md"
      provides: "Corrected CI Matrix Snapshot (PHP/WP/lane count) + updated E2E test count"
      contains: "CI Matrix Snapshot"
  key_links:
    - from: ".github/workflows/phpunit.yml install step"
      to: "/tmp/wordpress-tests-lib + /tmp/wordpress"
      via: "actions/cache restore + rm wp-tests-config.php before install"
      pattern: "actions/cache"
    - from: ".github/workflows/e2e.yml a group test-command"
      to: "tests/e2e/specs/access-grant.spec.ts"
      via: "npm run test:e2e -- ... access-grant.spec.ts"
      pattern: "access-grant\\.spec\\.ts"
---

<objective>
Reduce CI wall-clock and runner-minutes for the integration + E2E suites — without losing meaningful coverage and with every tradeoff documented. Three levers (CONTEXT measured PR #86 head `b277c4c`, 2026-06-17): trim the integration matrix fan-out, cache the WordPress test library, and rebalance the E2E shards (also closing the orphaned `access-grant.spec.ts` gap).

**Purpose:** The tests are fast; CI cost is fan-out + per-job setup. Cut that cost; document the coverage-vs-speed decisions.

**Output:** Config-only changes to `.github/workflows/phpunit.yml` and `.github/workflows/e2e.yml`, plus a corrected `docs/current-metrics.md` CI snapshot. **Its own reviewable PR, separate from Plan 01's feature work** (CONTEXT requirement).

CROSS-TRACK COUPLING: CIS-03 wires `access-grant.spec.ts` into a CI group. That spec's markup is updated by Plan 01 (Task 3) and its target user becomes administrator-role there. This plan `depends_on: [01]` so the spec is admin-ready before it runs in CI — wiring an un-updated spec would fail (`page.fill` on a `<select>` throws). This plan does NOT edit the spec content; it only assigns it to a group.

NOTE: This is CI-configuration work — CLAUDE.md's TDD carve-out for "configuration changes" applies. Validation is via the actual CI run on the PR, not a local test framework. No design review required (no security-sensitive code path).
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-CONTEXT.md
@.planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-RESEARCH.md
@.planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-VALIDATION.md
@./CLAUDE.md

<interfaces>
<!-- Verified against live source 2026-06-19 (see 13.1-RESEARCH.md). Use these directly. -->

.github/workflows/phpunit.yml — integration matrix @ lines 163–213, exactly 10 lanes:
  1: PHP8.2 WP6.4 MS=false MySQL8.0   (support floor — KEEP)
  2: PHP8.2 WP6.7 MS=false MySQL8.0   (KEEP — full PHP×WP)
  3: PHP8.2 WP6.7 MS=true  MySQL8.0   (DROP — MS redundant with lane representative)
  4: PHP8.3 WP6.7 MS=false MySQL8.0   (KEEP — full PHP×WP)
  5: PHP8.3 WP6.7 MS=true  MySQL8.0   (DROP — MS redundant)
  6: PHP8.2 WP7.0 MS=false MySQL8.0   (KEEP — full PHP×WP)
  7: PHP8.2 WP7.0 MS=true  MySQL8.0   (DROP — MS redundant)
  8: PHP8.3 WP7.0 MS=false MySQL8.0   (KEEP — full PHP×WP)
  9: PHP8.3 WP7.0 MS=true  MySQL8.0   (KEEP — the one MS=true representative, latest combo)
 10: PHP8.2 WP6.7 MS=false MariaDB LTS (KEEP — DB-engine coverage)
- actions/cache@v5 already used for Composer @ ~lines 88–95 (pattern to mirror)
- WP_TESTS_DIR env = /tmp/wordpress-tests-lib ; WP core dir = /tmp/wordpress

Trimmed matrix = 7 lanes (drop 3,5,7): the 5 MS=false lanes (1,2,4,6,8 = 8.2×{6.4,6.7,7.0} + 8.3×{6.7,7.0}) + 1 MS=true representative (lane 9 = 8.3×7.0) + 1 MariaDB (lane 10). Saves 3 lanes (4 MS=true → 1). NOTE: MS=false is 5 lanes, not a full 2×3 grid (8.3×6.4 is intentionally absent — 6.4 is the floor, covered once on PHP 8.2); do NOT add lanes to "complete" the grid. Verify the live lane set at execution before editing.

bin/install-wp-tests.sh — verified:
- Downloads WP core tarball → /tmp/wordpress ; SVN-exports tests/phpunit/{includes,data} → /tmp/wordpress-tests-lib
- SVN export guarded by `[ -d $WP_TESTS_DIR ]` (~line 246) — skipped if dir exists (cache hit)
- wp-tests-config.php written only if `[ ! -s ... ]` (~line 255) — STALE credentials on cache hit unless deleted first
- Pitfall: caching wp-tests-config.php carries wrong DB creds → connection failures. Delete it before re-running install.

actions/cache pattern for WP test lib (RESEARCH-recommended):
  path: |
    /tmp/wordpress-tests-lib/includes
    /tmp/wordpress-tests-lib/data
    /tmp/wordpress
  key: wp-tests-${{ matrix.wp }}-${{ hashFiles('bin/install-wp-tests.sh') }}
  restore-keys: wp-tests-${{ matrix.wp }}-
  then: rm -f /tmp/wordpress-tests-lib/wp-tests-config.php before bash bin/install-wp-tests.sh ...

.github/workflows/e2e.yml — 4 named groups @ lines 75–95 (verified):
  group 1 challenge-basic-admin:   admin-bar-deactivate, admin-bar-timer, challenge ABAR|TIMR|CHAL-0[1-5]
  group 2 challenge-2fa-ui:        challenge CHAL-0[6-9]|CHAL-10|CHAL-11 ; cookie, gate-ui, keyboard   (5m19s OUTLIER)
  group 3 challenge-lockout-surfaces: challenge CHAL-12|13|14 ; public-api, smoke, stack-smoke
  group 4 challenge-replay-multisite: challenge CHAL-15|16|17 ; mu-plugin, multisite-network-admin, multisite-stack-smoke
- access-grant.spec.ts is in NO group's test-command → orphaned, never runs in CI (RESEARCH-confirmed)
- wp-env spin-up ~60s fixed floor per group job (diminishing returns on adding groups)

docs/current-metrics.md — STALE entries to correct (verified):
- line 44: "E2E tests | 61" — recount after access-grant.spec.ts joins CI
- lines 62–63 CI Matrix Snapshot: says "Integration matrix: PHP 8.0, 8.1, 8.3; WordPress 6.2, 6.7, 7.0" — WRONG; live is PHP 8.2/8.3, WP 6.4/6.7/7.0. Correct to the new trimmed lane set.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct docs/current-metrics.md CI Matrix Snapshot FIRST (CLAUDE.md update-first rule)</name>
  <files>docs/current-metrics.md</files>
  <action>
    Per CLAUDE.md, update the canonical CI-matrix count BEFORE the workflow changes commit. NOTE the sequencing: the E2E test count depends on Task 3 (which wires access-grant.spec.ts into a CI group), so the E2E count is FINALIZED in Task 3, not here — doing it here would record a stale pre-wiring number. This task does the self-contained matrix correction only.
    1. CI Matrix Snapshot (lines ~62–63): replace the stale "PHP 8.0, 8.1, 8.3; WordPress 6.2, 6.7, 7.0" integration line with the trimmed-matrix truth — PHP 8.2/8.3 × WP 6.4/6.7/7.0, 5 MS=false lanes + 1 MS=true representative (PHP 8.3 × WP 7.0) + 1 MariaDB LTS lane = 7 integration lanes total. Note this is a trim from 10 lanes (verified against phpunit.yml).
    2. Record (do NOT finalize) the current E2E baseline count for Task 3's reference: run `npx playwright test --config tests/e2e/playwright.config.ts --list 2>/dev/null | tail -1` (or the project's documented list command) and note the pre-wiring number in the task notes. If the local E2E env is not runnable, say so explicitly and defer the count entirely to Task 3 — do NOT guess (CLAUDE.md confabulation rule).
    3. Do NOT hardcode any other volatile count; link to this file from prose where applicable.
  </action>
  <verify>
    <automated>grep -q "8.2" docs/current-metrics.md && grep -q "6.4" docs/current-metrics.md && ! grep -q "PHP 8.0, 8.1, 8.3" docs/current-metrics.md && echo "current-metrics CI snapshot updated"</automated>
  </verify>
  <done>CI Matrix Snapshot reflects PHP 8.2/8.3 × WP 6.4/6.7/7.0 and the 7-lane trimmed integration matrix; no stale PHP 8.0/8.1 integration line remains. E2E count deferred to Task 3 (baseline noted).</done>
</task>

<task type="auto">
  <name>Task 2: Trim integration matrix + cache WP test library (CIS-01, CIS-02)</name>
  <files>.github/workflows/phpunit.yml</files>
  <action>
    CIS-01 — Trim the integration matrix (lines 163–213) from 10 to 7 lanes by removing the three redundant MS=true lanes (3, 5, 7), keeping lane 9 (PHP 8.3 × WP 7.0 × MS=true) as the single multisite representative. Result: 5 MS=false (lanes 1,2,4,6,8 — unchanged) + 1 MS=true + 1 MariaDB LTS = 7 lanes. Do NOT add an 8.3×6.4 lane to "complete" the grid — the trim only removes. Add a comment block above the matrix documenting EACH dropped lane and the coverage it represented and why it is redundant (CIS-01 requires explicit dropped-coverage documentation), e.g.:
      # Dropped 2026-06-19 (Phase 13.1 CIS-01): MS=true on PHP8.2×WP6.7, PHP8.3×WP6.7, PHP8.2×WP7.0.
      # Multisite-specific tests (MultisiteTest.php @group multisite + scattered MS guards, ~20 tests,
      # all self-skipping on MS=false) are exercised by the retained PHP8.3×WP7.0 MS=true lane.

    CIS-02 — Add an actions/cache@v5 step (mirror the existing Composer cache ~lines 88–95) keyed per WP version, caching ONLY:
        /tmp/wordpress-tests-lib/includes
        /tmp/wordpress-tests-lib/data
        /tmp/wordpress
      key: wp-tests-${{ matrix.wp }}-${{ hashFiles('bin/install-wp-tests.sh') }}
      restore-keys: wp-tests-${{ matrix.wp }}-
    In the "Install WordPress test suite" step, BEFORE calling bash bin/install-wp-tests.sh, run `rm -f /tmp/wordpress-tests-lib/wp-tests-config.php` so DB credentials are always regenerated fresh (RESEARCH Pitfall 2). Do NOT cache wp-tests-config.php. The script's `[ -d $WP_TESTS_DIR ]` guard skips the SVN export on a cache hit; subversion install can stay (cheap).

    Make no other changes to the workflow (unit/static matrices are already fast — leave alone per CONTEXT).
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/phpunit.yml')); print('phpunit.yml parses')" && grep -c "actions/cache" .github/workflows/phpunit.yml && grep -q "rm -f /tmp/wordpress-tests-lib/wp-tests-config.php" .github/workflows/phpunit.yml && echo "matrix+cache OK"</automated>
  </verify>
  <done>Integration matrix is 8 lanes with a dropped-coverage comment naming the three removed MS=true lanes; an actions/cache step caches the WP test lib (includes/data + /tmp/wordpress) keyed on matrix.wp; wp-tests-config.php is deleted before install so credentials regenerate; YAML parses. Final proof: CI green on the PR with a "Cache restored" log line on a second run.</done>
</task>

<task type="auto">
  <name>Task 3: Assign access-grant.spec.ts to a group + rebalance E2E shards + finalize E2E count (CIS-03)</name>
  <files>.github/workflows/e2e.yml, docs/current-metrics.md</files>
  <action>
    CIS-03 has two parts:
    1. CLOSE THE ORPHAN GAP (required): add `tests/e2e/specs/access-grant.spec.ts` to a group's test-command. Use group 3 (challenge-lockout-surfaces) — semantically an access/surface concern and NOT the 5m19s outlier (group 2). Append it to group 3's `npm run test:e2e -- ...` line. (Depends on Plan 01 having updated the spec to use the `<select>` picker + admin-role target — wiring an un-updated spec would fail.)
    2. REBALANCE (lower critical path): group 2 (challenge-2fa-ui) is the 5m19s outlier vs ~4.3m. Move the 2FA-heavy subset out of group 2 to lower its time — RESEARCH preference order:
       - Preferred: move `challenge.spec.ts --grep 'CHAL-09|CHAL-10|CHAL-11'` from group 2 into group 1 (the lightest), leaving group 2 with CHAL-06-08 + cookie/gate-ui/keyboard. Keep group totals balanced; do not push group 1 above the current outlier.
       - Acceptable fallback if balancing is awkward: simply add access-grant.spec.ts to group 3 and leave group 2 as-is (minimal change, still closes the orphan gap) — but prefer the rebalance since CIS-03 explicitly asks to lower the critical-path shard time.
    3. Add a brief comment noting the wp-env spin-up floor (~60s/job) and that further gains hit diminishing returns (CONTEXT/CIS-03 documentation note).
    Do not change shard COUNT unless rebalancing within 4 groups proves insufficient (adding a 5th group adds a fixed ~60s spin-up — only worth it if it meaningfully lowers the critical path; if you add one, update the E2E group count in docs/current-metrics.md too).
    4. FINALIZE E2E COUNT (moved here from Task 1 — the count is only correct once access-grant.spec.ts is wired in): re-derive the authoritative E2E test/spec total with `npx playwright test --config tests/e2e/playwright.config.ts --list` and update docs/current-metrics.md (line ~44, was 61) to the verified value with the query date. If the local E2E env is not runnable, state that explicitly and update the count from the CI run that executes the newly-wired spec — do NOT guess (CLAUDE.md confabulation rule).
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/e2e.yml')); print('e2e.yml parses')" && [ "$(grep -c 'access-grant.spec.ts' .github/workflows/e2e.yml)" = "1" ] && echo "access-grant wired into exactly one group"</automated>
  </verify>
  <done>access-grant.spec.ts appears in EXACTLY ONE group's test-command (group 3 — verified by grep -c == 1); if the preferred rebalance was taken, the CHAL-09|10|11 subset no longer appears in group 2's command; group 2's 2FA-heavy subset is rebalanced to lower its critical-path time (or the documented minimal-change fallback is applied with rationale); wp-env spin-up floor noted in a comment; YAML parses; docs/current-metrics.md E2E count finalized with a query date. Final proof: CI green with ACCESS-01/ACCESS-02 visible in the run logs.</done>
</task>

</tasks>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/phpunit.yml')); yaml.safe_load(open('.github/workflows/e2e.yml'))"` — both workflows parse.
- phpunit.yml: 8 integration lanes; dropped-coverage comment present; actions/cache for WP test lib; wp-tests-config.php deleted before install.
- e2e.yml: access-grant.spec.ts assigned to group 3; group 2 rebalanced; spin-up floor comment.
- docs/current-metrics.md: CI snapshot corrected in Task 1 (PHP 8.2/8.3, WP 6.4/6.7/7.0, 7 lanes); E2E count finalized in Task 3 from `playwright --list` after the spec is wired in.
- CI-run gate (the real validation, config-only carve-out): on the PR, all 8 integration lanes green; a second run shows "Cache restored" for the WP test lib; the E2E run executes ACCESS-01/ACCESS-02; record before/after integration runner-minutes and group-2 critical-path time in the PR description.
</verification>

<success_criteria>
- CIS-01: integration matrix trimmed to a documented 7-lane representative subset (5 MS=false lanes retained + one MS=true representative + MariaDB LTS); dropped coverage explicitly recorded in the workflow.
- CIS-02: WP test library cached across lanes (skip download on hit); wp-tests-config.php always regenerated so tests pass on cache hit.
- CIS-03: access-grant.spec.ts runs in CI (orphan gap closed); E2E critical-path shard time lowered by rebalancing group 2; wp-env spin-up floor noted.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-02-SUMMARY.md`.
Record the realized before/after CI timing (integration runner-minutes; group-2 critical path) from the PR's Actions timing tab, and confirm the "Cache restored" log line appeared on a second run.
</output>
