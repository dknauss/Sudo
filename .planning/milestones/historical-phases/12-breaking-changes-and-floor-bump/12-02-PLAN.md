---
phase: 12-breaking-changes-and-floor-bump
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - wp-sudo.php
  - readme.txt
  - composer.json
  - composer.lock
  - .github/workflows/phpunit.yml
  - .github/workflows/plugin-check.yml
  - .github/workflows/compat-wordpress-minors.yml
autonomous: true
requirements: [BRK-04, BRK-05, BRK-06]
must_haves:
  truths:
    - "The plugin header, readme.txt, and composer.json all declare Requires at least: 6.4 and Requires PHP: 8.2"
    - "composer.json config.platform.php is 8.2.0 (no longer 8.1.99) and composer.lock is regenerated against it"
    - "The CI support-floor integration lane runs WP 6.4 (not 6.2)"
    - "The CI PHP matrix no longer runs the 8.0 lane; the php80-tests special-case conditionals and the .github/php80-tests/ directory are gone"
    - "No function_exists('wp_get_admin_notice') shim exists anywhere in production code (BRK-06 verify-then-close)"
  artifacts:
    - path: "wp-sudo.php"
      provides: "plugin header floor declarations 6.4 / 8.2"
      contains: "Requires at least: 6.4"
    - path: "readme.txt"
      provides: "readme floor declarations 6.4 / 8.2"
      contains: "Requires at least: 6.4"
    - path: "composer.json"
      provides: "require.php >=8.2 and config.platform.php 8.2.0"
      contains: "8.2"
    - path: ".github/workflows/phpunit.yml"
      provides: "support-floor lane WP 6.4 / PHP 8.2; no 8.0 lane or php80-tests conditionals"
  key_links:
    - from: "composer.json require.php >=8.2"
      to: "composer.json config.platform.php 8.2.0"
      via: "both bumped together then composer update --lock"
      pattern: "8.2"
    - from: ".github/workflows/phpunit.yml PHP matrix"
      to: ".github/php80-tests/ directory"
      via: "8.0 lane + conditionals removed when directory removed"
      pattern: "php80-tests"
---

<objective>
Raise the declared platform floor to WordPress 6.4 / PHP 8.2 across every declaration site, drop the now-dead PHP 8.0 CI infrastructure, and formally close BRK-06 (the claimed `wp_get_admin_notice` shim is already absent — verify and record).

Purpose: Honestly declare v4.0.0's minimum requirements (WP 6.4 drops the `wp_get_admin_notice` compat concern; PHP 8.2 is the security-supported floor) and remove the special-case PHP 8.0 CI vendor tree that becomes dead weight.

Output: Updated header/readme/composer floor declarations, a regenerated `composer.lock`, trimmed CI workflows, the `.github/php80-tests/` directory removed, and a recorded BRK-06 obsolescence finding.

ASSUMPTION (state in SUMMARY): this plan changes only the WP/PHP FLOOR declarations. It does NOT bump `WP_SUDO_VERSION` (currently 3.4.0) — the release-version bump is a separate release/tagging step. Floors and release version are independent.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-breaking-changes-and-floor-bump/12-RESEARCH.md
@.planning/phases/12-breaking-changes-and-floor-bump/12-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Floor declaration sites — verified against live files 2026-06-16. -->

WordPress 6.2 -> 6.4:
  wp-sudo.php line 7:                       `Requires at least: 6.2`  -> 6.4
  readme.txt line 5:                        `Requires at least: 6.2`  -> 6.4
  .github/workflows/phpunit.yml line 177:   `wp: '6.2'` (floor lane)  -> 6.4
  .github/workflows/plugin-check.yml ~103:  comment `the only newer-than-6.2` -> update to 6.4 (note remaining newer-than-floor calls are WP 7.0+ wp_get_connectors, still function_exists-guarded)
  .github/workflows/compat-wordpress-minors.yml line 20: `wp: '6.3'` lane -> drop (below new floor); lowest lane becomes 6.4 (Claude's discretion — note in SUMMARY)

PHP 8.0 -> 8.2:
  wp-sudo.php line 8:                        `Requires PHP: 8.0`  -> 8.2
  readme.txt line 7:                         `Requires PHP: 8.0`  -> 8.2
  composer.json line 7:                      `"php": ">=8.0"`     -> `">=8.2"`
  composer.json line 38 (config.platform):   `"php": "8.1.99"`    -> `"php": "8.2.0"`
  .github/workflows/phpunit.yml line 71:     `php: ['8.0', '8.1', '8.2', '8.3', '8.4']` -> `['8.2', '8.3', '8.4']` (drop 8.0 and 8.1)
  .github/workflows/phpunit.yml line 176:    floor lane `php: '8.0'` -> `'8.2'` (paired with wp 6.4 above)
  .github/workflows/phpunit.yml conditionals: lines ~96-97, ~112-113, ~271-272, ~287-289 — `if [ "${{ matrix.php }}" = "8.0" ]` special-cases that install/run from .github/php80-tests/. REMOVE these conditional blocks entirely.
  .github/php80-tests/ directory (composer.json, composer.lock, vendor/): REMOVE entirely.

BRK-06: `grep -rn "wp_get_admin_notice" includes/ wp-sudo.php` returns ZERO matches (verified). The only function_exists guards in class-admin.php are legitimate `graphql` runtime checks. No code change — verify + record.

Line numbers drift; match on the literal strings above, not absolute numbers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump WP 6.4 / PHP 8.2 floors in plugin + composer (BRK-04 + BRK-05)</name>
  <files>wp-sudo.php, readme.txt, composer.json, composer.lock</files>
  <action>
Edit the declaration sites (config/metadata — no TDD; verify by grep):
  - wp-sudo.php: header line `Requires at least: 6.2` -> `6.4`; `Requires PHP: 8.0` -> `8.2`. Do NOT change the `Version:` line or `WP_SUDO_VERSION` (stays 3.4.0).
  - readme.txt: `Requires at least: 6.2` -> `6.4`; `Requires PHP: 8.0` -> `8.2`. Do NOT change `Stable tag` (stays 3.4.0). Leave the `= 4.0.0 =` Upgrade Notice to plan 12-03.
  - composer.json: `"php": ">=8.0"` -> `">=8.2"` in require; `config.platform.php` `"8.1.99"` -> `"8.2.0"`.
  - Regenerate the lock against the new platform: run `composer update --lock` (sandbox may block network — if it fails with a network/sandbox error, retry with the sandbox disabled). Commit the updated composer.lock even if only the content-hash changes. If the lock genuinely does not change, note that in the SUMMARY.
Verify with the grep commands below. There are no _doing_it_wrong / function_exists concerns here — pure declaration edits.
PRE-COMMIT REVIEWER: composer.json/composer.lock and wp-sudo.php are code (not docs-only) — this commit requires reviewer approval. Spawn subagent_type=reviewer (factual description; it runs tests+lint and writes `reviewer-approved`), commit within 1800s, disable sandbox for the git commit command (hook re-runs PHPStan). readme.txt alone would be docs, but the commit includes .php/.json so the reviewer is required.
Commit: `chore(12-02): raise minimum floors to WordPress 6.4 and PHP 8.2`
  </action>
  <verify>
    <automated>grep -q "Requires at least: 6.4" wp-sudo.php && grep -q "Requires PHP:      8.2" wp-sudo.php && echo HEADER_OK</automated>
    <automated>grep -q "Requires at least: 6.4" readme.txt && grep -q "Requires PHP:      8.2" readme.txt && echo README_OK</automated>
    <automated>grep -q '">=8.2"' composer.json && grep -q '"8.2.0"' composer.json && echo COMPOSER_OK</automated>
    <automated>composer test:unit</automated>
  </verify>
  <done>
- `grep "6.2" wp-sudo.php readme.txt` shows no `Requires`/floor 6.2; both declare 6.4.
- `grep "8.0" wp-sudo.php readme.txt composer.json` shows no `Requires PHP`/require/platform 8.0; PHP floor is 8.2 and platform is 8.2.0.
- composer.lock regenerated (or confirmed unchanged) against platform 8.2.0.
- `composer test:unit` still green. Reviewer approved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Trim CI to the 6.4/8.2 floor + remove php80-tests; close BRK-06</name>
  <files>.github/workflows/phpunit.yml, .github/workflows/plugin-check.yml, .github/workflows/compat-wordpress-minors.yml</files>
  <action>
CI edits (config; verify by grep — this is the dependent task because the lock/floor must land first):
  - .github/workflows/phpunit.yml:
      * Line 71 PHP matrix: `['8.0', '8.1', '8.2', '8.3', '8.4']` -> `['8.2', '8.3', '8.4']` (drop 8.0 and 8.1 per RESEARCH).
      * Floor integration lane (lines ~176-177): `php: '8.0'` -> `'8.2'`, `wp: '6.2'` -> `'6.4'`.
      * REMOVE the four `if [ "${{ matrix.php }}" = "8.0" ]` special-case conditional blocks (~lines 96-97, 112-113, 271-272, 287-289) and the associated `WP_SUDO_VENDOR_AUTOLOAD` / `WP_SUDO_PHPUNIT_POLYFILLS_PATH` env wiring that points at `.github/php80-tests/`. After removal, the matrix runs the standard install/test path for every remaining lane.
  - Remove the `.github/php80-tests/` directory entirely (`git rm -r .github/php80-tests`).
  - .github/workflows/plugin-check.yml ~line 103: update the comment `the only newer-than-6.2 calls` -> reference 6.4, and note the only remaining newer-than-floor calls are WP 7.0+ (`wp_get_connectors`, still function_exists-guarded). Comment-only edit.
  - .github/workflows/compat-wordpress-minors.yml: drop the `wp: '6.3'` lane (line ~20) — it is below the new floor (Claude's discretion per RESEARCH; record the decision in SUMMARY). Lowest lane is now 6.4.
BRK-06 (verify-then-close, no code change):
  - Run `grep -rn "wp_get_admin_notice" includes/ wp-sudo.php` — confirm ZERO matches.
  - Update `.planning/REQUIREMENTS.md`: mark BRK-06 satisfied/obsolete (the shim premise was stale; no shim existed). Note the finding for the SUMMARY.
Validate the YAML stays well-formed (e.g. `grep` the new values; if a YAML linter is available, run it). Then `composer test:unit` to confirm nothing in the suite broke.
PRE-COMMIT REVIEWER: this commit stages workflow YAML + a directory removal (scripts/config — not docs-only) and a REQUIREMENTS.md edit. Workflow/script files require reviewer approval. Spawn subagent_type=reviewer (factual; runs tests+lint, writes `reviewer-approved`); commit within 1800s; disable sandbox for the git commit.
Commit: `ci(12-02): pin CI floor to WP 6.4 / PHP 8.2 and drop php80-tests`
  </action>
  <verify>
    <automated>grep -q "wp: '6.4'" .github/workflows/phpunit.yml && echo FLOOR_LANE_OK</automated>
    <automated>! grep -q "php: '8.0'" .github/workflows/phpunit.yml && ! grep -rq "php80-tests" .github/workflows/phpunit.yml && echo NO_PHP80</automated>
    <automated>test ! -d .github/php80-tests && echo DIR_GONE</automated>
    <automated>test -z "$(grep -rn 'wp_get_admin_notice' includes/ wp-sudo.php)" && echo BRK06_OBSOLETE</automated>
  </verify>
  <done>
- phpunit.yml PHP matrix is `['8.2', '8.3', '8.4']`; floor lane is WP 6.4 / PHP 8.2; no `8.0` lane; no `php80-tests` references or conditionals remain.
- `.github/php80-tests/` directory removed.
- plugin-check.yml comment references 6.4; compat-wordpress-minors.yml lowest lane is 6.4 (6.3 dropped).
- `grep -rn "wp_get_admin_notice" includes/ wp-sudo.php` is empty; REQUIREMENTS.md marks BRK-06 satisfied/obsolete.
- `composer test:unit` green. Reviewer approved.
  </done>
</task>

</tasks>

<verification>
- All floor declarations read 6.4 / 8.2; no 6.2 or 8.0 floor strings remain in the declaration sites.
- composer.lock regenerated (or confirmed unchanged) against platform 8.2.0; `composer test:unit` and `composer analyse` clean.
- CI: PHP matrix drops 8.0/8.1; floor lane is 6.4/8.2; php80-tests directory and conditionals gone.
- BRK-06: grep empty; REQUIREMENTS.md updated to satisfied/obsolete.
</verification>

<success_criteria>
- BRK-04: WordPress 6.4 declared in header + readme + CI support-floor lane.
- BRK-05: PHP 8.2 declared in header + readme + composer require + config.platform.php (8.2.0).
- BRK-06: confirmed no `wp_get_admin_notice` shim in production code; recorded satisfied/obsolete in SUMMARY and REQUIREMENTS.md.
- WP_SUDO_VERSION untouched (floors are independent of release version).
- Every code/CI commit reviewer-approved.
</success_criteria>

<output>
After completion, create `.planning/phases/12-breaking-changes-and-floor-bump/12-02-SUMMARY.md`.
Record: every floor site changed (old -> new); whether composer.lock changed; the compat-wordpress-minors 6.3-lane decision; the BRK-06 obsolescence finding and the REQUIREMENTS.md edit; and the assumption that WP_SUDO_VERSION was NOT bumped.
</output>
