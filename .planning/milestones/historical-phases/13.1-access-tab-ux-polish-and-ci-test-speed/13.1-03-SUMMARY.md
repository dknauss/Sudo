---
phase: 13.1-access-tab-ux-polish-and-ci-test-speed
plan: 03
subsystem: ui
tags: [wordpress-admin, access-tab, searchable-picker, metrics, screenshots]
requires:
  - phase: 13.1-01
    provides: Access-tab administrator select picker and plain-English capability labels
  - phase: 13.1-02
    provides: CI-speed metrics and canonical docs baseline
provides:
  - Explicit searchable administrator grant picker on the Access tab
  - Regression coverage for searchable picker markup and unchanged numeric user_id submission
  - Canonical live metrics aligned with repository counts
  - Refreshed Access tab public screenshot showing the searchable picker
affects: [phase-13.1, wordpress-org-readiness, access-control-docs]
tech-stack:
  added: []
  patterns:
    - Dependency-free progressive enhancement for native WordPress admin controls
    - Canonical metrics updated only from live verifier output
key-files:
  created:
    - .planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-03-SUMMARY.md
  modified:
    - includes/class-admin.php
    - admin/js/wp-sudo-admin.js
    - tests/Unit/AdminTest.php
    - docs/current-metrics.md
    - .wordpress-org/screenshot-6.png
key-decisions:
  - "Search remains progressive enhancement over the existing native select; option values stay numeric user IDs."
  - "The screenshot asset filename stayed stable as .wordpress-org/screenshot-6.png to avoid README/readme churn."
  - "Only screenshot 6 was retained from the capture run; unrelated regenerated listing assets were reverted to keep the commit atomic."
patterns-established:
  - "Searchable admin picker: input#wp-sudo-grant-user-search filters select#wp-sudo-grant-user without changing the AJAX payload contract."
  - "Screenshot refreshes should use the project capture workflow and then revert unrelated asset churn if a plan owns only one screenshot."
requirements-completed: [ACC-01, ACC-03, CIS-01, CIS-02, CIS-03]
duration: 1h 5min
completed: 2026-06-28
---

# Phase 13.1-03: Gap Closure Summary

**Searchable Access-tab administrator picker with live repository metrics and refreshed public Access screenshot**

## Performance

- **Duration:** ~1h 5min
- **Started:** 2026-06-28T15:20:00Z
- **Completed:** 2026-06-28T16:25:00Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- Added an explicit searchable grant-user control to Settings → Sudo → Access while preserving the administrator-scoped native select and exact numeric `user_id` AJAX payload.
- Extended `AdminTest::test_render_settings_page_renders_access_tab` so ACC-01 fails if the searchable picker markup/metadata disappears.
- Updated `docs/current-metrics.md` from live counts and verified it with `composer verify:metrics`.
- Refreshed `.wordpress-org/screenshot-6.png` through the project screenshot capture workflow and visually confirmed it shows the Access tab search field.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add explicit searchable administrator picker behavior for ACC-01** — `711dfaf` (`feat(13.1-03): add searchable access grant picker`)
2. **Task 2: Refresh canonical metrics from live counts and pass verify:metrics** — `73fe7cb` (`docs(13.1-03): refresh canonical repository metrics`)
3. **Task 3: Refresh or verify Access tab screenshot documentation** — `d5fd0f6` (`docs(13.1-03): refresh access tab screenshot`)

## Files Created/Modified

- `includes/class-admin.php` — Adds `input#wp-sudo-grant-user-search`, `aria-controls="wp-sudo-grant-user"`, helper text, `data-searchable`, and per-option `data-search-text` while preserving numeric option values.
- `admin/js/wp-sudo-admin.js` — Adds dependency-free filtering/selection behavior for the grant picker and keeps the grant action payload on `{ user_id: userId, cap: ... }`.
- `tests/Unit/AdminTest.php` — Adds rendered-output assertions for the explicit search affordance while retaining existing select/value/capability label checks.
- `docs/current-metrics.md` — Updates canonical counts: 888 unit tests, 2624 assertions, 15,995 production PHP lines, 32,481 tests PHP lines, 48,476 production+tests PHP lines, 48,748 total repo PHP lines, 2.03:1 test-to-production ratio.
- `.wordpress-org/screenshot-6.png` — Refreshed Access tab screenshot showing the searchable administrator picker.

## Decisions Made

- Kept the server-side grant/revoke authorization contract unchanged; the searchable field is convenience UI only.
- Used a native search input plus native select instead of a custom listbox to minimize accessibility and maintenance risk.
- Kept `.wordpress-org/screenshot-6.png` as the stable screenshot filename because README/readme references were already accurate.
- Reverted unrelated screenshots generated by the full capture workflow so Task 3 remained atomic to the Access tab asset.

## Deviations from Plan

### Auto-fixed Issues

**1. Screenshot capture environment needed local setup**
- **Found during:** Task 3 (screenshot refresh)
- **Issue:** Initial `npm run env:start` failed because another wp-env stack occupied test port 8890, and Playwright Chromium was not installed locally.
- **Fix:** Started a dev-only wp-env config on port 8889, installed the local Playwright Chromium browser with `npx playwright install chromium`, then ran the existing screenshot capture workflow with `WP_ENV_CONFIG_PATH=.tmp/wp-env-dev-only.json WP_SUDO_CAPTURE=1`.
- **Files modified:** `.wordpress-org/screenshot-6.png` only; temporary `.tmp/wp-env-dev-only.json` remained untracked and is excluded from metrics.
- **Verification:** Capture workflow passed and visual inspection confirmed the refreshed screenshot shows the Access tab search field.
- **Committed in:** `d5fd0f6`

---

**Total deviations:** 1 environment/setup deviation.
**Impact on plan:** No scope creep; the deviation was necessary to satisfy the browser-capable screenshot requirement in the current environment.

## Issues Encountered

- Subagent executor completed Tasks 1–2 and paused at a browser-capability checkpoint. The main orchestrator had in-app/browser-capable tooling and completed Task 3 directly.
- `npm run env:start` initially failed on test port 8890 because an unrelated wp-env stack was already bound there; a dev-only config avoided disturbing that stack.
- The full screenshot capture script regenerated several listing screenshots; only `.wordpress-org/screenshot-6.png` was retained, and unrelated screenshot asset changes were reverted.

## Verification

- RED/TDD: `./vendor/bin/phpunit tests/Unit/AdminTest.php --filter test_render_settings_page_renders_access_tab` failed before production markup because `wp-sudo-grant-user-search` was missing.
- Targeted render test after implementation: `OK (1 test, 19 assertions)`.
- Static search wiring check: `searchable picker static checks OK`.
- Unit suite during reviewer/pre-commit: `OK (888 tests, 2624 assertions)`.
- PHPStan/Psalm analysis: `composer analyse` passed with no errors.
- Lint: reviewer/pre-commit PHPCS passed.
- Metrics: `composer verify:metrics` → `Metrics verified: docs/current-metrics.md is in sync.`
- Screenshot capture: `WP_ENV_CONFIG_PATH=.tmp/wp-env-dev-only.json WP_SUDO_CAPTURE=1 npx playwright test --config tests/e2e/playwright.config.ts --project=chromium tests/e2e/specs/capture-screenshots.spec.ts` → `1 passed`.
- Screenshot asset checks: `.wordpress-org/screenshot-6.png` is valid PNG data; `readme.md`, `README.md`, and `readme.txt` still reference/caption screenshot 6 accurately.
- Browser-capable visual verification: inspected `/Users/danknauss/Developer/GitHub/wp-sudo/.wordpress-org/screenshot-6.png`; it shows Settings → Sudo → Access with the visible search field placeholder “Search administrators by name or username,” helper text, and native user select.

## User Setup Required

None.

## Next Phase Readiness

Phase 13.1 gaps are closed: ACC-01 now has explicit searchable-picker behavior and tests, canonical metrics verify cleanly, and the public Access tab screenshot is current. Ready for phase-level verification rerun.

---
*Phase: 13.1-access-tab-ux-polish-and-ci-test-speed*
*Completed: 2026-06-28*
