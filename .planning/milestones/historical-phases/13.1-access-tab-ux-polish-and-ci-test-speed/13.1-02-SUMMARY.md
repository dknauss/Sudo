---
phase: 13.1-access-tab-ux-polish-and-ci-test-speed
plan: "02"
subsystem: ci
tags: [github-actions, phpunit, playwright, e2e, metrics]

requires:
  - phase: 13.1-access-tab-ux-polish-and-ci-test-speed/13.1-01
    provides: "Admin-ready access-grant E2E spec markup from Plan 01"
provides:
  - "Documented 7-lane representative PHPUnit integration matrix"
  - "Verified WordPress test-library cache with fresh wp-tests-config.php regeneration"
  - "Verified access-grant.spec.ts CI wiring and E2E shard rebalance"
  - "Corrected canonical CI/E2E metrics snapshot"
affects: [ci, current-metrics, phase-13.1]

tech-stack:
  added: []
  patterns: ["Verify-first reconciliation when plan requirements are already present on main"]

key-files:
  created:
    - ".planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-02-SUMMARY.md"
  modified:
    - ".github/workflows/phpunit.yml"
    - "docs/current-metrics.md"

key-decisions:
  - "Do not duplicate existing CI-speed implementation already present on main; verify it, document the missing GSD trail, and only add missing documentation/metrics corrections."
  - "Retain four E2E groups because wp-env startup is a fixed ~60s/job floor; rebalance within existing groups instead of adding a fifth group."

patterns-established:
  - "CI speed work should document dropped matrix coverage in the workflow next to the matrix."
  - "E2E test counts are finalized only after CI group wiring is verified."

requirements-completed: [CIS-01, CIS-02, CIS-03]

duration: 4min
completed: 2026-06-28
---

# Phase 13.1 Plan 02: CI Test-Speed Summary

**Verified and GSD-documented the existing CI-speed implementation: 7 integration lanes, cached WP test library, rebalanced E2E groups, and a corrected 64-test E2E metric.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-28T05:55:55Z
- **Completed:** 2026-06-28T06:00:06Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- Corrected the canonical CI Matrix Snapshot to state the 10→7 integration-lane trim.
- Verified `.github/workflows/phpunit.yml` already has the representative 7-lane matrix, `actions/cache@v5` for `/tmp/wordpress-tests-lib/{includes,data}` and `/tmp/wordpress`, and `rm -f /tmp/wordpress-tests-lib/wp-tests-config.php` before install.
- Expanded the workflow dropped-coverage comment so each removed MS=true lane is named with the retained coverage.
- Verified `.github/workflows/e2e.yml` already wires `access-grant.spec.ts` exactly once into group 3 and moves `CHAL-09|CHAL-10|CHAL-11` out of the previous group-2 outlier.
- Re-ran the authoritative Playwright list command and updated `docs/current-metrics.md` from 63 to 64 E2E tests (`64 tests in 15 files`, verified 2026-06-28).

## Task Commits

Each task was committed atomically:

1. **Task 1: Correct docs/current-metrics.md CI Matrix Snapshot FIRST** - `2b91bf9` (docs)
2. **Task 2: Trim integration matrix + cache WP test library** - `18d8a20` (ci)
3. **Task 3: Assign access-grant.spec.ts to a group + rebalance E2E shards + finalize E2E count** - `24b9879` (docs)

**Plan metadata:** pending final docs commit.

## Files Created/Modified

- `.github/workflows/phpunit.yml` - Expanded dropped-lane coverage documentation for the existing 7-lane integration matrix.
- `docs/current-metrics.md` - Corrected CI Matrix Snapshot and finalized the E2E test count at 64 with query date.
- `.planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-02-SUMMARY.md` - Execution summary and verification record.

## Decisions Made

- Existing main-branch implementation was treated as authoritative after verification; this run avoided duplicating or reshuffling CI configuration already present.
- E2E remains at four groups because the workflow already documents the fixed wp-env startup floor and the preferred rebalance is present.

## Deviations from Plan

None - plan requirements were satisfied by verified current implementation plus the missing GSD documentation/metrics updates.

## Issues Encountered

- The repository had stale phase state indicating Phase 13.1 was already merged/released while GSD summary tracking for Plan 02 was missing. Resolved by verifying live files and creating the required summary/state trail.
- CI timing artifacts were available from merged PR #90 rather than a fresh PR in this run. Verified post-change run `27863473547` (PHPUnit) and `27863473541` (E2E) from 2026-06-20:
  - Integration after-state: 7 integration lanes, all green, total lane runtime about 6m39s; removed three former MS=true lanes versus the planned 10-lane baseline.
  - WP test-library cache: logs show `Cache hit for: wp-tests-...`, `Cache restored from key: wp-tests-...`, followed by `rm -f /tmp/wordpress-tests-lib/wp-tests-config.php`.
  - E2E after-state: group 2 `challenge-2fa-ui` completed in 3m57s; context baseline was 5m19s.

## Verification

- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/phpunit.yml')); yaml.safe_load(open('.github/workflows/e2e.yml'))"` — passed.
- PHPUnit matrix inspection — 7 lanes, exactly one multisite lane, MariaDB LTS retained.
- `grep -q "rm -f /tmp/wordpress-tests-lib/wp-tests-config.php" .github/workflows/phpunit.yml` — passed.
- `grep -c 'access-grant.spec.ts' .github/workflows/e2e.yml` — exactly `1`.
- `npx playwright test --config tests/e2e/playwright.config.ts --list` — `Total: 64 tests in 15 files`.
- Pre-commit hook for `18d8a20` ran `composer test:unit` (`888 tests`, `2616 assertions`) and `composer lint`.

## Auth Gates

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 13.1 is now fully summarized for GSD tracking. CI-speed requirements CIS-01, CIS-02, and CIS-03 are ready to mark complete.

## Self-Check: PASSED

- Found key files: `.github/workflows/phpunit.yml`, `.github/workflows/e2e.yml`, `docs/current-metrics.md`, and this summary.
- Found task commits: `2b91bf9`, `18d8a20`, `24b9879`.

---
*Phase: 13.1-access-tab-ux-polish-and-ci-test-speed*
*Completed: 2026-06-28*
