---
phase: 14-wordpress-org-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - readme.txt
  - wp-sudo.php
autonomous: true
requirements: [ORG-01, ORG-05]
must_haves:
  truths:
    - "readme.txt short description is <= 150 characters"
    - "readme.txt Description contains no stale version-specific What's-new subsection"
    - "Stable tag, License, and plugin-header fields are internally consistent across wp-sudo.php, readme.txt, and composer.json"
    - "Plugin Check CI passes (no stable_tag_mismatch, no plugin_name_mismatch)"
  artifacts:
    - path: "readme.txt"
      provides: "WordPress.org-compliant readme (short desc <=150, no stale subsection, consistent header)"
      contains: "Stable tag:"
    - path: "wp-sudo.php"
      provides: "Compliant plugin header with reconciled Plugin Name"
      contains: "Plugin Name:"
  key_links:
    - from: "wp-sudo.php"
      to: "readme.txt"
      via: "Plugin Name / Stable tag / Version agreement"
      pattern: "Plugin Name: *Sudo Security"
    - from: "readme.txt"
      to: "composer.json"
      via: "License field agreement"
      pattern: "GPL-2.0-or-later"
---

<objective>
Make readme.txt pass the WordPress.org readme validator and bring the plugin
header / license / stable-tag into WordPress.org-compliant internal agreement
(ORG-01, ORG-05).

Purpose: The short description currently exceeds the 150-char hard limit (the one
outright validator failure per RESEARCH), the Description carries a stale
"What's new in 3.2.0?" subsection, and the plugin header `Plugin Name: WP Sudo`
diverges from readme `=== Sudo ===` (a Plugin Check `plugin_name_mismatch` risk).
This plan fixes those and asserts the version-sync / license consistency that
RESEARCH found already clean at 4.0.0.

Output: A corrected readme.txt and reconciled wp-sudo.php header. Plugin Check CI
(the automated gate) validates the result on PR.
</objective>

<reconciliation_note>
⚠️ Codex has changed these files out-of-band on this branch — RE-VERIFY live state before editing; reconcile, do not blindly re-apply the research drafts:
- `4e40635` already TRIMMED the short description (now ~124 chars, passes the 150 limit). Task 1 step 1 is therefore likely **verify-only** — run `awk 'NR==12' readme.txt | wc -c` first; only edit if ≥151. Do NOT overwrite Codex's passing text with the research draft just to match the draft. The stale `= What's new in 3.2.0? =` subsection removal (Task 1 step 2) is still outstanding — verify and do it.
- `a1fb4d7` set the header to `Plugin Name: WP Sudo` (a quick CI-unblock revert of an invalid "Sudo"). **The USER DECISION is `Plugin Name: Sudo Security`** ("Sudo" alone fails Plugin Check's ≥5-latin-letter rule; "WordPress" is disallowed). This plan sets the header to "Sudo Security" and aligns the readme title to match (avoids `plugin_name_mismatch`). UI/brand strings stay "Sudo" (that's plan 14-02, not the readme title or header).
</reconciliation_note>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-wordpress-org-readiness/14-CONTEXT.md
@.planning/phases/14-wordpress-org-readiness/14-RESEARCH.md
@.planning/phases/14-wordpress-org-readiness/14-VALIDATION.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Canonical sources (CLAUDE.md — never hardcode versions/release state from memory)
@docs/release-status.md

# Files this plan edits
@readme.txt
@wp-sudo.php
@composer.json

<verified_research_findings>
<!-- Verified live 2026-06-19 in 14-RESEARCH.md, BUT reconcile against Codex's later edits (see reconciliation_note) — some are now stale. -->
- Short description was 204 chars at research time, but Codex `4e40635` already trimmed it (now ~124 chars, PASSES the 150 limit). RE-VERIFY current length first; this is likely verify-only now. Do NOT overwrite Codex's passing text with the older research draft below unless the live line is actually ≥151.
- (Stale fallback only) Draft <=150 replacement from RESEARCH if the live short description is somehow still too long:
  "WordPress security plugins guard the door. Sudo governs what happens inside — gating dangerous admin actions behind reauthentication."
  Verify final length with `awk 'NR==12' readme.txt | wc -c` < 151. Preserve the guard-door / govern-inside value prop; do not over-trim.
- Description contains a stale `= What's new in 3.2.0? =` subsection (current version 4.0.0). Remove it; rely on Changelog. (RESEARCH Pitfall 4)
- Version sync ALREADY clean at 4.0.0 across wp-sudo.php header + WP_SUDO_VERSION, phpstan-bootstrap.php, tests/bootstrap.php, readme.txt Stable tag. This plan ASSERTS agreement; do NOT bump.
- docs/release-status.md: latest *tagged* release is 3.4.0; 4.0.0 lives on the current (merged) branch. Stable tag 4.0.0 is correct for pre-release state. Do NOT fabricate a 4.0.0 release date.
- Changelog in readme.txt starts at 3.1.3 — there is NO `= 4.0.0 =` Changelog entry yet. This is a RELEASE-TIME task; do NOT fabricate one here. It belongs in the ORG-07 checklist (plan 14-03).
- Playground blueprint link references v3.4.0 — this is CORRECT now (current tag). Do NOT change it; updating at 4.0.0 tag time is an ORG-07 checklist item.
- License: wp-sudo.php `GPL-2.0-or-later`, License URI SPDX (valid), composer.json matches — all consistent. ASSERT only.
- Plugin header vs readme title can trigger Plugin Check `plugin_name_mismatch`. USER DECISION: set BOTH the header (`Plugin Name:`) and the readme title (`=== … ===`) to **"Sudo Security"** (≥5 latin letters, no "WordPress" — satisfies Plugin Check's `plugin_header_unsupported_plugin_name` rule that "Sudo" alone fails). Live header is currently "WP Sudo" (Codex's a1fb4d7 quick revert); change it to "Sudo Security".
- Upgrade Notice `= 4.0.0 =` is 761 chars (display-truncated ~300, not a validator error). Trimming is OPTIONAL/discretion — see Task 1.
- Contributor `dpknauss` vs Author URI `danknauss` — MANUAL .org verification, cannot be done from CLI; goes in the ORG-07 checklist (plan 14-03), NOT fixed blindly here.
</verified_research_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix readme.txt — trim short description, remove stale subsection</name>
  <files>readme.txt</files>
  <action>
    1. VERIFY-FIRST (Codex 4e40635 likely already did this): run `awk 'NR==12' readme.txt | wc -c`. If < 151, the short description already passes — leave Codex's text as-is, do NOT overwrite. Only if ≥151, trim it to a <=150-char version keeping the guard-door / govern-inside value prop (fallback draft in research findings).
    2. Remove the stale `= What's new in 3.2.0? =` Description subsection entirely (RESEARCH Pitfall 4).
       Do NOT replace it with a 4.0.0 "what's new" block — the Changelog/Upgrade Notice already carry that.
    3. Set the readme title `=== Sudo ===` → `=== Sudo Security ===` to match the plugin header (Task 2) and avoid Plugin Check `plugin_name_mismatch`. (The public .org listing title becomes "Sudo Security"; UI/brand strings stay "Sudo" — that's plan 14-02. If the user later wants the .org title to read just "Sudo", that's a separate decision that reintroduces the ≥5-letter/mismatch tension.)
    4. DO NOT touch: the Changelog (no fabricated `= 4.0.0 =` entry — release-time task),
       the Playground v3.4.0 blueprint link (correct now), Stable tag (4.0.0, correct),
       or Contributors (manual .org verification deferred to ORG-07 checklist).
    5. OPTIONAL (discretion): if the `= 4.0.0 =` Upgrade Notice exceeds ~300 chars, you MAY
       trim it to a ~300-char summary ending with "See changelog for full details." Keep the
       full breaking-change meaning; do not invent new claims. Skip if trimming would distort it.
  </action>
  <verify>
    <automated>test "$(awk 'NR==12' readme.txt | tr -d '\n' | wc -c)" -lt 151 && ! grep -q "What.s new in 3.2.0" readme.txt && grep -q '^=== Sudo Security ===' readme.txt && grep -q '^Stable tag: 4.0.0' readme.txt && echo PASS</automated>
  </verify>
  <done>Short description line < 151 bytes; no "What's new in 3.2.0?" subsection; readme title `=== Sudo Security ===`; Stable tag still 4.0.0; Changelog and Playground link untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile plugin header name and assert version/license consistency</name>
  <files>wp-sudo.php</files>
  <action>
    1. Set the plugin header to `Plugin Name: Sudo Security` in wp-sudo.php (live value is
       currently "WP Sudo" from Codex's a1fb4d7 revert). This matches the readme title set in
       Task 1 (`=== Sudo Security ===`), eliminates `plugin_name_mismatch`, AND satisfies
       Plugin Check's `plugin_header_unsupported_plugin_name` rule (≥5 latin letters — "Sudo"
       alone fails; "Sudo Security" passes; "WordPress" is disallowed). USER DECISION.
       Change ONLY the `Plugin Name:` header line — do NOT touch Text Domain (`wp-sudo`,
       intentional), Plugin URI, Author URI, Version, or WP_SUDO_VERSION.
    2. ASSERT (do not modify) version-sync agreement at 4.0.0 across all four locations and
       readme Stable tag, and license consistency across wp-sudo.php / readme.txt / composer.json.
       If — and only if — the assertion finds a real mismatch, STOP and report it (per CLAUDE.md
       this phase verifies, it does not bump versions). Confirm 4.0.0 is still the correct
       pre-release state against docs/release-status.md (do NOT invent a release date).
  </action>
  <verify>
    <automated>grep -q 'Plugin Name: *Sudo Security' wp-sudo.php && [ "$(grep -hoE '4\.0\.0' <(grep 'Version:' wp-sudo.php | head -1) <(grep 'define.*WP_SUDO_VERSION' wp-sudo.php) <(grep 'define.*WP_SUDO_VERSION' phpstan-bootstrap.php) <(grep 'define.*WP_SUDO_VERSION' tests/bootstrap.php) <(grep '^Stable tag:' readme.txt) | wc -l | tr -d ' ')" = "5" ] && grep -qE 'GPL-2.0-or-later' composer.json && echo PASS</automated>
  </verify>
  <done>Header reads `Plugin Name: Sudo Security` (matches readme title); all 5 version locations agree on 4.0.0; license GPL-2.0-or-later consistent across header/readme/composer. composer test:unit passes (header-only edit, no behavior change).</done>
</task>

</tasks>

<verification>
- ORG-01: `awk 'NR==12{print length}' readme.txt` < 151 (short description).
- ORG-01: `grep -n "What.s new in" readme.txt` returns nothing.
- ORG-01/ORG-05: Plugin Check CI green on PR (no stable_tag_mismatch, no plugin_name_mismatch).
- ORG-05: version-sync grep returns 4 matching `4.0.0` values + Stable tag 4.0.0.
- ORG-05: `grep -E "License" wp-sudo.php readme.txt composer.json` consistent.
- Regression guard: `composer test:unit && composer lint` green (wp-sudo.php is a code file — pre-commit reviewer required per CLAUDE.md).
</verification>

<success_criteria>
- readme.txt short description <= 150 chars and stale "What's new in 3.2.0?" removed.
- wp-sudo.php `Plugin Name: Sudo Security` reconciled with readme title `=== Sudo Security ===`; Plugin Check name-mismatch and ≥5-letter rules both satisfied.
- Version-sync (4 locations + Stable tag) and license fields asserted consistent at 4.0.0 — no version bump.
- No fabricated 4.0.0 Changelog entry or release date; Playground link left at v3.4.0.
</success_criteria>

<output>
After completion, create `.planning/phases/14-wordpress-org-readiness/14-01-SUMMARY.md`
</output>
