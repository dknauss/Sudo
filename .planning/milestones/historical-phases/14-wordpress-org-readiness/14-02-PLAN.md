---
phase: 14-wordpress-org-readiness
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - includes/class-site-health.php
  - includes/class-gate.php
  - includes/class-admin.php
  - docs/developer-reference.md
autonomous: true
requirements: [ORG-03]
must_haves:
  truths:
    - "User-facing strings consistently present the product as \"Sudo\" (not \"WP Sudo\"), except where \"WP Sudo\" is a deliberate, documented reference"
    - "docs/developer-reference.md documents the intentional divergence between the product name (\"Sudo\"), the WordPress.org Plugin Name header (referenced, set by Plan 14-01), the slug/text-domain (\"wp-sudo\"), and the GitHub repo (\"dknauss/Sudo\")"
    - "No README.md/readme.md case-collision is introduced (git tracks only readme.md — same inode confirmed)"
    - "composer test:unit and composer lint stay green (string edits cause no PHP regressions)"
  artifacts:
    - path: "includes/class-site-health.php"
      provides: "Site Health labels/strings read \"Sudo\""
      contains: "Sudo"
    - path: "docs/developer-reference.md"
      provides: "Documented name/slug/repo divergence section"
      contains: "wp-sudo"
  key_links:
    - from: "includes/*.php user-facing strings"
      to: "product name \"Sudo\""
      via: "i18n string text"
      pattern: "Sudo"
---

<objective>
Brand/identity consistency (ORG-03): make user-facing strings present the product as **"Sudo"**, and document the intentional divergence between the product name ("Sudo"), the WordPress.org Plugin Name header ("Sudo Security" — see reconciliation note), the slug/text-domain (`wp-sudo`), and the GitHub repo (`dknauss/Sudo`).

**Output:** updated i18n strings in `includes/` + a divergence-explainer section in `docs/developer-reference.md`. Text-domain stays `wp-sudo`; slug stays `wp-sudo`. No behavior change.
</objective>

<reconciliation_note>
⚠️ Codex has already done part of ORG-03 out-of-band on this branch:
- `4e40635` renamed the plugin header (`Plugin Name`) — and the PCP fix sets it to **"Sudo Security"** (the user's choice; "Sudo" alone fails Plugin Check's ≥5-latin-letter rule). Plan 14-01 owns the header line; this plan must NOT re-edit `wp-sudo.php`'s header — only reference the final value.
- Before editing, re-grep the live tree for remaining "WP Sudo" occurrences (Codex may have changed some). Treat the counts below as the research-time baseline, not gospel.
</reconciliation_note>

<context>
@.planning/phases/14-wordpress-org-readiness/14-CONTEXT.md
@.planning/phases/14-wordpress-org-readiness/14-RESEARCH.md
@.planning/phases/14-wordpress-org-readiness/14-VALIDATION.md
@./CLAUDE.md

<interfaces>
<!-- RESEARCH-verified 2026-06-19; RE-VERIFY against live tree (Codex edits in flight). -->
- "WP Sudo" user-facing strings (research baseline ~13): includes/class-admin.php (~3), includes/class-gate.php (~6), includes/class-site-health.php (~14). Site Health labels are the most user-visible.
- README.md / readme.md are the SAME inode (macOS case-insensitive FS); `git ls-files README.md readme.md` returns only `readme.md`. NO file rename/delete needed — just assert this and do not create a second tracked file.
- Final Plugin Name header value (owned by Plan 14-01 / Codex): "Sudo Security". Slug + text-domain: `wp-sudo`. Repo: `dknauss/Sudo`.
- Divergence-doc home: `docs/developer-reference.md` (keeps this plan off readme.txt, which Plan 14-01 owns).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the current brand-string state (Codex drift guard)</name>
  <files>(read-only audit)</files>
  <action>
    Re-grep the live tree before editing — Codex may have already changed some strings:
    `grep -rn "WP Sudo" includes/ wp-sudo.php | grep -E "__\(|_e\(|esc_html__\(|esc_attr__\("`
    Enumerate the remaining translated "WP Sudo" occurrences and decide each disposition: change to "Sudo" unless it is a deliberate proper-noun reference that must stay. Confirm `git ls-files README.md readme.md` returns only `readme.md` (no case-collision action needed).
  </action>
  <verify>
    <automated>git ls-files README.md readme.md | tr '\n' ' ' | grep -qv "README.md" && echo "only readme.md tracked (no case collision)"</automated>
  </verify>
  <done>Current "WP Sudo" translated strings enumerated with a disposition each; README/readme inode situation confirmed (no action).</done>
</task>

<task type="auto">
  <name>Task 2: Normalize user-facing strings to "Sudo" (ORG-03)</name>
  <files>includes/class-site-health.php, includes/class-gate.php, includes/class-admin.php</files>
  <action>
    This is a string-rename with a regression guard, not business-logic TDD (CLAUDE.md heuristic: you can't write `expect(fn(input)).toBe(output)` for a label rename). If existing AdminTest/SiteHealth tests assert on the changed label text, update those assertions FIRST (run, watch fail in the working tree), then change the strings — one GREEN commit per task per the CLAUDE.md commit-gate rule. If no test asserts a changed label, no test change is needed; `composer test:unit && composer lint` is the regression guard.
    Replace user-facing "WP Sudo" → "Sudo" in the enumerated strings, prioritising Site Health labels (most visible). Keep the text-domain argument `'wp-sudo'` UNCHANGED in every `__()`/`_e()` call — only the displayed string text changes. Do NOT touch the plugin header (Plan 14-01). Do NOT change any non-display identifier (option keys, meta keys, hook names, CSS classes, `wp-sudo` slug).
    Per CLAUDE.md "No Test-Environment Shims", do not add accessors for testability.
  </action>
  <verify>
    <automated>composer test:unit && composer lint && ! grep -rn "WP Sudo" includes/ | grep -E "__\(|_e\(" | grep -v "wp-sudo" | grep -q "WP Sudo" && echo "no stray translated 'WP Sudo' display strings"</automated>
  </verify>
  <done>User-facing strings read "Sudo"; text-domain stays `wp-sudo`; `composer test:unit` + `composer lint` green.</done>
</task>

<task type="auto">
  <name>Task 3: Document the name/slug/repo divergence (ORG-03)</name>
  <files>docs/developer-reference.md</files>
  <action>
    Add a short section explaining the intentional divergence, sourced from the live final values (verify, do not guess):
    - Product / UI name: **Sudo**
    - WordPress.org Plugin Name header: **Sudo Security** (the `.org` listing/slug-seed title; "Sudo" alone fails Plugin Check's ≥5-letter rule, and "WordPress" is disallowed in the name)
    - Slug + text-domain: **wp-sudo** (stable; do not change — would break translations and the directory URL)
    - GitHub repo: **dknauss/Sudo**
    Explain why each differs and that this is deliberate. Keep it factual; cite Plugin Check's header rule.
  </action>
  <verify>
    <automated>grep -q "wp-sudo" docs/developer-reference.md && grep -qi "Sudo Security\|Plugin Name" docs/developer-reference.md && echo "divergence documented"</automated>
  </verify>
  <done>developer-reference.md has a divergence section naming all four forms (product, header, slug/text-domain, repo) with rationale; values match the live tree.</done>
</task>

</tasks>

<verification>
- `composer test:unit && composer lint` green (no PHP regressions from string edits).
- `grep -rn "WP Sudo" includes/` shows only deliberate, documented references (if any).
- `git ls-files README.md readme.md` → only `readme.md` (no case collision).
- developer-reference.md documents the Sudo / Sudo Security / wp-sudo / dknauss/Sudo divergence.
- Plugin Check CI green (depends on Plan 14-01 header fix landing first — hence depends_on: [01]).
</verification>

<success_criteria>
- ORG-03: user-facing strings read "Sudo"; slug/text-domain intentionally remain `wp-sudo`; GitHub URLs reflect `dknauss/Sudo`; the product-name vs Plugin-Name-header vs slug vs repo relationship is documented as intentional divergence.
</success_criteria>

<output>
After completion, create `.planning/phases/14-wordpress-org-readiness/14-02-SUMMARY.md`. Record which "WP Sudo" strings were changed vs intentionally kept, and note reconciliation with Codex's `4e40635`/header work.
</output>
