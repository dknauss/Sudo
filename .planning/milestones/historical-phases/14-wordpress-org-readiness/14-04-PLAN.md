---
phase: 14-wordpress-org-readiness
plan: 04
type: execute
wave: 3
depends_on: [01]
blocked_by_external: "Phase 13.1 EXECUTION (Access-tab UI change) + browser-capable session"
files_modified:
  - .wordpress-org/screenshot-1.png
  - .wordpress-org/screenshot-2.png
  - .wordpress-org/screenshot-3.png
  - .wordpress-org/screenshot-4.png
  - .wordpress-org/screenshot-5.png
  - readme.txt
autonomous: false
requirements: [ORG-02]
must_haves:
  truths:
    - "Every .wordpress-org screenshot shows current UI — no removed or pre-13.1 Access-tab state"
    - "The Access-tab screenshot reflects the Phase 13.1 user-picker + plain-English labels (captured AFTER 13.1 executes)"
    - "readme.txt == Screenshots == captions match the screenshot file set and order"
    - "Recovery mode was OFF during capture (representative UI, not a recovery banner)"
  artifacts:
    - path: ".wordpress-org/screenshot-5.png"
      provides: "Current Access tab screenshot (post-13.1 UI)"
      contains: "(binary image — manual verification)"
    - path: "readme.txt"
      provides: "Screenshot captions matching the file set"
      contains: "== Screenshots =="
---

<objective>
Capture a current, accurate screenshot set for the WordPress.org listing (ORG-02):
Settings → Sudo tabs, the **Access tab** (post-Phase-13.1 UI), the Session Activity
dashboard widget, and the Request/Rule Tester — plus confirm banner/icon are fine —
and reconcile readme.txt `== Screenshots ==` captions to the file set.
</objective>

<blocker_notice>
🚫 THIS PLAN IS NOT AUTONOMOUSLY EXECUTABLE. Two hard gates:

1. **Gated on Phase 13.1 EXECUTION (not just planning).** Phase 13.1 Plan 01 changes the Access-tab grant UI (numeric ID field → admin-scoped user picker; raw slugs → plain-English labels). Capturing the Access-tab screenshot before 13.1 lands produces an immediately-stale asset. Do NOT start this plan until 13.1-01 is merged/executed.
2. **Requires a browser-capable session.** Per CLAUDE.md, screenshot/browser work CANNOT be done from a standard (non-browser) agent session. The executor must hand off to a fresh browser-capable session (`/Users/danknauss/bin/claude-playwright` or `claude-browser-handoff`) or capture manually. execute-phase must treat this as a human/handoff action, not an auto task.

Also: **restore Studio recovery mode OFF first** — `WP_SUDO_RECOVERY_MODE` is currently ON in Studio `wp-config.php` (backup at `wp-config.php.wpsudo-bak`). A recovery-mode banner in screenshots is not representative. Restore the backup before capture.
</blocker_notice>

<context>
@.planning/phases/14-wordpress-org-readiness/14-CONTEXT.md
@.planning/phases/14-wordpress-org-readiness/14-RESEARCH.md
@.planning/phases/14-wordpress-org-readiness/14-VALIDATION.md
@./CLAUDE.md

<interfaces>
<!-- RESEARCH-verified 2026-06-19. -->
- Existing assets in .wordpress-org/: banner-1544x500.png, banner-772x250.png, icon-128x128.png, icon-256x256.png (likely fine — verify, probably no recapture), screenshot-1..7.png.
- screenshot-1.png is stale (dated Apr 20) — audit for removed UI (e.g., a removed governance "compatibility mode" screen or pre-13.1 Access field).
- screenshot-5 is the Access tab per readme captions — MUST be recaptured after 13.1.
- readme.txt `== Screenshots ==` captions (Plan 14-01 may have adjusted the readme; coordinate — this plan owns only the caption↔file correspondence). depends_on: [01].
- Live env: WordPress Studio (recovery mode currently ON; restore wp-config.php.wpsudo-bak before capture).
</interfaces>
</context>

<tasks>

<task type="manual">
  <name>Task 1: Audit the existing screenshot set against current UI</name>
  <files>.wordpress-org/screenshot-*.png, readme.txt</files>
  <action>
    Review each existing screenshot vs the live UI and the readme captions. Flag: screenshot-1 (stale Apr 20) and screenshot-5 (Access tab — will be stale after 13.1). Decide which need recapture vs which are still accurate. Confirm banner/icon are current (likely yes). Produce the recapture shot list.
  </action>
  <verify>
    <automated>MISSING — manual visual audit; result recorded in SUMMARY.</automated>
  </verify>
  <done>A recapture list exists naming each screenshot to redo and why; banner/icon confirmed current or flagged.</done>
</task>

<task type="manual">
  <name>Task 2: Capture current screenshots (browser handoff; AFTER 13.1)</name>
  <files>.wordpress-org/screenshot-1.png … screenshot-5.png</files>
  <action>
    In a browser-capable session, with Phase 13.1 executed and recovery mode OFF:
    capture the agreed shots — Settings → Sudo tabs, the post-13.1 Access tab (user picker + plain-English capability labels), the Session Activity dashboard widget, and the Request/Rule Tester. Save with the existing `.wordpress-org/screenshot-N.png` naming so caption order is preserved. Keep dimensions/consistency aligned with the existing set.
  </action>
  <verify>
    <automated>MISSING — manual/browser capture; verified by visual review + matching caption count in Task 3.</automated>
  </verify>
  <done>Recaptured screenshots saved under .wordpress-org/ with stable names; no stale/removed UI remains; Access-tab shot shows the 13.1 UI.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile readme captions to the screenshot set (ORG-02)</name>
  <files>readme.txt</files>
  <action>
    Ensure readme.txt `== Screenshots ==` captions match the final screenshot files in order (count and content). Coordinate with Plan 14-01 (owns readme.txt) — apply caption edits after 14-01's readme changes land to avoid conflicts.
  </action>
  <verify>
    <automated>SCOUNT=$(ls .wordpress-org/screenshot-*.png | wc -l | tr -d ' '); CCOUNT=$(awk '/== Screenshots ==/{f=1;next} /^== /{f=0} f&&/^[0-9]+\./{c++} END{print c+0}' readme.txt); [ "$SCOUNT" = "$CCOUNT" ] && echo "caption count ($CCOUNT) matches screenshot files ($SCOUNT)"</automated>
  </verify>
  <done>readme `== Screenshots ==` caption count and order match the .wordpress-org screenshot files.</done>
</task>

</tasks>

<verification>
- Manual/visual: every screenshot shows current UI; Access-tab shot reflects the 13.1 user-picker + labels; no recovery banner.
- `== Screenshots ==` caption count == screenshot file count (automated check in Task 3).
- Banner/icon confirmed current.
</verification>

<success_criteria>
- ORG-02: .wordpress-org/ contains current screenshots of the live UI (Settings tabs, Access tab [post-13.1], Session Activity widget, Request/Rule Tester) plus banner and icon; no screenshot shows stale or removed UI; readme captions match.
</success_criteria>

<output>
After completion, create `.planning/phases/14-wordpress-org-readiness/14-04-SUMMARY.md`. Record which screenshots were recaptured, confirm recovery mode was OFF during capture, and confirm the wp-config.php.wpsudo-bak restore.
</output>
