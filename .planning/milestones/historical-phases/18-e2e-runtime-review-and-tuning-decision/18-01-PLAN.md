---
phase: 18-e2e-runtime-review-and-tuning-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/e2e-runtime-review.md
  - docs/release-status.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
must_haves:
  truths:
    - "Maintainers can see recent post-v4.2.2 GitHub Actions runtimes for every current E2E-related workflow."
    - "The runtime review compares explicit E2E job/group durations, not only whole-workflow wall time or anecdotal slowness."
    - "The final decision is either no-change or exactly one narrow no-coverage-loss follow-up action."
    - "Sparse or missing scheduled/manual workflow data is recorded as an evidence gap with repeatable refresh commands."
    - "Release and planning docs link to the durable review without making it a WordPress.org submission gate."
  artifacts:
    - path: "docs/e2e-runtime-review.md"
      provides: "Durable E2E runtime evidence, refresh commands, findings, and final decision"
      contains: "Final decision"
    - path: "docs/release-status.md"
      provides: "Canonical release-state link to E2E runtime review"
      contains: "e2e-runtime-review.md"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 18 planning status and review link"
      contains: "18-01-PLAN.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CI-01 through CI-03 completion state after execution"
      contains: "[x] **CI-01**"
    - path: ".planning/STATE.md"
      provides: "Current milestone state after Phase 18 execution"
      contains: "Phase 18"
  key_links:
    - from: "docs/e2e-runtime-review.md"
      to: "GitHub Actions"
      via: "gh run list / gh run view --json jobs refresh commands"
      pattern: "gh run view"
    - from: "docs/release-status.md"
      to: "docs/e2e-runtime-review.md"
      via: "Markdown link in release-readiness context"
      pattern: "e2e-runtime-review\\.md"
    - from: ".planning/ROADMAP.md"
      to: "docs/e2e-runtime-review.md"
      via: "Phase 18 plan/status note"
      pattern: "e2e-runtime-review\\.md"
---

<objective>
Refresh and record GitHub Actions E2E runtime evidence after `v4.2.2`, then document an evidence-based CI tuning decision.

Purpose: Prevent speculative CI churn by basing Phase 18 on measured job/group runtimes across the current E2E workflows.
Output: `docs/e2e-runtime-review.md` plus release/planning links and completed CI-01 through CI-03 planning state.
</objective>

<execution_context>
@/Users/danknauss/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-e2e-runtime-review-and-tuning-decision/18-CONTEXT.md
@.planning/phases/18-e2e-runtime-review-and-tuning-decision/18-RESEARCH.md
@.planning/phases/18-e2e-runtime-review-and-tuning-decision/18-VALIDATION.md
@.planning/phases/13.1-access-tab-ux-polish-and-ci-test-speed/13.1-02-SUMMARY.md
@.planning/phases/17-release-only-environment-assurance/17-01-SUMMARY.md
@.github/workflows/e2e.yml
@.github/workflows/e2e-nginx.yml
@.github/workflows/e2e-nginx-multisite.yml
@.github/workflows/e2e-sqlite.yml
@.github/workflows/e2e-visual.yml
@package.json
@docs/release-status.md

<interfaces>
<!-- Workflow/job contracts the executor should use directly. Do not infer different group names. -->

Current E2E workflow inventory:
- `.github/workflows/e2e.yml` — workflow name `E2E Tests`; four matrix jobs named `E2E Tests N/4 (...)`; final required gate job named exactly `E2E Tests` must be excluded from runtime comparison.
- `.github/workflows/e2e-nginx.yml` — workflow name `E2E Nginx Smoke`; real smoke job named `E2E Nginx Smoke (run)`; final gate job named exactly `E2E Nginx Smoke` must be excluded from runtime comparison.
- `.github/workflows/e2e-nginx-multisite.yml` — workflow name/job `E2E Nginx Multisite Smoke`; scheduled/manual only.
- `.github/workflows/e2e-sqlite.yml` — workflow name `E2E SQLite Smoke`; job `E2E Stack Smoke (Playground SQLite)`; scheduled/manual only.
- `.github/workflows/e2e-visual.yml` — workflow name `E2E Visual Baselines`; job `E2E Visual Baselines (Apache + MariaDB, non-blocking)`; scheduled/manual and non-blocking.

Decision contracts from Phase 18 context:
- Use GitHub Actions as authoritative evidence; do not use local Playwright timings for the final CI tuning decision.
- Use job/group durations from `startedAt` and `completedAt`.
- Treat a long pole as material when a group/job is at least about 25% slower than comparable E2E peers or repeatably dominates the workflow critical path.
- Prefer two recent completed observations before declaring a long pole real; caveat single observations instead of overfitting.
- If data is missing or sparse, record the gap and exact refresh/dispatch commands; do not block Phase 18 solely on missing scheduled workflow data.
- Do not update `docs/wporg-submission-checklist.md`; Phase 18 is CI evidence, not a WordPress.org submission gate.
- Do not run browser automation or local Playwright, and do not restructure CI workflows in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh GitHub Actions runtime evidence and start the review document</name>
  <files>docs/e2e-runtime-review.md</files>
  <action>
    Create `docs/e2e-runtime-review.md` and populate the evidence sections before writing any tuning conclusion.

    Use `gh` and `jq` to refresh post-`v4.2.2` Actions data for all five E2E workflows:
    - `e2e.yml`
    - `e2e-nginx.yml`
    - `e2e-nginx-multisite.yml`
    - `e2e-sqlite.yml`
    - `e2e-visual.yml`

    Use `dknauss/Sudo` as the repository and a post-release lower bound of `2026-06-28T18:22:50Z` unless a later, clearly documented cutoff is more appropriate from the refreshed run inventory. Query run inventory with `gh run list -R dknauss/Sudo --workflow "$wf" --limit 20 --json databaseId,displayTitle,event,headBranch,status,conclusion,createdAt,updatedAt,url`. Expand each completed run with `gh run view -R dknauss/Sudo "$run_id" --json databaseId,workflowName,displayTitle,event,headBranch,conclusion,createdAt,url,jobs` and compute job seconds as `completedAt - startedAt` using `jq fromdateiso8601`.

    In the doc, include:
    - collection date and exact refresh commands;
    - workflow inventory for all five workflow files;
    - run inventory with run IDs, URLs, branch/ref, event, status, conclusion, and date;
    - job/group runtime table with seconds and minutes;
    - evidence gaps for workflows with zero or one comparable post-tag observations;
    - optional manual-dispatch commands for sparse scheduled/manual workflows.

    Exclude gate/helper jobs from comparable runtime analysis: `Detect code changes`, exact `E2E Tests`, and exact `E2E Nginx Smoke`. Keep failed/cancelled/dependency-failure runs separate from successful green-run performance so they do not distort the long-pole table. Do not run local Playwright or use browser automation.
  </action>
  <verify>
    <automated>gh run list -R dknauss/Sudo --workflow e2e.yml --limit 1 --json databaseId,status,conclusion,createdAt,url</automated>
    <automated>test -f docs/e2e-runtime-review.md</automated>
    <automated>grep -q "gh run view" docs/e2e-runtime-review.md</automated>
    <automated>grep -q "Run ID" docs/e2e-runtime-review.md</automated>
    <automated>grep -q "E2E Tests 1/4" docs/e2e-runtime-review.md</automated>
  </verify>
  <done>`docs/e2e-runtime-review.md` records fresh GitHub Actions run/job evidence for the five current E2E workflows, with refresh commands and clear evidence gaps where data is sparse.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the long-pole rule and document the final CI tuning decision</name>
  <files>docs/e2e-runtime-review.md</files>
  <action>
    Complete the findings and decision sections in `docs/e2e-runtime-review.md`.

    Apply the locked decision rule:
    - Compare explicit E2E job/group durations, not only whole-workflow wall time.
    - Treat a long pole as material only if it is at least about 25% slower than comparable peers or repeatably dominates the workflow critical path.
    - Prefer at least two recent completed successful observations before declaring a long pole real.
    - If evidence is balanced, sparse, or complexity would exceed the gain, choose and document no-change.

    The final section must be titled `## Final decision` or `## Decision` and must choose exactly one of these shapes:
    1. `No change` — explain why extra CI complexity is not justified now.
    2. `One proposed follow-up` — name exactly one narrow, low-risk, no-coverage-loss action. If the refreshed evidence still supports the preliminary group-1 long pole, prefer a proposal-only follow-up to rebalance the existing four-group baseline E2E matrix by moving a small test slice out of group 1; do not add groups, remove specs, alter required checks, or move coverage to manual-only validation.

    If a follow-up is proposed but not implemented in this plan, include a `Phase follow-up note` subsection with the evidence, rationale, and owner/timing placeholder so Task 3 can carry it into planning state. Do not edit `.github/workflows/*.yml` in this phase plan.
  </action>
  <verify>
    <automated>grep -Eq "Final decision|## Decision" docs/e2e-runtime-review.md</automated>
    <automated>grep -Eqi "no change|no-change|one proposed|one follow-up|exactly one" docs/e2e-runtime-review.md</automated>
    <automated>grep -Eqi "no coverage|coverage-loss|coverage loss|preserve.*coverage" docs/e2e-runtime-review.md</automated>
    <automated>test -z "$(git diff --name-only -- .github/workflows)"</automated>
  </verify>
  <done>The review doc contains a threshold-based decision: either no-change or exactly one no-coverage-loss follow-up proposal, with no workflow files modified.</done>
</task>

<task type="auto">
  <name>Task 3: Link the review and finalize Phase 18 planning state</name>
  <files>docs/release-status.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    Link the completed runtime review without duplicating volatile tables.

    Update `docs/release-status.md` with a short release-readiness/CI evidence note linking to `docs/e2e-runtime-review.md`. Preserve the current WordPress.org delayed/on-hold posture and do not add the review to `docs/wporg-submission-checklist.md`.

    Update `.planning/ROADMAP.md` to mark Phase 18 complete after this plan is executed, list `18-01-PLAN.md`, and include a short link/decision summary pointing to `docs/e2e-runtime-review.md`.

    Update `.planning/REQUIREMENTS.md` to mark `CI-01`, `CI-02`, and `CI-03` complete only after the review doc and final decision exist.

    Update `.planning/STATE.md` to record Phase 18 completion and route next to Phase 19. If Task 2 proposed a follow-up action, add it as a concrete phase note or pending todo entry in STATE with evidence/rationale/owner timing; if Task 2 chose no-change, explicitly record that no follow-up todo was created because evidence did not justify complexity.
  </action>
  <verify>
    <automated>grep -q "e2e-runtime-review.md" docs/release-status.md</automated>
    <automated>grep -q "e2e-runtime-review.md" .planning/ROADMAP.md</automated>
    <automated>grep -q "18-01-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -n "\\[x\\] \\*\\*CI-01" .planning/REQUIREMENTS.md</automated>
    <automated>grep -n "\\[x\\] \\*\\*CI-02" .planning/REQUIREMENTS.md</automated>
    <automated>grep -n "\\[x\\] \\*\\*CI-03" .planning/REQUIREMENTS.md</automated>
    <automated>git diff --check</automated>
    <automated>composer verify:metrics</automated>
  </verify>
  <done>Release/planning docs link to the runtime review, CI-01 through CI-03 are marked complete, and Phase 18 state routes cleanly to Phase 19.</done>
</task>

</tasks>

<verification>
Run each verification command separately. For this docs/evidence phase, do not run local Playwright and do not wait for full E2E unless the user explicitly asks for release-grade CI confidence. Required phase-level checks:

<automated>test -f docs/e2e-runtime-review.md</automated>
<automated>grep -q "gh run view" docs/e2e-runtime-review.md</automated>
<automated>grep -Eq "Final decision|## Decision" docs/e2e-runtime-review.md</automated>
<automated>grep -q "e2e-runtime-review.md" docs/release-status.md</automated>
<automated>grep -q "e2e-runtime-review.md" .planning/ROADMAP.md</automated>
<automated>git diff --check</automated>
<automated>composer verify:metrics</automated>
</verification>

<success_criteria>
- `docs/e2e-runtime-review.md` exists and records all five E2E workflows, recent run/job evidence, refresh commands, and evidence gaps.
- The final decision follows the Phase 18 long-pole rule and is either no-change or exactly one no-coverage-loss follow-up proposal.
- No E2E workflow files are modified by default.
- `docs/release-status.md` and `.planning/ROADMAP.md` link to the review.
- `.planning/REQUIREMENTS.md` marks CI-01, CI-02, and CI-03 complete only after the review/decision is recorded.
- `.planning/STATE.md` records Phase 18 completion and routes to Phase 19.
</success_criteria>

<output>
After completion, create `.planning/phases/18-e2e-runtime-review-and-tuning-decision/18-01-SUMMARY.md`.
</output>
